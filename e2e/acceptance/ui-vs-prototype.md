# 外觀對照：測試站 vs 原型

- 目的：逐頁把測試站 `https://test.fju.roy422.dev` 的**外觀與版型**跟原型 `https://fju-prototype.roy422roy.workers.dev` 比，列出差異。畫面以原型為準（前台已定稿；後台照原型）。
- 前提：測試站已部署最新 main；Doppler stg 有 `E2E_ADMIN_EMAIL`、`E2E_ADMIN_PASSWORD`。
- 帳號：E2E 管理員（帳密在環境變數，不要寫在這裡）。老師與學生頁需要老師、學生身分，測試站**沒有現成的測試帳號**，第 2–4 步自己建（都帶 `CODEX-` 前綴），最後收尾停用。
- **原型只能看**：原型是假資料的展示站，不用登入。只開頁面、截圖、用它的「切換角色」；不要在原型上填表或按送出。原型的後台直接開網址 `/dashboard/<角色>/…` 就行（`<角色>`＝`admin`、`teacher`、`student`），也可以用頂列右上帳號選單的「切換角色（原型）」。
- **原型是假資料：內容不同不算差異。** 名字、數字、筆數、日期、文章內容、圖表的值不一樣都不用記。只比：
  - 版型：欄數、區塊排列順序、寬度、卡片／表格／清單的形式；
  - 側欄與頂列：有沒有、位置、寬度、項目分組與順序、圖示、頁名、通知鈴鐺、帳號選單；
  - 配色：是否只有系網橘一個主軸色（按鈕、選取、連結、重點數字），有沒有多出別的顯眼顏色；深淺底色是否一致；
  - 元件樣式：按鈕、輸入框、標籤（badge）、表格、對話框觸發鈕的形狀、圓角、陰影、邊框；
  - 字級與字重：頁標題、區塊標題、內文、表格字；
  - 間距：頁面內距、卡片間距、表格列高；
  - 缺少的區塊（原型有、測試站沒有）與多出的東西（測試站有、原型沒有）。
- **測試站是空狀態**（還沒有資料，只顯示「目前沒有……」）時，只比外框（側欄、頂列、標題區、空狀態卡片的樣式），並在「看到什麼」註明「測試站空狀態」。原型沒有對應頁（404 或空白）就記「原型沒有對應頁」，結果記「明顯不同」——**例外**：下面步驟裡明寫「原型沒有這頁」的（第 15 步屆別），照那一步的寫法比。
- **下面「已知規則差異」表裡的差異不算「明顯不同」也不算「小差異」**：看到了就在「看到什麼」寫「已知差異：<表裡的編號>」，其他差異照常比、照常分級；一頁若只有表裡的差異，結果記「一致」。表裡沒有的，一律照實記。
- 工作階段（Playwright CLI 的 `--session`）：`proto`（原型，不登入）、`visitor`（測試站訪客，不登入）、`admin`（E2E 管理員）、`teacher`（這一輪的老師）、`student`（這一輪的學生）。
- 用臺灣時間取一個代號 `<T>`＝月日時分（例如 `09251430`），下面的 `<T>` 一律換成它。自己編一組這一輪的測試密碼（至少 12 個字元），**測試密碼與臨時密碼都不要寫進報告**。

## 已知規則差異（不算明顯不同）

以下是各票審查已裁定為「規則已改」或「資料不同」的差異（外觀聽原型、內容與行為聽 Vault；2026-09-25 外觀對照裁定整理）。只有表裡寫的那一點算已知；同一頁其他地方的差異照常比。

| 編號 | 步驟 | 頁面 | 已知差異（測試站跟原型不同，但是對的） | 依據 |
|---|---|---|---|---|
| K1 | 5 | 首頁 | hero 標題文案不同 | #289 審查（comment 5826778762） 衝突 1 |
| K1b | 5–12 | 前台頂列（訪客） | 未登入時頂列不列「產學合作」「檔案下載」 | #289 審查 衝突 2 |
| K1c | 5 | 首頁（**只限桌面 1440**） | 「優秀專題」是 4 張靜態卡＋「查看更多」，沒有輪播的左右箭頭與圓點（手機 390 見 K1d） | #307 審查（comment 5829703279） |
| K1d | 5 | 首頁（**只限手機 390**） | 「優秀專題」是橫向滑動、一次一張（下面一行「左右滑動看其他 N 件」），沒有輪播的左右箭頭與圓點 | #316 審查（comment 5832147822） |
| K2 | 7 | 註冊頁 | 多出說明句、多「系級」欄；`/register/pending` 不顯示名單比對結果、不提寄信 | #289 審查 衝突 3 |
| K3 | 8 | 最新公告 | 列表沒有置頂大卡、沒有分頁；內容頁沒有「相關專題事務」色塊與上一則／下一則；分類 pill 的順序依資料 | #289 審查 衝突 6、7；分類順序屬資料不同 |
| K4 | 10–12 | 檔案下載、產學合作、本人帳號頁 | `/files` 以資源項目為單位；`/industry` 沒有老師篩選、即時搜尋、題目欄；`/account` 登入 Email、姓名、學號不能自己改；`/files` 手機版分類是橫排 pill（原型直列）；`/files` 左側分類清單與順序來自示範資料（原型另有「114 學年度」「113 學年度」等分類）；`/account` 欄位組成依角色與 Vault 01（管理員沒有屆別欄、身分顯示在帳號狀態的標籤；登入 Email 唯讀），Google 還沒連結時「登入方式」列顯示「連結 Google」按鈕 | #289 審查 衝突 4、9、10 與非阻擋第 2 點；分類清單與欄位組成屬資料不同 |
| K5 | 13 | 系辦首頁 | 5 個磚（多「儲存與備份」）；逾期按收件者算；沒有「催繳」按鈕、沒有「訂閱到 Google 日曆」；圖例只有截止與活動；多「本屆階段」四段列；「例外組別」列改成「進行中提案」 | #294 審查（comment 5827140322） B-1～B-7 |
| K6 | 14 | 帳號管理 | 待審核獨立區塊（申請／名冊並列）、多孤兒帳號磚、「新增老師」與「發臨時密碼」 | #296 審查（comment 5827573069） B-5 |
| K7 | 15 | 屆別 | 原型沒有這頁；測試站沿用後台同一套版型（見第 15 步） | #285 票文、#296 審查 |
| K8 | 16、36 | 時間軸 | 四段在一個對話框編輯、沒有「新增階段」；保留獨立活動；展開只顯示天數；沒有階段說明 | #299 審查（comment 5827617541） B-3～B-5、#291 審查（comment 5827080511） C-5 |
| K9 | 17 | 分組總覽 | 沒有「例外組」；原型兩塊之後多「進行中的提案」「最近終止的提案」兩塊（同樣的 Panel）；「全部組別」表的欄位依模組 03 §6（改類型／重選／解除連結、成立時間、組員信箱），沒有「專題題目」欄 | #296 審查 B-1（與 PR 內文截圖表） |
| K10 | 18 | 專題事務工作台與收件名單 | 種類篩選是 公告／資源／文件繳交／專題規則；名單三分類、沒有催繳／重開對話框 | #299 審查 B-2、B-6 |
| K11 | 19 | 內容編輯器 | 沒有 Markdown 工具列、沒有公開範圍欄；多分類與封面；側欄沒有「內容編輯器」；新建頁還沒填標題時，第 4 段檢查表只列被擋的那一項（紅列＋「回去補」）＋一列「其他項目…」灰字，補好標題後才是完整檢查表；檢查列的項目依正式碼規則（沒有公開範圍、有所屬階段與時間）；訪客／學生／老師視角是頁內切換 | #299 審查（PR 內文小差異）、B-1；#312 審查（comment 5830757761）；#309 審查（comment 5829646667） |
| K12 | 21 | 成績管理 | 每組每階段份數由系辦設定（不是固定兩位）；各組成績表每列列全部階段＋最終成績＋註記，更正／退回在「計算明細」頁；表上方有篩選鈕（階段／組別／完成狀態）與匯出；「評分要求與指派」與「版本紀錄」收在對話框；評分方案區多一行等第對照 | #296 審查 B-2、#313 審查（comment 5830900121） |
| K13 | 22、30、35 | 通知匣（三角色） | 三角色通知匣都有屆別篩選 pill（全部／全站…）；系辦通知匣另有「發一則測試通知」卡（測試站專用）；通知列是標題連結＋「標為已讀」小字 | #299 審查 B-9；#292、#291 的 PR 內文列為票 12 功能，兩支審查都通過（#292 comment 5827081939） |
| K14 | 22a | 檔案管理 | 唯讀：沒有「上傳資源」與刪除鈕；類型篩選是膠囊 pill（原型是篩選鈕＋欄位選單＋分頁）；表格捲動、最多 500 筆 | #299 審查 B-7、PR 內文小差異 |
| K15 | 20 | 產學合作 | 系辦頁沒有「新增合作案」（合作案由老師建） | #296 審查 B-4 |
| K16 | 23、39 | 簽核／同意書 | 兩個用途分頁；沒有同意書 PDF 區塊、每列沒有重置鈕；學生頁放同意全文＋附件，投了不能改；「各組進度」每列多「最後事件」時間與版本／狀態標籤 | #296 審查 B-3、#291 審查 C-3、C-4 |
| K17 | 24 | 操作紀錄 | 多「系統」分頁（系統與背景工作的紀錄歸這裡） | #296 審查 B-6 |
| K18 | 25–30 | 老師後台 | 摘要用「份」不用「組」；不寫「占總成績 60%」 | #294 審查 B-8、B-11 |
| K19 | 31–39 | 學生後台 | 側欄多「成績」「通知」；「組員確認」分母是提案邀請數；座位數照本屆人數設定；選中的篩選 pill 是深藍 | #291 審查 C-1、C-2、C-6、C-7，#294 審查 B-9 |
| K20 | 32 | 我的組別 | 多「組別類型」與「提案紀錄」（提案制） | #291 審查（PR 內文比對表） |
| K21 | 13–30 | 系辦／老師側欄 | 多出「精選」「屆別」「各組繳交」「操作紀錄」這幾項 | #299 審查 B-10 |
| K22 | 有屆別膠囊列的頁 | 各頁 | 多一排「選擇屆別」膠囊（原型只有一屆；測試站多屆並存時才出現） | #296 共用元件 `CohortPills`、#299 審查 B-9 |
| K23 | 11 | 產學合作內容頁（`admin` 工作階段） | 系辦或案主看合作案內容頁，多一顆「到合作案管理」按鈕（訪客與學生看不到） | 主控台裁定保留（2026-09-25 第三輪外觀對照） |

「comment 數字」是該 PR 審查留言的編號（PR 頁網址後面接 `#issuecomment-<數字>`；清單裡不能寫 GitHub 網址，`ops/codex-e2e.sh` 會擋）。

**已登記待修、不是已知差異**（照實記、照常分級，在「看到什麼」加一句「已登記待修」即可）：目前沒有。這裡沒列的差異一律照實記。

## 每一頁怎麼比（第 5 步以後每一步都照這個做）

1. 兩個視窗寬度各做一次：**桌面 1440×900**、**手機 390×844**（Playwright CLI 用 `resize`；換寬度後重新整理再截圖）。
   **原型每次截整頁圖之前，先慢慢捲到頁底、再捲回頂端**（每次捲大約一個畫面高、停一下）：原型的區塊有進場動畫（`.reveal`），沒被捲到過的區塊是透明的，整頁圖會截成一片空白。第 5 步首頁尤其如此。截完若還有整片空白，再捲一次重截；仍然空白才照實記。
2. 每個寬度各截兩張整頁圖：原型那一張、測試站那一張，檔名：
   - `screenshots/NN-<英文頁名>-proto-1440.png`、`screenshots/NN-<英文頁名>-test-1440.png`
   - `screenshots/NN-<英文頁名>-proto-390.png`、`screenshots/NN-<英文頁名>-test-390.png`
   （NN 是步驟編號。環境裡有 ImageMagick 或 Python PIL 的話，另外合成左右並排的 `NN-<英文頁名>-side-1440.png`、`NN-<英文頁名>-side-390.png`；沒有就不用。）
3. 一張一張對著看，照上面「只比」的項目列出差異；手機寬另外看側欄是否收成選單鈕、表格是否變卡片或能橫向捲動、有沒有整頁橫向捲動。
4. 這一步的「結果」只填三級之一：
   - **明顯不同**：版型、側欄／頂列、主軸色、缺整個區塊這類一眼看得出的不同；
   - **小差異**：字級、間距、圓角、陰影、圖示、按鈕樣式這類細節；
   - **一致**：看不出外觀上的差別。

   **要評「明顯不同」之前，先對上一輪**（減少同一個畫面兩輪評出不同等級）：上一輪是這一輪輸出目錄的上一層（`../`）裡，名稱以 `-ui-vs-prototype` 結尾、時間最新、但不是這一輪的那個資料夾。打開它同一步的 `screenshots/NN-<英文頁名>-test-*.png` 與 `-proto-*.png`（1440 與 390）和 `report.md` 同一步那一列：
   - 先把上一輪「看到什麼」裡**這一輪已進「已知規則差異」表**的差異劃掉，再照剩下的差異重看上一輪的等級（剩下沒有差異就是「一致」）。
   - 測試站**與原型**兩半都跟上一輪**看起來沒變**（同樣的區塊、欄位、列數、排列），而且上一輪的原型截圖是完整的（不是沒捲到的空白）→ **沿用上一步重看後的等級**，「看到什麼」開頭寫「沿用上輪等級（兩半都未變）」。只要有一半變了（例如測試站修過、或這一輪原型截到上一輪沒截到的區塊），就照常評。
   - 任一半有變、上一輪原型截圖不完整、或找不到上一輪 → 照常評。
   上一輪只拿來對照，不要改、不要刪它的任何檔案。
5. 「看到什麼」欄寫差異清單（最多 5 點，最重要的先寫，桌面與手機分開寫，例如「桌面：測試站側欄沒有分組標題；手機：頂列沒有鈴鐺」）；「截圖」欄寫 test-1440 那一張，其他三張檔名照規則推得出來。

## 報告格式（這份清單專用）

- 報告表格照腳本給的格式，但「結果」欄填「明顯不同／小差異／一致」（第 1–4 步與收尾步驟照常填「通過／不通過」）。
- 表格之後，加一節 `## 差異彙整`，一張表：

| 頁面 | 原型路由 | 測試站路由 | 桌面 | 手機 | 最主要的差異 |
|---|---|---|---|---|---|
| 首頁 | `/` | `/` | 小差異 | 明顯不同 | …… |

- 最後一行寫：`總結：明顯不同 A、小差異 B、一致 C`（只算頁面對照的步驟；桌面與手機取比較差的那一級）。

## 準備

1. **管理員登入**（工作階段 `admin`）
   - 做：打開 `https://test.fju.roy422.dev/login` 用 E2E 管理員登入，再打開 `https://test.fju.roy422.dev/dashboard/admin`。
   - 預期：看到歡迎色塊「歡迎回來，E2E 測試管理員」。
2. **建一位老師**（工作階段 `admin`）
   - 做：打開 `https://test.fju.roy422.dev/dashboard/admin/accounts`，按「新增老師」，「登入 Email」填 `codex-ui-<T>-teacher@example.com`、「姓名」填 `CODEX-UI-<T>老師`，勾「當面核對學生證或其他身分證件」，按「建立並產生臨時密碼」；把臨時密碼**只記在心裡**，按「關閉」。**臨時密碼顯示時不要截圖。**
   - 預期：出現「只顯示這一次」與臨時密碼。
3. **老師第一次登入**（工作階段 `teacher`）
   - 做：`/login` 用老師的 Email 與臨時密碼登入；改密頁「目前的一次性密碼」填臨時密碼、「新密碼」「再輸入一次新密碼」填測試密碼，按「設定新密碼」；補資料頁「手機」填 `0911-111-111`，按「儲存並進入老師首頁」。
   - 預期：最後到 `/dashboard/teacher`，標題「歡迎回來，CODEX-UI-<T>老師」。
4. **建一位學生**（工作階段 `student`，核准用 `admin`）
   - 做：打開 `https://test.fju.roy422.dev/register`，填姓名 `CODEX-UI-<T>學生`、學號 `9<T>01`、系級 `資管二甲`、手機 `0912-345-678`、登入 Email `codex-ui-<T>-student@example.com`、密碼與確認密碼填測試密碼，按「送出註冊」。接著 `admin` 在帳號頁按「審核 CODEX-UI-<T>學生」，勾「當面核對學生證或其他身分證件」；這位學生不在名單上，要指定屆別：有預選就用預選的那一屆，沒有預選就選一個 `CODEX-` 開頭的屆別（都沒有就選列表第一個），**不要去改屆別旗標**。按「核准」再「關閉」。最後把 `student` 工作階段 `close` 再重新 `open`（換掉待審時的登入狀態），用學生的 Email 與測試密碼登入。
   - 預期：學生登入後到 `/dashboard/student`，標題「歡迎回來，CODEX-UI-<T>學生」。

## 前台（原型與測試站路由相同）

測試站用 `visitor` 工作階段（不登入）；第 10、11 步的「檔案下載」「產學合作」要登入才看得到內容，測試站改用 `admin` 工作階段。

5. **首頁 `/`**（英文頁名 `home`）。原型整頁截圖前一定先捲到頁底再捲回頂（第二輪就是沒捲到，下半段截成空白）。
6. **登入頁 `/login`**（`login`）
7. **註冊頁 `/register`**（`register`）
8. **最新公告 `/news`**（`news`）；列表有公告的話，再各點第一則看內容頁 `/news/<id>`，差異一起寫在這一步。
9. **專題規則 `/rules`**（`rules`）
10. **檔案下載 `/files`**（`files`）
11. **產學合作 `/industry`**（`industry`）；列表有合作案的話，再各點第一個看 `/industry/<id>`，差異一起寫在這一步。
12. **本人帳號頁 `/account`**（`account`；測試站用 `admin` 工作階段）

## 系辦後台（原型 `/dashboard/admin/…`；測試站用 `admin` 工作階段）

**先選有資料的屆別**：E2E 管理員的預設工作屆別是驗收腳本建的空屆別。測試站頁面上方有「選擇屆別」膠囊列的（時間軸、分組總覽、專題事務、成績、通知匣、簽核等），**先點 `DEMO-114` 那顆膠囊、等頁面換好再截圖**（膠囊是連結，只換這一頁看的屆別，不改任何設定）；沒有 `DEMO-114` 就照預設的截，在「看到什麼」註明。沒有膠囊列的頁（例如系辦首頁）顯示的是預設工作屆別，資料空就照空狀態規則只比外框。**不要**為了有資料去按「設為預設工作屆別」或改任何屆別旗標——第二～四站的腳本靠它們。

13. **系辦首頁**：原型 `/dashboard/admin`、測試站 `/dashboard/admin`（`admin-home`）
14. **帳號管理**：原型 `/dashboard/admin/accounts`、測試站 `/dashboard/admin/accounts`（`admin-accounts`）
15. **屆別**：測試站 `/dashboard/admin/cohorts`（`admin-cohorts`）。**原型沒有這頁**（票 36 選了沿用後台同一套版型，見 K7），不要拿原型 `/dashboard/admin/timeline` 當對照、也不要記「原型沒有對應頁→明顯不同」。改成只比**版型元件是不是同一套**：頁標題（PageTitle）、Panel 卡片、表格、狀態 pill、按鈕的樣式，拿第 14 步帳號管理（原型與測試站）的同類元件對照；原型截圖用原型 `/dashboard/admin/accounts` 那張即可（檔名照規則取 `15-admin-cohorts-proto-*.png`）。同一套就記「一致」，元件樣式不同才記差異。
16. **時間軸設定**：原型 `/dashboard/admin/timeline`、測試站 `/dashboard/admin/timeline`（`admin-timeline`）
17. **分組總覽**：原型 `/dashboard/admin/groups`、測試站 `/dashboard/admin/groups`（`admin-groups`）
18. **專題事務工作台**：原型 `/dashboard/admin/affairs`、測試站 `/dashboard/admin/affairs`（`admin-affairs`）；工作台有項目的話，各點第一個看收件名單頁 `/dashboard/admin/affairs/<id>`，差異一起寫。
19. **內容編輯器**：原型 `/dashboard/admin/editor`、測試站 `/dashboard/admin/editor/new`（`admin-editor`）。**只看，不要按存草稿或發布。**
20. **產學合作**：原型 `/dashboard/admin/industry`、測試站 `/dashboard/admin/industry`（`admin-industry`）
21. **成績管理／評分**：原型 `/dashboard/admin/grading`、測試站 `/dashboard/admin/grading`（`admin-grading`）
22. **通知匣**：原型 `/dashboard/admin/inbox`、測試站 `/dashboard/admin/inbox`（`admin-inbox`）。**不要按「發送測試通知」。**「發一則測試通知」卡本身、通知列「標為已讀」小字、屆別篩選是已知差異（K13）。
22a. **檔案管理**（票 35）：原型 `/dashboard/admin/files`、測試站 `/dashboard/admin/files`（`admin-files`）。測試站沒有「上傳資源」與刪除鈕是已知的規則差異（檔案工作台／回收排在之後），不算「明顯不同」。**不要點下載以外的東西。**
22b. **後台深色模式**（票 35）：兩邊都按頂列右上的月亮鈕切成深色，各截一次 `/dashboard/admin/timeline`（`admin-timeline-dark`）與 `/dashboard/admin/affairs`（`admin-affairs-dark`），測試站兩頁都照上面先點 `DEMO-114`；比完再按一次（太陽鈕）切回淺色。
23. **簽核管理**：原型 `/dashboard/admin/signoff`、測試站 `/dashboard/admin/signoff`（`admin-signoff`；票 36）。「各組進度」上方的兩個用途分頁各看一次。**不要按「新增簽核」裡的建立。**
24. **操作紀錄**：原型 `/dashboard/admin/audit`、測試站 `/dashboard/admin/audit`（`admin-audit`；票 36 新頁）。點「管理員」分頁看一次。

## 老師後台（原型 `/dashboard/teacher/…`；測試站用 `teacher` 工作階段）

25. **老師首頁**：`/dashboard/teacher`（`teacher-home`）
26. **分組**：`/dashboard/teacher/groups`（`teacher-groups`）。**不要按「認領」。**
27. **各組繳交**：`/dashboard/teacher/affairs`（`teacher-affairs`）
28. **我的合作案**：`/dashboard/teacher/industry`（`teacher-industry`）。**不要新增合作案。**
29. **評分**：`/dashboard/teacher/grading`（`teacher-grading`）
30. **通知匣**：`/dashboard/teacher/inbox`（`teacher-inbox`）

## 學生後台（原型 `/dashboard/student/…`；測試站用 `student` 工作階段；第 4 步不通過就全部略過）

31. **學生首頁（含行事曆）**：`/dashboard/student`（`student-home`）
32. **我的組別**：`/dashboard/student/groups`（`student-groups`）。**不要按「公開找組員」或發起提案。**這一輪的學生是新建的、**未分組**，測試站會看到「發起提案」表單，原型的學生已經在組裡（座位卡＋找組員）——兩邊資料狀態不同是預期的（主控台裁定）。**只比外框與版型元件**：側欄、頂列、頁標題、Panel 卡片、表單輸入框、按鈕、pill 的樣式；不比區塊內容與排列（已分組的版型以 #291 的截圖為準）。組別類型與提案紀錄見 K20。
33. **作業區**：`/dashboard/student/affairs`（`student-affairs`）
34. **成績**：`/dashboard/student/grading`（`student-grading`）
35. **通知匣**：`/dashboard/student/inbox`（`student-inbox`）
36. **專題時間軸**：`/dashboard/student/timeline`（`student-timeline`；票 38 新頁）。點一下目前階段以外的一張卡片看展開，再點回來。
37. **作業內容**：作業區有收件的話，點第一個看 `/dashboard/student/affairs/<id>`（原型 `/dashboard/student/affairs/mi-011`），再點「繳交歷史」分頁（`student-affair`）。**不要按儲存草稿或正式送出。**
38. **產學合作**：`/dashboard/student/industry`（`student-industry`；票 38 新頁）
39. **同意書**：`/dashboard/student/signoff`（`student-signoff`）。**不要勾「已完整閱讀」或按同意／不同意。**

## 收尾（不論前面成敗，一定要做）

40. **停用這一輪的老師與學生**（工作階段 `admin`）
    - 做：打開 `https://test.fju.roy422.dev/dashboard/admin/accounts?q=CODEX-UI-<T>`。學生若還在待審核清單，按「審核」→ 理由填 `CODEX 外觀對照收尾` → 「退回」；已核准的老師與學生各按「停用」，理由填 `CODEX 外觀對照收尾：停用測試帳號`，按「確認停用」。**只動姓名完全等於 `CODEX-UI-<T>老師`、`CODEX-UI-<T>學生` 的兩位。**
    - 預期：兩位都是「已停用」（或學生是「已退回」）。
41. **登出**
    - 做：`admin` 按「登出」。
    - 預期：回到登入頁或首頁。

## 失敗時

- 第 1 步不通過：後台頁都做不了；前台（第 5–11 步）照做，其餘略過。
- 第 2–3 步不通過：老師頁（第 25–30 步）略過，記「沒有老師身分」。
- 第 4 步不通過：學生頁（第 31–39 步）略過，記「沒有學生身分」。
- 某一頁打不開（測試站 404／500、原型 404）：照「每一頁怎麼比」的第 4 點記「明顯不同」並寫原因，繼續下一頁。
- 收尾第 40–41 步一定要做。
- 管理員只登入**一次**、整條流程共用 `admin` 工作階段（登入限速：同 IP 同帳號 10 分鐘 10 次）。
