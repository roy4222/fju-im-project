# 149 案補充 X4：評分方案型態、平均與捨入、改派三選一的執行、失效暫存、更正待復核、新版方案預覽、老師停用

- 前提：測試站已部署含第四站票 23–26 的版本；Doppler stg 有 `E2E_ADMIN_EMAIL`、`E2E_ADMIN_PASSWORD`。這份**不動模擬業務鐘**。
- 目的：補 149 案裡評分與成績、既有第四站清單沒跑到的部分（見 [`149-coverage.md`](149-coverage.md)）。第四站只**預覽**改派三選一，這份**實際執行**「替換」與「保留」並看結果。每一步標題後面的括號是它驗的案例編號。
- 帳號：E2E 管理員（帳密在環境變數，不要寫在這裡）。2 位學生由這份清單自己註冊、管理員核准；3 位老師由管理員直接建立，用臨時密碼第一次登入後改密。
- 開始前：
  - 用臺灣時間取一個代號 `<T>`＝月日時分（例如 9 月 25 日 14:30 → `09251430`）。這一輪建的東西都用 `CODEX-` 開頭、帶 `<T>`，下面的 `<T>` 一律換成它。另外記下今天的臺灣日期 `<今天>`，「今天 ±N 天」從它算。
  - 工作階段（Playwright CLI 的 `--session`）：`admin`、`s1`、`s2`、`ta`（甲老師）、`tb`（乙老師）、`tc`（丙老師）。要清掉某個工作階段的登入狀態，就把它 `close` 再重新 `open`。
  - 自己編一組這一輪的測試密碼（至少 12 個字元），學生與老師都用它。**這組密碼與老師的臨時密碼都不要寫進報告**；臨時密碼顯示時**不要截圖**。登入頁的 Google 按鈕不要按。
  - 通知由背景工作每 30 秒投影一次：等通知的步驟**重新整理，最多等 2 分鐘**。
- 這一輪會建的資料：

| 資料 | 值 |
|---|---|
| 屆別代碼／名稱 | `CODEX-X4-<T>`／`CODEX 驗收屆 X4-<T>` |
| 學生 1、2 | 姓名 `CODEX-X4-<T>學生1`、`學生2`；學號 `8<T>01`、`8<T>02`；系級 `資管三甲`；手機 `0912-345-678`；登入 Email `codex-x4-<T>-s1@example.com`、`-s2@example.com` |
| 甲／乙／丙老師 | 姓名 `CODEX-X4-<T>甲老師`／`乙老師`／`丙老師`；登入 Email `codex-x4-<T>-ta@example.com`／`codex-x4-<T>-tb@example.com`／`codex-x4-<T>-tc@example.com` |
| 評分方案 v1 | `系統驗收` 60%：項目 `功能完整度`（分數，權重 100）＋ `出席`（通過／不通過，不計分）；`專題發表` 40%：項目 `發表內容`（分數，權重 100） |

這一輪的分數設計（畫面上的數字都是兩位小數）：

- 系統驗收：甲 80、乙 84.29 → 平均原始值 82.145 → 顯示 **82.15**（不是先把各人四捨五入再平均）。
- 專題發表：丙 90 → **90.00**。最終 82.145×60%＋90×40%＝85.287 → **85.29**。
- 系辦更正為 **88.00**；之後系統驗收把甲替換成丙、丙給 76 → 系統驗收 (84.29＋76)/2＝80.145 → **80.15**，計算出來的最終 80.145×60%＋90×40%＝84.087 → **84.09**，但更正先進「待復核」，復核沿用後採用 **88.00**。

報告裡請記下：原本的兩個旗標（第 3 步）、組別代碼 `<G>`（第 7 步）、第 11 步記下的評分頁網址。

## 步驟

### 前置

1. **背景工作在跑**（工作階段 `admin`，還沒登入）
   - 做：打開 `https://test.fju.roy422.dev/api/health`。
   - 預期：回一段 JSON，`worker.lastTickAt` 距離現在不到 2 分鐘；在「看到什麼」寫 `commit=<前 8 碼>`。
2. **管理員登入**
   - 做：打開 `https://test.fju.roy422.dev/login` 用 E2E 管理員登入，再打開 `https://test.fju.roy422.dev/dashboard/admin`。
   - 預期：看到「歡迎回來，E2E 測試管理員」。
3. **記下兩個全系旗標原本在哪一屆**
   - 做：打開 `https://test.fju.roy422.dev/dashboard/admin/cohorts`，記下「開放註冊中」與「預設工作中」的屆別代碼（沒有就寫「無」）。
   - 預期：看到標題「屆別」。**收尾要靠這一步的紀錄把旗標還回去。**
4. **建屆別、設旗標、排階段、轉進行中、分組設定**
   - 做：新增 `CODEX-X4-<T>`／`CODEX 驗收屆 X4-<T>`，設為開放註冊與預設工作屆別；時間軸「編輯階段與日期」：`成組期`、`期中`、`期末`、`成果`，開始日 今天−10、今天+30、今天+90、今天+150 天，年度結束日 今天+300 天，「儲存」；「把 CODEX-X4-<T> 轉為進行中」；分組總覽「分組設定」：最少 `2`、最多 `2`、提案預設天數 `7`，「儲存」。
   - 預期：各自出現成功訊息。
5. **兩位學生進來**
   - 做：建 `CODEX-X4-roster-<T>.csv`（`student_no,name,cohort,email,department_class` 加兩行，屆別 `CODEX-X4-<T>`），「匯入名單 CSV」→「匯入 2 筆」→「關閉」；`s1`、`s2` 在 `https://test.fju.roy422.dev/register` 註冊；`admin` 各「審核」→ 勾「當面核對學生證或其他身分證件」→「核准」→「關閉」；兩位 `close` 再 `open`，用 Email 與測試密碼登入。
   - 預期：兩位都到 `/dashboard/student`。
6. **三位老師進來**
   - 做：`admin` 帳號頁「新增老師」→「直接新增」，甲、乙、丙各建一次（勾「當面核對學生證或其他身分證件」→「建立並產生臨時密碼」→ 臨時密碼只記在心裡 →「關閉」）。`ta`、`tb`、`tc` 各用臨時密碼登入，改成測試密碼，補資料頁手機填 `0911-111-111`，「儲存並進入老師首頁」。
   - 預期：三位都到 `/dashboard/teacher`。
7. **成組、指派甲老師為主指導**
   - 做：`s1` 在 `https://test.fju.roy422.dev/dashboard/student/groups` 選「一般專題」、同學 1 `8<T>02`，「發起提案」；`s2`「確認加入」，記下 `<G>`。`admin` 分組總覽（`CODEX-X4-<T>`）`<G>` 那一列按「指派老師」，選 `CODEX-X4-<T>甲老師`，理由 `CODEX 驗收：抽籤結果`，「確認指派」。
   - 預期：`s2` 看到「全員確認，組別 <G> 成立了。」；指派出現「已指派 CODEX-X4-<T>甲老師 老師指導 <G>…」。

### 方案：型態、等第對照、權重合計

8. **等第型態有對照表；權重合計 90% 不能建立**（GRD-01）（工作階段 `admin`）
   - 做：打開 `https://test.fju.roy422.dev/dashboard/admin/grading`，屆別選 `CODEX-X4-<T>`，按「建立新方案版本」。把「專題發表」的「發表內容」型態改成「等第 A–F」，看對話框；再改回「分數」。把「專題發表」的「占總成績 %」改成 `30`，按「建立草稿」。
   - 預期：改成等第時出現「等第對照（隨版本保存）」與 A／B／C／D／F 的分數欄；改回分數後對照消失；權重 30 時標示「階段合計 90%（要 100%）」，按下出現「各階段占總成績的合計是 90%，要剛好 100% 才能建立。」，對話框沒關、沒建出版本。
9. **加一個通過／不通過項目，建立並發布 v1**（GRD-01）
   - 做：把「專題發表」改回 `40`；在「系統驗收」按「＋ 新增項目」，項目名稱 `出席`、型態「通過／不通過」。按「建立草稿」；在「方案版本」v1 那一列按「發布」。
   - 預期：「出席」那一列權重欄寫「不計分」、系統驗收的項目合計仍是 100%；出現「已建立方案 v1（草稿）」；發布後「評分方案」寫「v1・已發布」、公式「系統驗收 × 60% ＋ 專題發表 × 40%」（出席不進公式）。
10. **指派：系統驗收 2 份（甲、乙），專題發表 1 份（甲）**
    - 做：「評分要求與指派」選「系統驗收」，`<G>` 那一列要求份數填 `2` 按「儲存」，評分老師選甲按「指派」、再選乙按「指派」；「選擇階段」點「專題發表」，要求份數 `1`「儲存」，評分老師選甲「指派」。
    - 預期：各出現「已指派 … 老師評 <G>「系統驗收」」「…「專題發表」」；兩列分別寫「已正式送出 0／2 份」「0／1 份」。

### 老師評分：驗證、平均與捨入（GRD-05、GRD-12）

11. **甲老師送出系統驗收 80（出席通過）**（工作階段 `ta`）
    - 做：打開 `https://test.fju.roy422.dev/dashboard/teacher/grading`，點「CODEX-X4-<T>・系統驗收」的 `<G>`；**記下這一頁的網址**。「功能完整度」填 `80`、「出席」選「通過」，按「正式送出」→「確認送出」→「知道了」。
    - 預期：回執「已收件」含 `80.00`；重新整理後寫「已正式送出」、不能再改。
12. **沒被指派的丙老師打不開這一組的評分表**（GRD-12、GRD-02）（工作階段 `tc`）
    - 做：打開 `https://test.fju.roy422.dev/dashboard/teacher/grading`；再打開第 11 步記下的網址。
    - 預期：丙的評分佇列沒有 `<G>`；打開網址看到「你沒有被指派評這一組」（或找不到頁面），看不到甲的 80 分。
13. **乙老師：負數與漏填被擋，84.29 可以**（GRD-12、GRD-05）（工作階段 `tb`）
    - 做：打開評分工作台點 `<G>`（系統驗收）。「功能完整度」填 `-5`，按「暫存」；清空「功能完整度」、「出席」也不選，看「正式送出」；最後「功能完整度」填 `84.29`、「出席」選「通過」，「正式送出」→「確認送出」→「知道了」。
    - 預期：`-5` 時出現「第 1 項要填 0–100 的數字（最多兩位小數）」，暫存被擋（「…修正後才能暫存。」）；漏填時「正式送出」按不下去、寫「還差 N 項，填齊後才能正式送出。」；最後回執含 `84.29`。
14. **甲老師先暫存專題發表 88（不送出）**（GRD-14 前置）（工作階段 `ta`）
    - 做：在評分佇列點「CODEX-X4-<T>・專題發表」的 `<G>`，「發表內容」填 `88`，按「暫存」。**這一頁之後不要重新整理。**
    - 預期：出現「已暫存到伺服器。暫存只有你和系辦看得到，不算正式分數。」
15. **系統驗收平均 82.15（不是先捨入再平均）**（GRD-05、GRD-03）（工作階段 `admin`）
    - 做：重新整理成績管理頁，看「各組成績」`<G>` 那一列；按「計算明細」。
    - 預期：系統驗收 `82.15`「已完成」；專題發表「尚未完成」、甲那一格標「暫存中（未正式）」；最終成績「尚未完成」；計算明細系統驗收的算式含 `80`、`84.29` 與 `82.15`。

### 改派三選一（實際執行）與失效暫存（GRD-13、GRD-14）

16. **「替換」專題發表的甲 → 丙：甲的暫存失效**（GRD-13、GRD-14）
    - 做：回成績管理頁，「選擇階段」點「專題發表」，`<G>` 那一列按「移除或改派 CODEX-X4-<T>甲老師 老師」。選「替換評分老師、重新評分」，「接手的評分老師」選丙，「理由（必填，留在指派紀錄裡）」填 `CODEX 驗收：甲老師改當主指導就好`，按「確認執行」。
    - 預期：執行前頁面寫甲的暫存會標成失效（「…只有系辦查得到），不會轉給新老師」）；執行後到計算明細，寫「…CODEX-X4-<T>甲老師 老師 → CODEX-X4-<T>丙老師 老師（替換評分老師、重新評分…」；「評分紀錄」有一筆「失效的暫存」，原因含「評分指派已移除」。
17. **甲老師的舊頁面不能再存；但仍是主指導**（GRD-14）（工作階段 `ta`）
    - 做：在第 14 步**沒重新整理**的專題發表頁，把分數改成 `89`，按「暫存」；再打開 `https://test.fju.roy422.dev/dashboard/teacher/groups`。
    - 預期：暫存被拒，出現「你沒有被指派評這一組（或指派已結束），不能評分。」；分組頁 `<G>` 仍是甲指導（「你指導中」或指導老師欄寫甲）。
18. **丙老師收到指派通知並送出專題發表 90**（GRD-02）（工作階段 `tc`）
    - 做：打開 `https://test.fju.roy422.dev/dashboard/teacher/inbox`（最多等 2 分鐘）；再到評分工作台點 `<G>`（專題發表），「發表內容」填 `90`，「正式送出」→「確認送出」→「知道了」。
    - 預期：通知匣有「你被指派評分：<G>「專題發表」」；評分表沒有帶入甲的 88（暫存不轉給新老師）；回執含 `90.00`。
19. **最終加權與捨入 85.29**（GRD-06）（工作階段 `admin`）
    - 做：重新整理成績管理頁看 `<G>` 那一列，再按「計算明細」。
    - 預期：系統驗收 `82.15`、專題發表 `90.00`、最終成績 `85.29`；最終成績的算式含 `60%`、`40%`。
20. **「保留已完成評分」移除乙：份數與平均不變**（GRD-13）
    - 做：「選擇階段」點「系統驗收」，按「移除或改派 CODEX-X4-<T>乙老師 老師」，選「保留已完成評分」，「接手的評分老師」選「先不指派」，理由 `CODEX 驗收：乙老師離職，分數保留`，「確認執行」。回成績管理頁；`tb` 重新打開評分工作台。
    - 預期：系統驗收仍是「已正式送出 2／2 份」、平均 `82.15`、最終 `85.29`；乙的佇列不再有 `<G>` 的系統驗收。（改派「保留」時被移除的老師會不會收到解除通知：目前沒有這種通知，照實記下即可，不算這一步不通過。）

### 更正與待復核（GRD-08、GRD-15、NTF-17）

21. **更正最終成績 88（保留原值與理由）**（GRD-08、GRD-15）
    - 做：在 `<G>` 的計算明細按「更正最終成績」，填 `88`、理由 `CODEX 驗收：口試補考`，「確認更正」。
    - 預期：出現「<G> 的最終成績已更正為 88.00（原 85.29，原始老師輸入不變）。」；頁面寫「已更正：原 85.29 → 88.00（CODEX 驗收：口試補考」。
22. **計算基礎改變：更正進待復核，系辦收到通知**（GRD-15、NTF-17）
    - 做：成績管理頁「系統驗收」，按「移除或改派 CODEX-X4-<T>甲老師 老師」，選「替換評分老師、重新評分」，接手選丙，理由 `CODEX 驗收：重評系統驗收`；看頁面提示後按「確認執行」。回成績管理頁；打開 `https://test.fju.roy422.dev/dashboard/admin/inbox`（最多等 2 分鐘）。
    - 預期：執行前頁面提示「…那筆更正會進「待復核」。」；成績表 `<G>` 寫「更正待復核」，有「待復核的更正（1）」區；計算明細寫「更正待復核：計算基礎已改變，原更正暫不套用。」；系統驗收變「尚未完成」；系辦通知匣有「<G> 的成績更正待復核：計算基礎改變了」。
23. **丙老師送出系統驗收 76：重算，但更正仍待復核**（GRD-15）（工作階段 `tc`、`admin`）
    - 做：`tc` 在評分工作台點 `<G>`（系統驗收），「功能完整度」`76`、「出席」選「通過」，「正式送出」→「確認送出」→「知道了」。`admin` 重新整理成績管理頁與計算明細。
    - 預期：系統驗收 `80.15`（84.29 與 76 的平均）；計算出來的最終 `84.09`；仍標「更正待復核」，採用的不是 88 也不是自動套到新值（照畫面寫的採用值記下）。
24. **復核：沿用原更正值**（GRD-15）
    - 做：在「待復核的更正」選「沿用原更正值 88.00」，「復核說明」填 `CODEX 驗收：補考成績維持`，按「確認復核」。
    - 預期：出現「已處理待復核：…」；最終採用 `88.00`，「更正待復核」標示消失。

### 新版方案預覽後取消（GRD-09）

25. **方案已鎖定：建 v2 看影響後取消，不重算**（GRD-09）
    - 做：成績管理頁「評分方案」應寫「v1・已鎖定」。按「建立新方案版本」，兩個階段改成 `50`、`50`，「建立草稿」；打開「版本紀錄」，在 v2 按「看影響並套用 v2」；看完按「取消（不重算）」；回成績管理頁。
    - 預期：鎖定提示含「結構已鎖定」；套用頁標題「套用新評分方案」、「v1 → v2 的影響」、寫有幾組成績會變（並提到更正會進待復核）；取消後成績表仍用 v1、最終仍採用 `88.00`，「評分方案」仍是 v1。

### 老師停用：缺評待處理（GRD-14）

26. **停用有未完成指派的老師：顯示缺評待處理，不自動改派**（GRD-14）
    - 做：「選擇階段」點「專題發表」，`<G>` 那一列要求份數改 `2`「儲存」，評分老師選乙「指派」。到帳號頁（`?q=CODEX-X4-<T>乙老師`）按「停用 CODEX-X4-<T>乙老師」，理由 `CODEX 驗收：停用評分老師`，「確認停用」「關閉」。回成績管理頁看 `<G>`。
    - 預期：專題發表變「已正式送出 1／2 份」「尚未完成」；註記寫「專題發表：CODEX-X4-<T>乙老師（老師已停用，缺評待處理）」（或「帳號已停用，待管理員處理」）；丙的 90 仍採計，沒有自動改派給別人。

## 收尾（不論前面成敗，一定要做；只動 `CODEX-X4-<T>` 開頭的東西）

這一站沒有發布到前台的東西。評分方案、成績、組別、屆別後台沒有刪除功能，會留在測試站。

27. **停用這一輪的學生與老師**（工作階段 `admin`）
    - 做：打開 `https://test.fju.roy422.dev/dashboard/admin/accounts?q=CODEX-X4-<T>`，兩位學生與甲、丙老師各按「停用」，理由 `CODEX 驗收收尾：停用測試帳號`，「確認停用」（乙已停用就略過；還在待審的學生按「審核」→ 理由 `CODEX 驗收收尾` →「退回」）。**只動姓名完全等於上表的五位**。
    - 預期：五位都是「已停用」（或「已退回」）。
28. **把兩個旗標還回去**
    - 做：打開 `/dashboard/admin/cohorts`，照第 3 步的紀錄按回原本的屆別；寫「無」的就留在 `CODEX-X4-<T>` 上。
    - 預期：兩個標籤回到第 3 步記下的屆別。
29. **登出**
    - 做：`admin` 按「登出」（其他帳號已停用，略過）。
    - 預期：回到登入頁或首頁。

## 失敗時

- 第 2 步不通過：停止，不用收尾。第 3 步不通過：停止，不要做第 4 步以後動到旗標的步驟。
- 第 5–7 步有人沒進來或沒成組：第 8 步以後都做不了，跳到收尾。
- 第 6 步有老師沒登入成功：跳過需要他的步驟；後面的數字會對不上，照實記下看到的數字。
- 第 9 步沒發布方案：跳過第 10–26 步。
- 其他步驟不通過：記下來，繼續下一步。
- 收尾第 27–29 步一定要做；做不成的在報告寫清楚哪一筆沒收掉（旗標是全站狀態，沒還原要特別標出來）。
- 管理員只登入一次。這份註冊 2 位學生。**不要和 Playwright 站驗收或另一份 Codex 清單同時跑**。
