# S00 收尾與下一小批交接

核對日期：2026-09-15。執行追蹤：[下一批 #201](https://github.com/roy4222/fju-im-project/issues/201)；[原型依賴維護 #202](https://github.com/roy4222/fju-im-project/issues/202)。

## 已確認的基線

- #7 與 #199 已合併，同一 merge commit `3d6741de010c0c492db86aefdf0c9024aed51d43`。
- [main CI 七道通過](https://github.com/roy4222/fju-im-project/actions/runs/34963158736)。
- #33–#43 工程票已關閉；[逐票證據](../steps/S00/README.md)。
- #41 補齊故意失敗證據：[PR #200](https://github.com/roy4222/fju-im-project/pull/200)（已關閉，未合併），[run 34963728606](https://github.com/roy4222/fju-im-project/actions/runs/34963728606) unit FAILURE、其餘六道 SUCCESS。測試刻意比對不同字串。
- main 的既有 ruleset `23429589` 保留禁止刪除、禁止強推、要求 PR；已新增 typecheck、lint、unit、integration、build、e2e-smoke、audit 七項 required checks，strict 更新基線。
- E00 #18 保留 OPEN：工程檢查通過；Roy 本人展示确认尚未取得，不能替本人打勾。
- VM / GHCR 真映像部署與回滾仍 NOT_RUN；不能把 CI 稱為 CD 已完成。

## 下一小批：VM 測試站＋管理員首次登入

| 順序 | 票 | 交付與停點 |
|---|---|---|
| 環境線 | #187 | 重新探測 VM，再依 SOP 設定 Docker、目錄、帳號、網路／憑證；舊 SSH／sudo 成功不代表現在全就緒 |
| 環境線 | #188 | 完成本批必要設定；原票 Google／Turnstile 等未完成部分保留 checklist，不因局部完成關原票 |
| 1 | #44 | S01 新表、系級資料流與權限矩陣／migration；延續 S00，不重建已存在資料 |
| 2 | #45 | Better Auth 路由白名單、hooks 與運行驗證 |
| 3 | #46 | 操作身分、內部呼叫邊界、稽核與冪等帳本 |
| 4 | #47 | 從原型按需搬頁並拆元件，提供正式導覽與授權空狀態 |
| 5 | #48 | A1 一次性密碼登入、強制改密、登出／重登 |
| 展示線 | #189 | 部署指定版本、提供固定網址；原票要求的真 digest、成功／回滾及 A1 改密全完成才能關票 |

環境準備可與功能開發並行；仍遵守原票的依賴與秘密／人類操作界線。提前部署改為本批的交付目標，原完整 S14 備份、還原、監測驗收不前移也不誤關。先使用測試資料。開發資料庫、破壞性測試與展示資料庫必須隔離。

### Roy 的展示步驟

1. 打開工程交付的測試站 URL，確認所部署 commit。
2. 使用安全管道取得的 A1 一次性密碼登入（不在文件記密碼）。
3. 系統必須先導向改密；改密前不能執行其他管理功能。
4. 改密後進管理員首頁，看到正式側欄；未做功能顯示空狀態。
5. 登出，用新密碼重登；舊密碼失效。
6. 未登入直接進後台被拒絕或導登入；提供對应測試與畫面證據。

停在此處交付 review 與 Roy 操作，**不要接著實作 #49–#59、S02 或整個 S14**。產品規則沿既定設計：評分不改、系級與屆別分開、名單匯出由 #59 承接。

## 給實作者的接續 prompt

> 先讀本檔、docs/README.md、#201、#44–#48 與 #187–#189 的最新正文，以及工作區 AGENTS。從最新 main 建立隔離分支，保存既有改動；不要在有大量未提交改動的 proto/role-ux-round1 上直接開工。按專案要求核對 Matt skills 上游，開發採合適技能與相依套件本機文件。
>
> 本批目標是 VM 測試站上的 A1 首次登入→強制改密→後台→登出重登。功能票依 #44→#45→#46→#47→#48；VM 依 #187→#188→#189 前置。先重查 VM 與平台狀態；原票要求 Roy 提供的秘密或首次真部署操作仍保留，不從聊天取得或輸出密碼。若缺必要值，明確指出缺哪個設定，同時推進不受影響的程式工作。
>
> 每張票先說「這張會新增什麼能力」，實作後交白話摘要、PR/commit、實際測試、畫面／API 證據、3–5 步操作、未完成項。每個可獨立 review 的部分用小 PR，不混入原型依賴升級。不宣稱未執行測試 PASS。部署指定版本，展示頁上能辨認版本，測試不能清掉展示資料。到上述展示停點即停止，等待 review 與 Roy 操作；不自動擴到整個 S01。

## 後续節奏：有幾批？

正式 backlog 是 **15 個大切片（S00–S14）、166 張原始實作票**。S00 11 張已關閉，原始實作票尚餘 155 張；#201 是協調票、#202 是原型維護票，不混進 166 的完成率。E00 尚待本人展示，不能寫成 1/15 完整驗收。

「小批」是可 review／展示的工作量，不是固定切片數。若每批約 4–6 張，整案約 30–40 小批只能作粗估；依複雜度、依賴與修正變動，不承諾一週或固定總批數。

| 大切片 | 主題 |
|---|---|
| S00 | 工程骨架與 CI（工程票完成，本人展示待確認） |
| S01 | 帳號、名單、申請、核准與登入管理 |
| S02 | 屆別階段、業務時間、背景工作、通知 |
| S03 | 分組提案與確認 |
| S04 | 專題事務編輯與發布 |
| S05 | 個人繳交 |
| S06 | 指導老師與產學 |
| S07 | 組別繳交與老師矩陣 |
| S08 | 名單變動、截止快照與重開 |
| S09 | 換成員、組長、解散（有 S10／S11 前置，不按編號直跑） |
| S10 | 評分與成績匯出 |
| S11 | 線上簽核 |
| S12 | 公開展示、共用介面與維運頁 |
| S13 | 封存、多屆並行與下一屆 |
| S14 | VM、部署、備份、還原、監測實跑（最小部署前置提前） |

具體依賴沿用 [切片總圖](<engineering/slices/00 切片總圖與依賴.md>)。S01 可再分「#49–#53 名單到學生核准」與「#54–#59 帳號管理等」；這兩段只是後續拆批方向，尚未在本批開工。

## 完成與關票方式

- 實作中／待 review 不能寫 completed；合併且原票證據齊才關功能票。
- 需要 Roy 操作的票保留待本人確認，CI 不替代人類驗收。
- #6、#8–#17 是仍被 backlog 引用的規格來源，本次不因文件 PR 合併就批次關掉。
- 原型依賴維護 #202 單獨做；截圖中的漏洞版本先重新查證，真 Cloudflare 部署另按該票授權執行。
- 原主工作樹仍有未提交文件及部署改動，本次保留；後續整合需先備份逐檔比對，不能 reset/clean。
