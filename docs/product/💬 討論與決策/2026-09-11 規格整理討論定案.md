---
type: decision-log
project: FJU IM Project
date: 2026-09-11
confirmation: Roy 口頭確認，尚未寫回完整產品規格
---
# 2026-09-11 規格整理討論定案

來源：本日 Roy 與 Claude（Fable）三輪 grill-with-docs 對話。這些是 Roy 的口頭決定，尚未寫回 📋 完整產品規格；寫回時以本頁為來源。前一輪 Codex 討論的 Q1–Q9 已在 §22，不重列。

## 已決定

| 項目 | 決定 |
|---|---|
| 週一 9/14 目標 | 以完整版上 VM 為目標，不先縮成切片；滑落時公開內容後台維護與備份演練排最後 |
| 分工 | Claude 寫總 spec 與 ticket → Codex 審 spec → Opus 實作 → Codex 依驗收案例操作驗收 → Roy 看 UI 與流程；Febo 週五晚額度回來後 review |
| Email 與 Google 日曆 | Email 寄送、寫入個人日曆延後；保留 port 與 outbox；站內通知全做；.ics 訂閱保留 |
| 架構 | Clean Architecture 四層（domain／application／infrastructure／app）放 `web/`，與 `prototype/` 並列；邊界靠 lint 擋 |
| CI/CD | GitHub Actions；PR 跑 typecheck、lint、邊界、單元、整合、build、煙霧 E2E；main 合併自動 build image 推 GHCR、SSH 部署 VM、migration、health check、失敗回滾 |
| 環境 | 一台 VM 現為 staging；正式開放前清庫重 seed；同機第二套留可行不做。網域暫用 `fju.roy422.dev`（DNS 灰雲，Caddy 取證）。Google OAuth client 用 Roy 個人帳號建 |
| 備份 | 只備份資料庫到 Cloudflare R2（每日、加密、保留 30 天）；附件不異地，Roy 接受風險；VM 內留 7 天本機快照 |
| 監測 | `/api/health` 加 GitHub Actions cron，失敗寄 Roy；磁碟 80% 告警 |
| 反人機 | Turnstile 只在密碼註冊、忘記密碼、連續登入失敗後；伺服器端限速 |
| 富文字 | Tiptap，存受限 HTML，伺服器清理 |
| 模擬日期 | staging 管理員一個「模擬業務日期」欄位，正式環境沒有；只影響業務判斷，不影響 session、OAuth、TLS、備份 |
| 原型 | 不再補原型；原型是 2026-09-11 前的固定參考，之後要改操作方式仍依 §22 Q9 先改原型再回寫 |
| Codex 角色 | spec review 與完整專題流程 E2E；明列案例保證覆蓋，探索式操作補漏；UI 由 Roy 檢視 |
| 文件結構 | Vault 分 🎯 專案目標與 🛠️ 工程開發兩區；產品拆十個模組；共通規則一份；驗收案例住模組檔；結果住工程區；repo 鏡像兩區；ticket 在 GitHub Issues |

## 待確認（Roy 或校方）

- 個人收件重送、完成率分母是否含晚加入者、對象變更後被移出者的回答顯示（放繳交模組討論）。
- 提案過期天數、解散組別對回答／評分／簽核的影響（放分組模組討論）。
- 密碼限速的可測預設（工程區提出）。
- 正式放行是否要求 Email 真寄達（Codex 建議必須；週一僅驗站內與 outbox）。
- 站內通知事件是否加審核結果、評分指派、簽核完成三項。
- 同一 Email 合併帳號的前提：密碼帳號 Email 需先驗證。
- 學校網域、CSV 樣本、一般組指派流程、產學基數、線上同意採認（沿用 §18、§19.6）。

## 查到但尚未寫回規格的事實

- VM 磁碟為 97G（規格 §17 寫 200G）。
- §6.1 殘留訪客可看句與 INDUSTRY 驗收文字與 DECIDED 條文矛盾；§8.1 五人描述與 Q8 矛盾；§4.6 整組與 Q7 矛盾；§15.3 9/14 與 Q3 矛盾；後台清單 §1／§2 仍有獨立內容編輯器；docs/README 交付目標仍寫 9/10。
- 回滾前提：migration 只做向後相容的「加」，刪改留下一版（工程規則，待寫入）。
