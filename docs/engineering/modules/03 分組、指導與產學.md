---
type: module-engineering-spec
module: 03
project: FJU IM Project
updated: 2026-09-12
status: draft-v2.1-pending-review
---
# 模組實作設計 03｜分組、指導與產學（v2.1）

## 1. 使用者要完成的工作與案例
- 產品原文：[03 分組、指導與產學](<../../product/🧩 功能模組/03 分組、指導與產學.md>) §5.1–5.5、6.1–6.3、2026-09-12 定案；故事 US-07、08、15、20、21、31、32、33。案例：GRP-01、03–20（GRP-02 空號）。
- 工作：學生找組員、提案並逐人確認成組；系辦建例外組、換成員、換組長、解散；老師認領產學組；系辦逐組或批次指派主指導、重派；老師建合作案；組長連結或換案；案主解除。
- 校方待辦：GRP-05 人數範圍（過渡：需個案實際核可依據；測試環境用標示的模擬核可資料）。

## 2. 名詞與不變量
- 開放提案期間每位被邀請者只能被一個提案占用（`proposal_occupancy` 唯一）；成立或終止在同交易釋放。
- 成立以單一交易建立 `groups`＋memberships＋leader；一人同屆一個有效 membership；一組一位有效組長（必為有效成員）、一位有效主指導、最多一個有效合作案連結。
- 成員集合或主指導改變→07 supersede；主指導重派不自動動評分指派。
- 解散：`groups.status=dissolved`；05 凍結、06 停止、07 作廢；閱覽依當時有效授權轉唯讀（產品 §5.5）。

## 3. 狀態表
| 對象 | 狀態轉移 | 觸發者 | 前置 | 拒絕 |
|---|---|---|---|---|
| 提案 | →open | 提案人 | 五位同屆已核准未停用、無有效組、無占用；到期＝min(預設天數, 成組截止) | `ALREADY_MEMBER`、`INVITED_ELSEWHERE`、`GROUP_PERIOD_CLOSED` |
| 提案 | open→established | 最後一位確認者 | 全員 confirmed | `PROPOSAL_NOT_OPEN` |
| 提案 | open→terminated（declined／member_withdrew／proposer_withdrew／expired／admin_voided／conflict） | 被邀請者／已確認者／提案人／due_work／管理員（理由） | — | 成立後撤回→`PROPOSAL_NOT_OPEN` |
| 邀請 | pending→confirmed／declined／withdrawn／released | 本人／終止 | — | — |
| 組別 | →active | 成立或管理員例外（核可依據、組長、零人拒） | — | `VALIDATION_FAILED` |
| 組別 | 成員異動 | 管理員（理由、影響預覽） | `groups` `FOR UPDATE`；移出組長須指定接任；移出最後一人須解散 | — |
| 組別 | active→dissolved | 管理員（理由、預覽） | 同交易呼叫 05／06／07 | — |
| 類型 | 組長改 | 成組期內、無主指導、無合作案 | 否則管理員改並處理關聯 | `FORBIDDEN` |
| 主指導 | 認領 | 老師；產學組且無有效指派 | 部分唯一 | `ALREADY_CLAIMED` |
| 主指導 | 指派／重派／解除 | 管理員（理由）；重派對話框列評分指派預設不勾 | 勾選者呼叫 06；07 supersede | — |
| 批次指派 | CSV | 先選屆別；六類預覽；每列帶 `groups.revision` | revision 不符→該列 `CONFLICT` | — |
| 合作案 | draft→published→withdrawn→published | 案主／管理員 | 下架不收新連結 | — |
| 連結 | 建立／換案／解除 | 組長或管理員；案主與管理員解除（理由） | 已成立產學組、案已發布 | `OPPORTUNITY_NOT_LINKABLE` |

## 4. 資料
附錄 A。

## 5. Port
| Port | 方法（主要） | 授權 | 冪等 |
|---|---|---|---|
| `GroupCommand` | `propose`、`confirmInvitation`、`declineInvitation`、`withdrawConfirmation`、`withdrawProposal`、`voidProposal(admin, reason)`、`createExceptionGroup`、`changeMembers(groupId, revision, add[], remove[], reason, successorLeaderId?)`、`changeLeader`、`changeType`、`dissolve(groupId, revision, reason)`、`assignAdvisor`、`reassignAdvisor(groupId, revision, teacherId, reason, gradingSelections[])`、`unassignAdvisor`、`claimGroup(teacher)`、`batchAssign(cohortId, rows[])` | 依列 | requestId |
| `OpportunityCommand` | `create`、`update`、`publish`、`withdraw`、`republish`、`link`、`relink`、`unlink(reason)` | 案主／組長／admin | requestId |
| `GroupMembershipQuery` | `currentMembers(groupId, at)`、`membershipOf(userId, cohortId)`、`snapshotAt(groupId, at)` | policy | — |
| `AdvisorQuery` | `advisorOf(groupId)`、`groupsOf(teacherId)`、`unassignedIndustryGroups(cohortId)` | — | — |
| `GroupLifecycleQuery` | `dissolutionSnapshot(groupId)` | — | — |

依賴：01、02（成組期、鐘）、05 `freezeForDissolution`、06 `applyAdvisorReassignment／stopForDissolution／listAssignmentsForTeacher`、07 `supersedeForParticipantChange／voidForDissolution`、08、10。

## 6. 交易與並行
- 成立：`group_proposals` `FOR UPDATE`→全員 confirmed→插入 group／memberships／leader→刪 `proposal_occupancy`；membership 唯一違反→整體回滾並終止（conflict）。
- 終止：`FOR UPDATE` 提案，邀請 released，占用刪除，`due_work(proposal_expiry)` 取消。
- 成員異動／解散／重派：`groups` `FOR UPDATE`→呼叫 07／06／05（同 tx）→事件（recipients 聯集＋主指導）。
- 認領：`advisor_assignments` 部分唯一→`ALREADY_CLAIMED`。
- 批次：每列一交易；預覽 revision 比對。

## 7. 頁面、UI 狀態與原型差異
| 路由 | 成功 | 錯誤 | 衝突 | 結果未知 | 原型差異 |
|---|---|---|---|---|---|
| `/dashboard/student/group` | 提案送出、確認回執、成立通知 | `INVITED_ELSEWHERE` 等 | — | 查回執 | 提案、逐人確認、拒絕、撤回、到期時間、終止紀錄、組長標示 |
| `/dashboard/teacher/groups` | 認領成功 | `ALREADY_CLAIMED` 文案 | — | 查回執 | 重派不由老師 |
| `/dashboard/admin/groups` | 例外組、指派、重派、成員異動、解散回執 | 核可依據空、零人 | 預覽 token 過期→重做 | 查回執 | 例外組表單、影響預覽、重派對話框列評分指派、批次 CSV、組長更換 |
| `/industry`、`/dashboard/teacher/industry` | 建立、發布、連結回執 | `OPPORTUNITY_NOT_LINKABLE` | — | — | 移除「網際網路公開」；下架啟用；連結／換案／解除 |

## 8. 序列
正常：S01 提 G1→S02–S05 各確認→最後一人成立→占用釋放→通知。拒絕：S03 拒→終止→釋放→S01 重發→全員重確認。到期：due_work→終止。成員異動：移出 S13 加入 SX3→預覽→執行→07 新版→通知聯集；S13 之後能再被邀請（占用已釋放）。解散：05 凍結、06 停止、07 作廢→通知。重派：對話框列 T2 期中評分指派預設不勾→執行→07 新版、06 不動；預覽期間 T2 送出正式分→`CONFLICT`。斷網：確認回應遺失→查回執。

## 9. 保存、歷史、匯出
提案與終止、membership 區間、組長歷史、指派歷史、連結歷史可查；匯出只管理員；解散後閱覽依 GRP-20。

## 10. 自動測試
單元：狀態機五種終止、到期 min、類型三條件、例外組驗證、CSV 六類。整合：成立單一交易與唯一；兩提案成立競態；占用釋放後再提案（成組→移出→再提案）；兩老師認領；成員異動同交易觸發 07；解散同交易 05／06／07；批次 revision；到期 handler。對應 GRP-03、04、06、07、09、10、11、12、13、14、15、17、18、19、20。

## 11. Codex 操作
劇本 P04（成組、指導、合作案）、B02（終止、撤回、例外人數、成員異動、解散、批次、換案、解散後閱覽）。證據：確認回執、`group_memberships`、`proposal_occupancy` 為空、認領衝突回應、重派對話框截圖、批次預覽、六角色存取回應。

## 12. 依賴與放行
切片 S03、S06、S09。必決：無（Q-GRP05 過渡已定）。範圍外：跨組成果抵認。

## 附錄 A 資料字典

### `group_proposals`
| 欄位 | 型別 | NULL | default | 約束 | 理由 |
|---|---|---|---|---|---|
| `id` | uuid | NO | — | PK | — |
| `cohort_id` | uuid | NO | — | FK cohorts | — |
| `proposer_user_id` | uuid | NO | — | FK users | 歷史事實 |
| `group_type` | text | NO | — | CHECK IN ('general','industry') | — |
| `expires_business_at` | timestamptz | NO | — | — | min(預設天數, 成組截止) |
| `state` | text | NO | `'open'` | CHECK IN ('open','established','terminated') | — |
| `termination_kind` | text | YES | NULL | CHECK IN ('declined','member_withdrew','proposer_withdrew','expired','admin_voided','conflict')；terminated 時 NOT NULL | — |
| `reason` | text | YES | NULL | admin_voided 時 NOT NULL | — |
| `established_group_id` | uuid | YES | NULL | FK groups | — |
| `deadline_version` | integer | NO | 1 | — | due_work 版本 |
| `revision`、通用欄 | — | — | — | — | — |

### `proposal_invitations`（狀態列；成立或終止後不再變，`fju_app` 只能 UPDATE `state`、`decided_real_at`）
| 欄位 | 型別 | NULL | 約束 | 理由 |
|---|---|---|---|---|
| `id` | uuid | NO | PK | — |
| `proposal_id` | uuid | NO | FK；UNIQUE `(proposal_id, user_id)` | — |
| `user_id` | uuid | NO | FK users | — |
| `state` | text | NO | CHECK IN ('pending','confirmed','declined','withdrawn','released') | 同意歷史 |
| `decided_real_at` | timestamptz | YES | — | — |

### `proposal_occupancy`（占用表）
| 欄位 | 型別 | NULL | 約束 | 理由 |
|---|---|---|---|---|
| `user_id` | uuid | NO | PK；FK users | 一人只在一個開放提案 |
| `proposal_id` | uuid | NO | FK group_proposals | 成立或終止時 DELETE |
| `created_at` | timestamptz | NO | — | — |

### `groups`
| 欄位 | 型別 | NULL | default | 約束 | 理由 |
|---|---|---|---|---|---|
| `id` | uuid | NO | — | PK | — |
| `cohort_id` | uuid | NO | — | FK cohorts | — |
| `code` | text | NO | — | UNIQUE `(cohort_id, code)` | 例如 G01 |
| `group_type` | text | NO | — | CHECK IN ('general','industry') | — |
| `status` | text | NO | `'active'` | CHECK IN ('active','dissolved') | — |
| `established_business_at` | timestamptz | NO | — | — | — |
| `dissolved_real_at`、`dissolve_reason` | timestamptz／text | YES | NULL | dissolved 時 NOT NULL | — |
| `exception_basis` | jsonb | YES | NULL | 例外組 NOT NULL：approver、approvedAt、applicableCount、source、simulated(bool) | 核可依據 |
| `revision`、通用欄 | — | — | — | — | — |

### `group_memberships`（有效區間列）
| 欄位 | 型別 | NULL | 約束 | 理由 |
|---|---|---|---|---|
| `id` | uuid | NO | PK | — |
| `group_id` | uuid | NO | FK groups | — |
| `cohort_id` | uuid | NO | FK cohorts（冗餘，供唯一） | — |
| `user_id` | uuid | NO | FK users；部分唯一 `(user_id, cohort_id) WHERE valid_to IS NULL` | 一人同屆一有效 |
| `valid_from`、`valid_to` | timestamptz | NO／YES | — | 只允許設 valid_to |
| `added_by_kind` | text | NO | CHECK IN ('user','system','worker') | 提案成立＝user（提案人）；管理員加入＝user |
| `added_by_user_id` | uuid | YES | FK users RESTRICT | — |
| `removal_reason` | text | YES | — | 移出時 NOT NULL（用例檢查；欄級 UPDATE） |

### `group_leaders`
| 欄位 | 型別 | NULL | 約束 | 理由 |
|---|---|---|---|---|
| `id` | uuid | NO | PK | — |
| `group_id` | uuid | NO | FK；部分唯一 `(group_id) WHERE valid_to IS NULL` | 一組一有效組長 |
| `user_id` | uuid | NO | FK users | 必須是有效成員（用例檢查） |
| `valid_from` | timestamptz | NO | — | — |
| `valid_to` | timestamptz | YES | — | 結束欄（欄級 UPDATE） |
| `changed_by_user_id` | uuid | NO | FK users RESTRICT | 成立時＝提案人；更換時＝管理員 |
| `reason` | text | YES | — | 更換理由 |

### `advisor_assignments`
| 欄位 | 型別 | NULL | 約束 | 理由 |
|---|---|---|---|---|
| `id` | uuid | NO | PK | — |
| `group_id` | uuid | NO | FK；部分唯一 `(group_id) WHERE valid_to IS NULL` | 一組一主指導 |
| `teacher_user_id` | uuid | NO | FK users | — |
| `source` | text | NO | CHECK IN ('claim','admin','csv') | — |
| `valid_from`、`valid_to` | timestamptz | NO／YES | — | — |
| `reason`、`assigned_by_user_id` | text／uuid | YES | — | — |

### `industry_opportunities`
| 欄位 | 型別 | NULL | 約束 | 理由 |
|---|---|---|---|---|
| `id` | uuid | NO | PK | — |
| `owner_teacher_user_id` | uuid | NO | FK users | 案主 |
| `company_name`、`department`、`content`、`requirements` | text | NO | — | 登入者可見 |
| `notes`、`notes_visibility` | text | YES／NO | CHECK IN ('signed_in','internal') | — |
| `address`、`contact_name`、`contact_phone`、`contact_email` | text | YES | — | 只案主與管理員 |
| `status` | text | NO | CHECK IN ('draft','published','withdrawn') | — |
| `published_business_at`、`withdrawn_business_at` | timestamptz | YES | — | — |
| `revision`、通用欄 | — | — | — | — |

### `opportunity_links`
| 欄位 | 型別 | NULL | 約束 | 理由 |
|---|---|---|---|---|
| `id` | uuid | NO | PK | — |
| `group_id` | uuid | NO | FK；部分唯一 `(group_id) WHERE valid_to IS NULL` | 一組最多一案 |
| `opportunity_id` | uuid | NO | FK industry_opportunities | 一案可多組 |
| `valid_from`、`valid_to` | timestamptz | NO／YES | — | — |
| `linked_by_user_id` | uuid | NO | FK users RESTRICT | 組長 |
| `ended_by_user_id` | uuid | YES | FK users RESTRICT | 解除者（欄級 UPDATE） |
| `reason` | text | YES | — | 解除或換案理由 |
