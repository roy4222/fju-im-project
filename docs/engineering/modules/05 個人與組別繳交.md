---
type: module-engineering-spec
module: 05
project: FJU IM Project
updated: 2026-09-12
status: draft-v2.1-pending-review
---
# 模組實作設計 05｜個人與組別繳交（v2.1）

## 1. 使用者要完成的工作與案例
- 產品原文：[05 個人與組別繳交](<../../product/🧩 功能模組/05 個人與組別繳交.md>) §4.6 與「個人填報與收件名單」；故事 US-09–15、22、25、35、36。案例：SUB-01–25。
- 工作：學生填個人收件與重送；組員共用草稿、上傳、正式送出、看歷史；被移出者唯讀；系辦管理名單、免填、移出、加回、指定重開、看完成率與截止快照；老師看有權的版本。

## 2. 名詞與不變量
- 收件者＝人或組；名單成員列有效區間；分母＝目前有效且非免填成員數；分子＝其中有正式版本者。
- 截止前自動加入、截止後不自動追加、封存後不重算；正式版本不可變；版本號在草稿列鎖內分配；準時看 `receivedBusinessAt`。
- 指定重開只改該收件者期限，不繞過資格、停用、封存、解散。
- 個人回答預設本人與管理員；主指導閱覽需設定 enabled 且版本≥生效版本且目前有效指導關係。
- 截止快照 insert-only；晚 commit 的準時版本由 `snapshot_reconcile` 到期工作追加註記（保持 pending 直到快照存在）。
- **送出守門固定順序（v2.1，Codex 小項）**：授權（`NOT_MEMBER`／`NOT_IN_ROSTER`／`EXEMPTED`／`FORBIDDEN`）→ 屆別與組別（`COHORT_ARCHIVED`、`GROUP_DISSOLVED`）→ 時間（`ITEM_NOT_OPEN`、`DEADLINE_PASSED`，含指定重開的個別期限）→ schema（`DRAFT_NEEDS_REVIEW`）→ 欄位驗證（`VALIDATION_FAILED`）→ 檔案（`FILE_*`）→ 版本競爭（`CONFLICT`）；先命中者回傳，不合併。截止後的舊頁送出因此回 `DEADLINE_PASSED` 而不是 `DRAFT_NEEDS_REVIEW`（P06 步驟 16–17 與 P07 步驟 1 依此）。

## 3. 狀態表
| 對象 | 狀態 | 觸發者 | 前置 | 拒絕 |
|---|---|---|---|---|
| 名單成員 | eligible | 發布（自動）、截止前核准（01 同交易呼叫 `syncRoster`）、管理員加入 | 對象符合 | 截止後自動加入不發生 |
| 名單成員 | exempt／unexempt | 管理員（理由） | — | 恢復時已截止不顯示可填 |
| 名單成員 | removed | 管理員／受眾變更／停用／跨屆 | — | — |
| 名單成員 | 加回 | 管理員 | 同人同屆同項目；舊回答 schema 仍有效→計完成，否則需補交 | — |
| 草稿 | 儲存 | 有效收件者 | revision；項目開放；屆別 active；組別 active | `CONFLICT`、`ITEM_NOT_OPEN`、`COHORT_ARCHIVED`、`GROUP_DISSOLVED` |
| 草稿 | needs_review | 系統（schema 新版） | — | 送出→`DRAFT_NEEDS_REVIEW` |
| 正式版本 | 建立 | 有效收件者 | 母 spec §4.17 全部守門 | 見錯誤碼 |
| 指定重開 | 建立 | 管理員（理由、新期限） | 收件者仍有資格、未停用、未解散、屆別 active | `NOT_IN_ROSTER`、`GROUP_DISSOLVED` |
| 快照 | 產生 | due_work（截止＋60 秒） | — | — |
| 快照註記 | 追加 | due_work `snapshot_reconcile` | 快照存在 | 不存在→pending 重試 |

## 4. 資料
附錄 A。完成率＝`count(distinct receiver having version)/count(eligible & not exempt)`。

## 5. Port
| Port | 方法 | 授權 | 冪等 |
|---|---|---|---|
| `SubmissionCommand` | `buildRoster(tx, item)`、`syncRoster(tx, item, cause)`、`addToRoster(itemId, receiver, reason, reopenUntil?)`、`removeFromRoster`、`exempt`、`unexempt`、`restore`、`saveDraft(itemId, receiver, revision, answers, fileIds, requestId)`、`submit(itemId, receiver, draftRevision, requestId)`、`reopen(itemId, receiver, newDueAt, reason)`、`setAdvisorVisibility`、`applyAudienceChange(tx, item, diff)`、`freezeForDissolution(tx, groupId)` | 收件者／組員／admin | requestId |
| `SubmissionQuery` | `draft`、`versions`、`latest`、`roster(itemId)`、`completion(itemId)`、`snapshot(itemId, deadlineVersion)`、`mySubmissions(userId)`、`hasAnyResponse` | policy（含主指導閱覽） | — |
| `UnfinishedWorkQuery` | `list(tx, cohortId)` | — | — |

依賴：01、02、03（membership、advisor）、04（ItemQuery、ReceiverResolver）、08（事件、due_work）、10（FileStorage、audit、ledger）。

## 6. 交易、並行、時間
- 送出：鎖序 cohorts(FOR SHARE)→groups(FOR SHARE)→drafts(FOR UPDATE)；版本號在鎖內；帳本 ON CONFLICT；`due_work(snapshot_reconcile)` 同交易。
- 儲存草稿：`drafts` `FOR UPDATE`＋revision；答案以目前 schema 驗證（必填只在送出擋）。
- 名單同步：核准用例同交易呼叫；停用／跨屆同交易 `removeFromRoster`。
- 快照：worker 在截止＋60 秒讀名單與版本 insert-only。
- 附件移除再附回：`file_references` 是部分唯一（`released_at IS NULL`），attach 對同一檔案插入新的有效引用列，舊列保留（契約 01 §11）；GC 只看有效列。

## 7. 頁面、UI 狀態與原型差異
| 路由 | 成功 | 錯誤 | 衝突 | 結果未知 | 原型差異 |
|---|---|---|---|---|---|
| 作業區、內容頁、繳交歷史 | 儲存狀態帶伺服器時間；收件章回執含版本、接收時間、送出者 | `DEADLINE_PASSED` 含截止文案；`FILE_*` | `CONFLICT` 顯示差異保留輸入 | 「結果尚未確認」＋查回執 | 個人填報畫面；結果未確認狀態；needs_review 差異；主指導可見告知 |
| `/dashboard/student/my-submissions`（新） | 唯讀版本與附件 | — | — | — | 新做 |
| 老師矩陣 | 只顯示有權組別 | — | — | — | 依授權矩陣 |
| 管理員項目詳情、名單頁、重開對話框 | 名單分類、快照、重開回執 | — | — | 查回執 | 名單頁、免填、加回、快照、個別重開、通知範圍 |

## 8. 序列
正常（組別）：母 spec §4.17。個人：S01 填 F1→儲存→送出 v1→重送 v2→A1 名單頁 12／13。免填：S09 免填→分母 12→恢復→13。移出：移出已交者→7／9；加回遇新 schema→需補交。截止後晚加入：SX2 核准不進名單；A1 加入＋重開一次操作→一則通知。斷網：送出回應遺失→查回執。舊頁：截止後送出→`DEADLINE_PASSED`。解散：凍結。

## 9. 保存、歷史、下載、他角色所見
版本歷史含送出者、雙時間、schema、附件 checksum；下載逐次授權；老師依矩陣；學生看不到他人。

## 10. 自動測試
單元：名單推導與分母、截止分鐘、快照 payload、schema 遷移分類。整合：兩人同版、同 requestId、封存×送出、移出×送出、檔案不屬本組、快照寬限與 `snapshot_reconcile`（快照已讀未 commit／送出已 commit 未後處理／worker 中斷重啟）、指定重開只影響該收件者、解散凍結。對應 SUB-01–25、COH-07／08。

## 11. Codex 操作
劇本 P03、P05、P06、P08，分支 B03。證據：回執 JSON、`submission_versions` 與 hash、名單頁、`roster_snapshots`、G2 直接請求回應。

## 12. 依賴與放行
切片 S04（名單表交付）、S05、S07、S08。必決：無。範圍外：老師填表。

## 附錄 A 資料字典

### `response_rosters`
| 欄位 | 型別 | NULL | default | 約束 | 理由 |
|---|---|---|---|---|---|
| `id` | uuid | NO | — | PK | — |
| `item_id` | uuid | NO | — | FK managed_items | — |
| `cohort_id` | uuid | NO | — | FK cohorts | — |
| `receiver_kind` | text | NO | — | CHECK IN ('user','group') | — |
| `receiver_id` | uuid | NO | — | 部分唯一 `(item_id, receiver_kind, receiver_id) WHERE eligible_to_business_at IS NULL` | 目前一列 |
| `eligible_from_business_at`、`eligible_to_business_at` | timestamptz | NO／YES | — | — | 區間 |
| `source` | text | NO | — | CHECK IN ('auto','admin') | — |
| `exempt` | boolean | NO | false | — | 免填 |
| `exempt_reason`、`removed_reason` | text | YES | NULL | exempt 時 reason NOT NULL | — |
| `revision`、通用欄 | — | — | — | — | — |

### `roster_snapshots`（不可變）
| 欄位 | 型別 | NULL | 約束 | 理由 |
|---|---|---|---|---|
| `id` | uuid | NO | PK | — |
| `item_id` | uuid | NO | FK | — |
| `deadline_version` | integer | NO | UNIQUE `(item_id, deadline_version, scope, receiver_id)` | — |
| `scope` | text | NO | CHECK IN ('global','targeted') | — |
| `receiver_id` | uuid | YES | targeted 時 NOT NULL；唯一鍵用 COALESCE(receiver_id, uuid_nil) | NULL 語義 |
| `taken_business_at` | timestamptz | NO | — | — |
| `payload` | jsonb | NO | members[]、completed、denominator | — |

### `snapshot_annotations`（不可變）
| 欄位 | 型別 | NULL | 約束 | 理由 |
|---|---|---|---|---|
| `id` | uuid | NO | PK | — |
| `snapshot_id` | uuid | NO | FK；UNIQUE `(snapshot_id, submission_version_id)` | 冪等 |
| `submission_version_id` | uuid | NO | FK | 晚 commit 的準時版本 |
| `kind` | text | NO | CHECK IN ('late_commit','correction') | — |
| `created_at` | timestamptz | NO | — | — |

### `drafts`
| 欄位 | 型別 | NULL | default | 約束 | 理由 |
|---|---|---|---|---|---|
| `id` | uuid | NO | — | PK | — |
| `item_id` | uuid | NO | — | FK managed_items；UNIQUE `(item_id, receiver_kind, receiver_id)` | 一份草稿 |
| `receiver_kind` | text | NO | — | CHECK IN ('user','group') | — |
| `receiver_id` | uuid | NO | — | user→users、group→groups（用例檢查，不設 FK） | — |
| `schema_version_id` | uuid | NO | — | FK | — |
| `answers` | jsonb | NO | `'{}'` | — | — |
| `legacy_answers` | jsonb | YES | NULL | — | 消失欄位保留 |
| `file_ids` | uuid[] | NO | `'{}'` | — | 附件引用（attach 時驗證） |
| `migration_state` | text | NO | `'none'` | CHECK IN ('none','auto','needs_review') | — |
| `revision`、`updated_*` | — | — | — | 通用（契約 01 §1） | — |

### `submission_versions`（不可變）
| 欄位 | 型別 | NULL | 約束 | 理由 |
|---|---|---|---|---|
| `id` | uuid | NO | PK | — |
| `item_id` | uuid | NO | FK managed_items；UNIQUE `(item_id, receiver_kind, receiver_id, version_no)` | — |
| `receiver_kind` | text | NO | CHECK IN ('user','group') | — |
| `receiver_id` | uuid | NO | 用例檢查對應表 | — |
| `version_no` | integer | NO | — | 鎖內分配 |
| `schema_version_id` | uuid | NO | FK | — |
| `answers` | jsonb | NO | — | 快照 |
| `submitted_by_user_id` | uuid | NO | FK users | — |
| `received_real_at`、`received_business_at` | timestamptz | NO | — | 準時判定 |
| `request_id` | uuid | NO | — | 追溯帳本 |
| `membership_snapshot` | jsonb | YES | 組別時 NOT NULL | 當時成員 ID 集合 |
| `advisor_snapshot` | jsonb | YES | — | 當時主指導 |
| `deadline_version_at_submit` | integer | NO | — | 對帳用 |

### `submission_files`（不可變）
| 欄位 | 型別 | NULL | 約束 |
|---|---|---|---|
| `submission_version_id` | uuid | NO | FK；PK `(submission_version_id, file_id)` |
| `file_id` | uuid | NO | FK stored_files |
| `field_key` | text | NO | — |
| `checksum` | text | NO | 當時 checksum |

### `targeted_reopens`（不可變）
| 欄位 | 型別 | NULL | 約束 | 理由 |
|---|---|---|---|---|
| `id` | uuid | NO | PK | — |
| `item_id` | uuid | NO | FK managed_items | — |
| `receiver_kind` | text | NO | CHECK IN ('user','group') | — |
| `receiver_id` | uuid | NO | 用例檢查對應表 | — |
| `new_due_at` | timestamptz | NO | — | — |
| `deadline_version` | integer | NO | — | 每次重開新版本 |
| `reason` | text | NO | — | — |
| `created_by_user_id` | uuid | NO | FK users RESTRICT | — |
| `created_business_at` | timestamptz | NO | — | — |

### `advisor_visibility_settings`（不可變）
| 欄位 | 型別 | NULL | 約束 | 理由 |
|---|---|---|---|---|
| `id` | uuid | NO | PK | — |
| `item_id` | uuid | NO | FK | — |
| `enabled` | boolean | NO | — | — |
| `effective_from_version_no` | integer | NO | — | 舊回答不擴權 |
| `set_by_user_id` | uuid | NO | FK users RESTRICT | — |
| `set_at` | timestamptz | NO | — | — |
