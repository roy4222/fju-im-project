---
type: module-engineering-spec
module: 08
project: FJU IM Project
updated: 2026-09-12
status: draft-v2.3-pending-review
---
# 模組實作設計 08｜站內通知與日曆（v2.3）

## 1. 使用者要完成的工作與案例
- 產品原文：[08 站內通知與日曆](<../../product/🧩 功能模組/08 站內通知與日曆.md>) 事件矩陣、去重補建、舊通知與失權、通知匣；故事 US-06、17、43。案例：NTF-01–08、13–20；NTF-09、10、11、12 延後（DEFERRED，不在任何切片）。
- 工作：使用者在通知匣看到與自己有關的事件並標已讀；系辦看到彙整與維運告警；三角色看站內日曆。

## 2. 名詞與不變量
- 事件 insert-only，`recipients` 寫入時固定；投影狀態分表；通知 `(event_id, recipient)` 唯一。
- 補建不重展開收件人；重試不重複；無實際變更不產生事件。
- 通知列只存 ID 與標題；渲染時逐筆重驗來源；失權「此項目目前無法存取」，撤回「來源已撤回」，封存唯讀。
- 通知匣預設顯示本人所有屆別與全站通知，可篩屆別；封存後仍可標已讀。
- 到期工作表由本模組擁有，handler 呼叫各模組 command；`snapshot_reconcile` 快照不存在時保持 pending 重試。

## 3. 狀態表
| 對象 | 狀態 | 觸發者 |
|---|---|---|
| 事件 | created | 各模組用例（同交易 `EventPublisher.publish`） |
| 投影 | pending→done／failed→pending（重跑） | worker／管理員 |
| 通知 | unread→read | 本人 |
| due_work | pending→done／cancelled／failed；pending 退避重試 | worker／期限變更用例／管理員重跑 |

## 4. 資料
共用表在契約 01 §4.5–4.7；本模組附錄 A：`notifications`、`digest_events`。事件型別與 recipients 規則見產品模組 08 §4 矩陣（本文不重抄，實作以該矩陣為準並在 `application/notifications/event-catalog.ts` 對照）。

## 5. Port
| Port | 方法 |
|---|---|
| `EventPublisher` | `publish(tx, {type, scope, cohortId, source, version, actor, recipients, recipientBasis, payload})` |
| `DueWorkScheduler` | `schedule(tx, kind, subject, deadlineVersion, dueBusinessAt)`、`cancel(tx, kind, subject, oldVersion)` |
| `InboxQuery` | `list(userId, {cohortId?, cursor})`（渲染重驗）、`unreadCount(userId)` |
| `InboxCommand` | `markRead(ids)`、`markAllRead` |
| `CalendarQuery` | `range(actor, cohortId, from, to)` |
| `OpsCommand`（08 部分） | `retryProjection(eventId, consumer)`、`retryDueWork(id)` |

## 6. worker 設計
Compose 服務 `worker`（同映像）；`pg_try_advisory_lock` 單實例；投影迴圈 5 秒：`FOR UPDATE SKIP LOCKED LIMIT 50`，每筆一交易，寫通知（唯一鍵忽略重複）→標 done；失敗 attempts+1、退避 2^n 秒；≥5 標 failed＋管理員告警事件。到期迴圈 30 秒：`BusinessClock.now()`→認領 `due_business_at <= now AND state='pending' AND (next_attempt_at IS NULL OR next_attempt_at <= real_now)`→handler：`proposal_expiry`→03；`deadline_snapshot`→05；`snapshot_reconcile`→05（快照不存在→pending、退避）；`overdue_digest`／`stage_end_unassigned`→本模組 digest；`file_gc`→10；`receipt_purge`→10；`test_noop`→無操作（契約 01 §4.7 的合法 kind，只在 `BUSINESS_CLOCK_OVERRIDE_ENABLED=true` 環境註冊，供 S02 驗收）。投影驗收用測試事件 `test.notification`（收件人由發送者指定，只在同一環境可由管理端「發送測試通知」產生；v2.2，Codex B3）；真實 `account.approved` 依產品規則不投影通知，S02 驗證核准後通知匣沒有新項。heartbeat 每 tick 寫 `worker_heartbeat`；`/api/health` 曝露版本與 `lastTickAt`。

## 7. 頁面、UI 狀態與原型差異
| 路由 | 成功 | 失權顯示 | 原型差異 |
|---|---|---|---|
| 鈴鐺與通知匣 | 列表、已讀、全部已讀 | 「此項目目前無法存取」「來源已撤回」「已封存（唯讀）」 | 存 DB、跨屆篩選、60 秒輪詢 |
| 首頁行事曆 | 活動與截止 | — | 移除訂閱 |
| 管理員維運頁 | pending／failed 投影、重跑、worker 狀態 | — | 新做 |

### 7.1 原型畫面對照（原型 2026-09-11（commit `8e1cff8`）；圖檔沿用產品模組 08，不重拍）

> 判讀：每張圖標題格式「角色｜路由｜操作／狀態」，日期與 commit 同上；圖是 2026-09-12 產品決策前的原型畫面，只證明畫面存在，不是驗收證據（149 案維持 NOT_RUN）。每組先寫「對應」（本文節與案例 ID）與「正式版差異」（9/12 決策後正式版必須不同之處），逐圖再列狀態；沒有原型的流程與狀態列在 7.2，不用假圖補齊。產品原文與逐張索引：[08 站內通知與日曆](<../../product/🧩 功能模組/08 站內通知與日曆.md>)、[截圖核對與缺口](<../../product/🔎 截圖核對與缺口.md>)、[畫面總覽](<../../product/🗺️ 畫面總覽.md>)。

#### 通知匣（學生／老師／管理員）

對應：§7 鈴鐺與通知匣、§6 投影；案例 NTF-01、NTF-02、NTF-03、NTF-04、NTF-05、NTF-07、NTF-08、NTF-13、NTF-14。正式版差異：存 DB、跨屆篩選、失權遮罩文案、60 秒輪詢；點入重驗權限。

![學生｜/dashboard/student/inbox｜截止、簽核、繳交、系統四類通知](<../../product/assets/prototype-2026-09-11/student--dashboard-student-inbox.png>)

*學生｜`/dashboard/student/inbox`｜截止、簽核、繳交、系統四類通知｜原型 2026-09-11（commit `8e1cff8`）｜對應與差異見本組說明*

![學生｜按全部標為已讀後](<../../product/assets/prototype-2026-09-11/student--inbox-read-all.png>)

*學生｜`按全部標為已讀後`｜｜原型 2026-09-11（commit `8e1cff8`）｜對應與差異見本組說明*

![老師｜/dashboard/teacher/inbox｜評分、簽核、繳交、系統](<../../product/assets/prototype-2026-09-11/teacher--dashboard-teacher-inbox.png>)

*老師｜`/dashboard/teacher/inbox`｜評分、簽核、繳交、系統｜原型 2026-09-11（commit `8e1cff8`）｜對應與差異見本組說明*

![管理員｜/dashboard/admin/inbox｜帳號、截止、評分、備份](<../../product/assets/prototype-2026-09-11/admin--dashboard-admin-inbox.png>)

*管理員｜`/dashboard/admin/inbox`｜帳號、截止、評分、備份｜原型 2026-09-11（commit `8e1cff8`）｜對應與差異見本組說明*

#### 首頁行事曆與手機展開（學生）

對應：§7 行事曆；案例 NTF-13。正式版差異：移除「訂閱到 Google 日曆」；活動與截止讀 DB；手機三狀態沿用。

![學生｜/dashboard/student｜手機預設近期三筆](<../../product/assets/prototype-2026-09-11-supplement/student--calendar-expand-verified--collapsed.png>)

*學生｜`/dashboard/student`｜手機預設近期三筆｜原型 2026-09-11（commit `8e1cff8`）｜對應與差異見本組說明*

![學生｜/dashboard/student｜已展開完整月份](<../../product/assets/prototype-2026-09-11-supplement/student--calendar-expand-verified--expanded.png>)

*學生｜`/dashboard/student`｜已展開完整月份｜原型 2026-09-11（commit `8e1cff8`）｜對應與差異見本組說明*

![學生｜/dashboard/student｜切至下個月](<../../product/assets/prototype-2026-09-11-supplement/student--calendar-expand-verified--next-month.png>)

*學生｜`/dashboard/student`｜切至下個月｜原型 2026-09-11（commit `8e1cff8`）｜對應與差異見本組說明*

![學生｜首頁行事曆｜月曆與圖例；「訂閱到 Google 日曆」按鈕本輪延後](<../../product/assets/prototype-2026-09-11/student--home-calendar-subscribe.png>)

*學生｜`首頁行事曆`｜月曆與圖例；「訂閱到 Google 日曆」按鈕本輪延後｜原型 2026-09-11（commit `8e1cff8`）｜對應與差異見本組說明*

### 7.2 尚無原型畫面的流程與狀態（缺口；不用假圖補）

- 失權遮罩「此項目目前無法存取」「來源已撤回」「已封存（唯讀）」
- 管理員維運頁（投影 pending／failed、重跑、worker 狀態）
- 逾期彙整通知內容
- 備份失敗通知（模組 10）
- 通知點入重驗權限的拒絕畫面

共 8 張原型圖；新增或修改原型流程須另依 Roy 的原型流程確認後才補圖。

## 8. 序列
正常：S03 送出→事件 recipients S01、S02、S04、S05→worker 建四則→鈴鐺 +1。補建：寫通知後崩潰→重啟→pending 重跑→唯一鍵擋重複。失權：S01 被移出→舊通知遮罩→點入 `FORBIDDEN`。到期：worker 停機→跨截止→重啟→快照與彙整各一次；`snapshot_reconcile` 在快照未產生時保持 pending。

## 9. 保存與他角色所見
通知永久保留；已讀跨登入；只有本人看得到自己的通知。

## 10. 自動測試
單元：recipients 計算、去重鍵、遮罩選擇。整合：投影冪等重跑、毒事件、advisory lock 單實例、due_work 跳月與回撥、快照寬限與 reconcile 三種情況（快照已讀未 commit、送出已 commit 未後處理、worker 中斷）。對應 NTF-01–08、13–20。

## 11. Codex 操作
劇本 B08（通知）、各主線劇本的通知步驟；工程提供「停／啟 worker」「重跑投影」操作（手冊 §6）。證據：`domain_events`、`event_projections`、`notifications` 列、通知匣截圖。

## 12. 依賴與放行
切片 S02、S08、S12。必決：無。範圍外：Email、外部日曆。

## 附錄 A 資料字典

### `notifications`
| 欄位 | 型別 | NULL | default | 約束 | 理由 |
|---|---|---|---|---|---|
| `id` | uuid | NO | — | PK | — |
| `event_id` | uuid | NO | — | FK domain_events；UNIQUE `(event_id, recipient_user_id)` | 去重 |
| `recipient_user_id` | uuid | NO | — | FK users | — |
| `scope` | text | NO | — | CHECK IN ('cohort','global') | 跨屆篩選 |
| `cohort_id` | uuid | YES | NULL | FK cohorts RESTRICT | global 時 NULL |
| `kind` | text | NO | — | — | 類型標籤 |
| `title` | text | NO | — | 不含私有正文 | — |
| `source_ref` | jsonb | NO | `'{}'` | `{type, id, version}` | 渲染時重驗 |
| `created_at` | timestamptz | NO | now() | — | — |
| `read_at` | timestamptz | YES | NULL | — | 個人已讀 |
索引：`(recipient_user_id, created_at DESC)`、`(recipient_user_id, read_at)`。

### `digest_events`（不可變）
| 欄位 | 型別 | NULL | 約束 | 理由 |
|---|---|---|---|---|
| `id` | uuid | NO | PK | — |
| `kind` | text | NO | CHECK IN ('overdue','unassigned_groups','projection_failed','backup_failed')；UNIQUE `(kind, subject_id, deadline_version)` | 去重 |
| `cohort_id` | uuid | YES | FK | — |
| `subject_id` | uuid | YES | — | 項目或屆別 |
| `deadline_version` | integer | NO | — | — |
| `count` | integer | NO | — | 產生時數量 |
| `payload` | jsonb | NO | — | 名單 ID |
| `event_id` | uuid | NO | FK domain_events | 對應事件 |
| `generated_business_at` | timestamptz | NO | — | — |

### `worker_heartbeat`（可變單列）
| 欄位 | 型別 | NULL | 約束 | 理由 |
|---|---|---|---|---|
| `id` | smallint | NO | PK；CHECK `(id = 1)` | 固定一列 |
| `version` | text | NO | — | worker 映像版本 |
| `last_tick_real_at` | timestamptz | NO | — | `/api/health` 的 `worker.lastTickAt` |
| `last_projection_at`、`last_due_work_at` | timestamptz | YES | — | — |
| `updated_at` | timestamptz | NO | — | — |
