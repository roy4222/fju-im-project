---
type: slice
slice: S14
project: FJU IM Project
updated: 2026-09-23
status: written-v2.1-pending-review
---

> **2026-09-16 導覽更新**：本頁保存原切片規格與歷史狀態。即時進度與 session 分工看 [進度與工作包](</Users/lubaiyu/Documents/roy422的人生online/專案/🌐 網站與互動/📁 輔大資管系專題網站/🛠️ 工程開發/🧱 實作切片/S14 維運執行/00 進度與工作包.md>)；開工依 [並行規則](</Users/lubaiyu/Documents/roy422的人生online/專案/🌐 網站與互動/📁 輔大資管系專題網站/🛠️ 工程開發/🧱 實作切片/02 Session 並行與交付規則.md>)。原有產品接受條件不變。

> **2026-09-23 規格變更（Roy 定案，[主控台交接](</Users/lubaiyu/Documents/roy422的人生online/專案/🌐 網站與互動/📁 輔大資管系專題網站/🛠️ 工程開發/📝 開發紀錄/2026-09-23 主控台交接.md>) 第 2 節；總表見 [05 執行手冊](</Users/lubaiyu/Documents/roy422的人生online/專案/🌐 網站與互動/📁 輔大資管系專題網站/🛠️ 工程開發/🧱 實作切片/05 執行手冊.md>) §6）**：同一台 VM 兩站——正式 `fju.roy422.dev`、測試 `test.fju.roy422.dev`；Cloudflare 只當 DNS。Compose 兩組 project（`fju-test`、`fju-prod`），各自 PostgreSQL、volume、`.env`，共用一個 Caddy 依主機名分流；正式站先建好維持空站，每版同一映像：測試站→Roy 接受→推正式站。Turnstile、R2、age、告警 webhook 不用；備份只放 VM、不做異地；`b1`／`b2` 插槽取消；秘密值存 Doppler（`stg`／`prd`），VM 取值方式由 0-C 設計。#187–#189 屬 TP1（9/27）；#190–#198 的里程碑歸屬待主控台定。#197（清庫預演）前提消失、新範圍待定；#194 副本對外入口、#198 外部告警待定。下方原文保留，與本段衝突時以本段為準。

# S14｜維運：VM 首次設定、Secrets、首次部署、備份到 R2、還原演練、告警、SOP 實跑

## 成果
VM 首次設定、三組憑證就位、第一次 staging 部署（時點待 Roy，預設 S01 後；SOP 01–03 可提前）、health cron、磁碟告警、每日備份到 R2 與本機快照、隔離環境還原演練（獨立專案名、DB、port、附件目錄）、服務重啟證據、worker 重跑 SOP、staging→正式 SOP 預演（不切換）。

## 模組與前置
10、契約 05；前置 S12、S13（SOP 01–03 可在 S01 出場後提前執行，但 FIL-05／06 的 PASS 要在最終版本）。

## 進場資料與產生方式
VM、R2 bucket、age 金鑰、GHCR token、Google OAuth client、Turnstile 金鑰（Roy）。

## schema／API／狀態依賴
`backup` 服務、`backup_runs／restore_drills／storage_stats` 寫入、`deploy.sh` 實跑、`cd.yml` 接上 Secrets 與 environment（方案支援時）。

## 責任範圍、公開介面、跨模組協議
SOP 01–06；每份執行後填執行狀態與證據；還原演練報告明寫附件不在備份範圍。

## 正常與拒絕路徑；重試／回滾／恢復
停止條件：健康判定失敗回滾；備份失敗告警；還原後抽查不符即失敗。恢復：SOP 04。

## 最終責任案例、子步驟、自動測試、Codex、四欄
- 最終責任案例：FIL-05（資料庫備份還原）、FIL-06（重啟與服務狀態）；正式 Gate G6。環境：VM；劇本：SOP 04 的還原演練與重啟段（非瀏覽器）。
- 自動：無（人類操作）；契約 04 §4 的「備份還原」由此證明。
- Codex：核對 SOP 執行紀錄與證據（`restore_drills`、`backup_runs`、health 回應）；不操作 VM。
- v2.1：SOP 04 隔離副本改為寫入暫停窗口＋逐檔 checksum 核對；備份紀錄由 `fju_backup` INSERT；Roy 的平台登入、開通與金鑰待辦見 `🚀 部署與維運/00 Roy 前置工作清單`。

## 出場條件（環境：VM）
SOP 01–05 執行有證據；還原演練報告寫明附件不在範圍；FIL-05、06 PASS；Fable review；Roy 看備份狀態頁。

## 未涵蓋
正式切換（SOP 06 只預演）。

## 依賴
Roy：VM sudo、DNS、OAuth、R2、金鑰、GitHub Secrets 與 environment（方案支援時）；部署 approval＝Roy 親自 dispatch（前置清單 §2.1）。

## 狀態
文件已寫｜待 review｜可實作：否（待前置切片完成與 review）｜實作完成：否｜自動驗證：否｜瀏覽器：否｜VM：否｜Roy 接受：否
