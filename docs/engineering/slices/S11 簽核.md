---
type: slice
slice: S11
project: FJU IM Project
updated: 2026-09-12
status: written-v2.1-pending-review
responsibility: 01 案例責任表
---
# S11｜簽核：版本、逐人同意、老師、退回、重置、匯出、授權查詢

## 成果
管理員建版（全文＋附件版本＋參與者快照）；學生逐人同意／不同意；老師最後同意；退回與新版；管理員重開／作廢／重置；舊頁拒絕；可列印頁與 CSV；`PublicationAuthorizationQuery`。

## 模組與前置
07、08、10；前置 S07。

## 進場資料與產生方式
主線三組。

## schema／API／狀態依賴
契約 01 §3「07」；`SignoffCommand／StatusQuery／PublicationAuthorizationQuery`；DB trigger 拒改內容。

## 責任範圍、公開介面、跨模組協議
S06 的 stub 改真實 supersede；文案「站內內容確認與同意紀錄，行政採認待確認」。頁面：三角色簽核頁、管理建版與匯出。

## 正常與拒絕路徑；重試／回滾／恢復
拒絕：`VERSION_SUPERSEDED`、`STUDENTS_PENDING`、`ALREADY_VOTED`、`NOT_PARTICIPANT`；管理員代簽無入口。恢復：同 requestId。

## 最終責任案例、子步驟、自動測試、Codex、四欄
- 最終責任案例（唯一維護處：[01 案例責任表](<01 案例責任表.md>)）：NTF-05、SGN-01–08、SGN-10–11。
- 自動：狀態機、最後一票、老師提前拒、trigger 與角色拒 UPDATE、匯出欄位。
- Codex：劇本 P07（期中結果確認）、P08（最終授權、匯出）、B05（紀錄內容與改字被拒）。
- 切片出場操作起點（v2.1）：總圖「切片出場與年度案例 PASS 的分界」表的 S11 列；證據存 `steps/S11/`；不以案例 PASS 為出場條件。
- 四欄證據：approvals、匯出檔、拒絕回應、`signoff_version_status`。

## 出場條件（環境：staging）／未涵蓋／依賴
出場：本切片自動測試綠（含內容 UPDATE 被拒、涵蓋四拒）；操作起點走通並留 `steps/S11/`；Fable review。案例 PASS 在完整年度判定。未涵蓋：正式用途採認（校方）。依賴：校方 SGN-01a／b（不阻塞實作）。

## 狀態
文件已寫｜待 review｜可實作：否（待 S00–前置切片完成與 review）｜實作完成：否｜自動驗證：否｜瀏覽器：否｜VM：否｜Roy 接受：否
