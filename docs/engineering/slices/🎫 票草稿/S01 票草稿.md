---
type: ticket-drafts
slice: S01
project: FJU IM Project
updated: 2026-09-13
status: draft-v0.3-pending-codex-review
---
# S01 票草稿｜帳號與 Auth、最小屆別與開放註冊、名單、核准、稽核與帳本（v0.3，2026-09-13）

## 2026-09-15 定案補充

Roy 在 9/14 會議後確認：完整 V1 可試用、分區實作；評分規則維持既定設計，僅補齊成績匯出與名單匯入／匯出。系級指「資管二甲／資管二乙」，不是專題屆別。本節為現行增補；與下方舊文字有衝突時以本節為準。僅文件與 issue 更新，未實作、未驗收，PR 未在本次合併。

### [#44](https://github.com/roy4222/fju-im-project/issues/44)

系級資料需在名單列、註冊申請與其修改歷史、核准後學生資料之間完整保存，支援 #51、#52、#53、#59。系級是「資管二甲／資管二乙」這類完整文字，與 cohort 專題屆別分開；舊資料缺值保留空白，不從屆別猜填。資料字典、migration 與欄位往返測試須一起補齊；不改既有名單比對與授權規則。

### [#51](https://github.com/roy4222/fju-im-project/issues/51)

匯入功能已在本票，不新增重複票。CSV 增加可帶入的系級欄位（department_class，例：資管二甲、資管二乙）；保留既有 student_no、name、email 與匯入時選屆別的流程。舊三欄 CSV 仍可匯入，缺系級保留空白；預覽顯示系級，確認後保存到名單列，離頁重開仍相同。fixture 同時涵蓋兩種系級與缺值；原檔下載仍保留原始內容。原檔下載不等於帳號名單匯出；管理頁所選名單匯出由 #59 承接。

### [#52](https://github.com/roy4222/fju-im-project/issues/52)

學生註冊、待審檢視與修改申請增加系級（例：資管二甲、資管二乙），保存完整文字並留下修改版本；登出再登入仍可讀回。系級與專題屆別分開，不能由系級推導屆別或取得權限。Google 首次註冊補資料也使用相同欄位與保存流程，與 #57 對接；既有人工審核規則不變。

### [#53](https://github.com/roy4222/fju-im-project/issues/53)

審核對話框並列申請者填寫的系級與名單系級；來源缺值或不一致時如實顯示，供管理員核實，不默默覆蓋其中一方。核准時保存本次確認的系級供 #59 名單匯出；原始名單與申請修改歷史仍保留。系級不取代姓名／學號的名單比對，也不成為自動核准條件。驗證申請修改後舊審核頁仍會拒絕過期提交。

### [#57](https://github.com/roy4222/fju-im-project/issues/57)

Google 首次註冊補學生資料時，使用 #52 的同一系級欄位與申請流程；既有 Google 帳號連結不另建學生資料，不清空或覆蓋已保存系級。補受控測試：Google 新申請的系級可在 #53 審核後由 #59 匯出；已存在帳號連結 Google 後系級保持。此補充不改已核准者的本人可編輯欄位或真 Google 人工驗證界線。

### [#59](https://github.com/roy4222/fju-im-project/issues/59)

本票正式承接帳號／學生名單匯出，取代舊文「匯出 CSV 不在本票／未定切片」。管理員可在目前篩選結果中勾選部分或全選全部符合條件資料（不限目前分頁），下載 UTF-8 CSV。欄位至少含姓名、學號、系級、屆別、手機、Email、角色、狀態；可匯出未分組學生，不以組別是否成立為前提。這是管理名單輸出；原始上傳 CSV 下載仍由 #51 負責，組別資料匯出仍由 #105 負責。

完成條件：
- A1 匯入含「資管二甲／資管二乙」的名單、完成學生申請與核准後，管理頁與匯出系級一致；舊資料缺值保留空白。
- 搜尋篩選後勾選部分、跨頁全選，匯出人員與筆數等於伺服器當下授權範圍及所選結果，不混入未選屆別。
- 學號保留前導零，中文、逗號、引號與換行可正確讀回；沿用契約 03 §5 的 CSV 公式字首防護。
- 匯出每次重新授權；未登入 401，學生／老師 403；回傳 attachment、nosniff 並留匯出稽核紀錄。
- 提供實際 CSV、畫面與下載筆數核對、拒絕回應與資料保存證據；追溯 ACC-15。


> spec 基準：母 spec v3.6（`f974bce`）通過；v0.3 依票草稿集中 review（F01–F15、R01–R08）局部修訂，spec 局部回寫為母 spec v3.7（契約 01 v2.5、05 v2.4、模組 02／05／07／08／09／10 v2.4、總圖 v2.4）。本檔是票草稿，不是 ticket；待 Roy 與 Codex 集中 review 後才開 GitHub 票。切片卡：[S01 帳號與 Auth](</Users/lubaiyu/Documents/roy422的人生online/專案/🌐 網站與互動/📁 輔大資管系專題網站/🛠️ 工程開發/🧱 實作切片/S01 帳號與 Auth/00 進度與工作包.md>)。v0.2 對第一批 review 的修正：T01——Auth schema 與測試工具已在 S00-02／S00-03，本切片的 S01-02 只留路由、hook、運行行為 gate；T02——S01-04 改為早期共用殼（可獨立關閉），功能票交各自頁面，整條走通留 E01 出場；T03——S01-13 預授權沿同一個已建立的 user 登入、改密、補資料，`users` 只有一筆、ID 不變、teacher role 保留；帳號管理列表獨立成 S01-16。
>
> **epic 出場（E01）**：自動測試綠——模組 01 §10 全部（含 D1 釘版（1）–（8）中本切片負責的（1）（2）（3）（4）（5）（6）（7）（8）、`SecretOnce` 掃描、Turnstile 六條路徑）；切片操作起點（總圖表 S01 列：A1 一次性密碼登入→改密→建最小屆別＋兩旗標→上傳 CSV 預覽／匯入與原檔下載→學生密碼註冊→待審修改→A1 核實核准→學生登入首頁殼→停用→臨時密碼→連結入口截圖）留 `steps/S01/`；Fable review；Roy 看核准對話框與等待審核頁。環境 local→staging（staging 未就緒可 local 並標明）。案例 PASS 在完整年度（P01、P02、B01）；ACC-13／16／18 人工 Roy。

## 建議順序（每張票只用編號更前的前置）
| 票 | 目標 | 前置 | 提供給後面的票什麼 |
|---|---|---|---|
| S01-01 | 第二支 migration（增量） | S00-04、S00-05 | 模組 01 九表、`stored_files`、`file_references`、對應 GRANT |
| S01-02 | Better Auth 掛載、路由矩陣、hooks、運行 gate | S01-01 | `/api/auth/*` 白名單與封鎖、pending 注入、`login_method`、route table 與 ctx gate 結果 |
| S01-03 | 內部呼叫辨識、ActorResolver、AuditWriter、OperationLedger | S00-03、S01-02 | 包裝器（唯一入口）、`Actor`、audit、requestId 冪等與回執 |
| S01-04 | 早期共用殼 | S00-11、S01-03 | 路由、layout、導覽、掛載點、授權邊界、空狀態 |
| S01-05 | seed A1、登入、一次性密碼改密、登出 | S01-03、S01-04 | 可登入的 A1、`/login`、改密頁、登入限速 |
| S01-06 | 最小屆別與兩旗標 | S01-05 | `CohortCommand` 最小、`CohortStatusQuery.registrationOpen()`、屆別最小頁 |
| S01-07 | 最小檔案能力 | S01-01、S01-03、S01-04 | `FileStorage` ticket→upload→finalize→attach／release→download |
| S01-08 | 名單匯入 | S01-06、S01-07 | `roster_versions`／`roster_entries`、fixture CSV、匯入 UI |
| S01-09 | 學生密碼註冊、待審、修改 | S01-04、S01-06 | pending 帳號與申請、`/register`、`/register/pending` |
| S01-10 | 核准與退回 | S01-08、S01-09 | active 學生、`student_identities`、`role_assignments`、審核對話框、`syncRoster` no-op 介面 |
| S01-11 | 狀態事件、停用／恢復／去識別化、執行器、`readBanned` | S01-02、S01-03、S01-10 | `user_status_events`、`session_revocations` 主工作＋收斂、`SessionRevocationExecutor`、`BetterAuthUserStateReader` |
| S01-12 | 臨時密碼 `SecretOnce` | S01-05、S01-11 | `issueTemporaryPassword`、must-change 流程 |
| S01-13 | 老師建立與預授權（沿同一 user） | S01-10、S01-12 | `createTeacher`／`preauthorizeTeacher`、補資料頁 |
| S01-14 | 本人帳號頁 | S01-05、S01-11 | `/account`、`SelfAccountCommand` 全部方法 |
| S01-15 | Turnstile 伺服器驗證 | S01-05、S01-09 | 門檻計數與 siteverify |
| S01-16 | 帳號管理列表與磚 | S01-10、S01-11 | `UserDirectoryQuery`、`EligibleStudentsQuery`、搜尋篩選 |

## 票

### S01-01 第二支 migration（增量）
- **目標**：在 S00 表之上**只新增**模組 01 附錄 A 九表與模組 10 的 `stored_files`、`file_references`；S00 的 Auth 四表、最小 `cohorts`、基礎表不重建不改；GRANT 矩陣與 `roles.test.ts` 擴充到新表；不可變表 trigger 就位。
- **spec**：模組 01 v2.4 附錄 A（`user_profiles`、`student_identities`、`registration_applications`、`application_revisions`、`roster_versions`、`roster_entries`、`role_assignments`、`user_status_events`、`session_revocations`——含 `trigger`／`reconcile_reason`／`reconcile_of_id`／`reconcile_round`、新增的 CHECK、三條部分唯一、索引）；模組 10 附錄 A `stored_files`、`file_references`；契約 01 §5、§11、§12。
- **範圍**：一支純 SQL；新表 GRANT；trigger（`application_revisions`、`roster_entries`、`user_status_events`）；`roles.test.ts` 新增列。**非範圍**：模組 02 擴充欄（S02-01）；任何用例。
- **前置**：S00-04、S00-05。
- **路徑**：空庫 S00→S01 升級；前一版 seed 庫升版（契約 01 §12 CI 項）；重跑 no-op。
- **自動測試**：兩種 migration；逐欄存在性；`session_revocations` CHECK 與三條部分唯一以 INSERT 反例證明；`file_references` 部分唯一；`roles.test.ts` 擴充。
- **操作證據**：migration 輸出；約束反例輸出（`steps/S01/S01-01/`）。
- **完成定義**：票上附「本票新增表 vs S00 已有表」對照，逐欄核對附錄 A；測試綠。
- **待 Roy／平台**：無。

### S01-02 Better Auth 掛載、路由矩陣、hooks、運行 gate
- **目標**：Better Auth 掛在 `app/api/auth/[...all]`，前置白名單與封鎖清單依契約 03 §2 生效（含 `GET` 與 `POST` 的 `/admin/list-users`、其他 `/admin/*`、`unlink-account`、`update-user`、`forget-password` 等全部封鎖；第二層 `hooks.before` 丟 `APIError`）；`databaseHooks.user.create.before` 注入 `status='pending'`；`databaseHooks.session.create.before` 寫 `sessions.login_method`；`generateId` uuidv7；`disableImplicitLinking=true`；`cookieCache` 關；`freshAge` 10 分鐘且驗證選項存在；admin plugin 只在伺服器。
- **spec**：母 spec §4.12、§7.3；契約 03 §2；契約 01 §4.1；模組 01 §12（route table 覆核）。
- **範圍**：路由掛載、白名單、hooks、設定；**兩個運行 gate**寫成測試並附版本：（a）實際 route table 與矩陣逐路由對照（GET／POST 都列）；（b）`hooks.before` 在 server-only 呼叫時 `ctx.request` 不存在。**非範圍**：內部呼叫包裝器與 ActorResolver（S01-03）；欄名 gate（已在 S00-02）；任何頁面。
- **前置**：S01-01。
- **路徑**：白名單路由可達；封鎖路由對任何 session（含管理員）403。
- **自動測試**：路由矩陣（每路由 × 每狀態；狀態相關格在 S01-03、S01-05、S01-09 補齊時擴充同一測試）；兩個 gate；pending 注入；`login_method` 寫入。
- **操作證據**：安裝版本、route table 輸出、封鎖端點回應（P00 步驟 7）。
- **完成定義**：gate 結果記在票與模組 01 §12（不一致先回 Vault 改 spec）；測試綠。
- **待 Roy／平台**：無。

### S01-03 內部呼叫辨識、ActorResolver、AuditWriter、OperationLedger
- **目標**：`auth.api.*` 管理員能力只能經內部包裝器（AsyncLocalStorage marker＋無 `ctx.request`）呼叫，直接引用被 lint 擋；`ActorResolver.resolve(sessionToken)` 回 `Actor{userId, roles[], status, mustChangePassword, cohortMemberships[]}` 並依狀態拒絕（pending 只准查看／修改申請，must-change 只准改密與登出，disabled 一律 `UNAUTHENTICATED`）；`AuditWriter.append`、`OperationLedger.begin／commit／get` 可用，同 requestId 重播回同一回執，`getOperationResult` 可取回。
- **spec**：母 spec §4.12、§4.14；契約 03 §2（三向測試）、§4；契約 01 §4.3、§4.4、§8；模組 10 §5；模組 01 §5 `ActorResolver`。
- **範圍**：包裝器、ActorResolver、AuditWriter、OperationLedger（ON CONFLICT 協議）。**非範圍**：`receipt_purge` 到期工作（S12）；任何業務用例；policy 的各模組規則（各切片自己的票）。
- **前置**：S00-03、S01-02。
- **路徑**：外部 HTTP 打 `admin/*` 被擋；內部包裝呼叫成功；缺 marker 的伺服器呼叫被擋；帳本兩連線同 requestId 只成功一次；`fju_app` 對 audit UPDATE 被拒（S00-05 已證，此處引用）。
- **自動測試**：三向測試；ActorResolver 狀態矩陣（以直接寫入 `users.status` 的測試資料，不需註冊流程）；帳本 ON CONFLICT 兩連線；audit 雙時間欄。
- **操作證據**：封鎖端點回應；帳本重播回應。
- **完成定義**：測試綠；lint 規則證明包裝器是唯一入口。
- **待 Roy／平台**：無。

### S01-04 早期共用殼
- **目標**：app 路由與 layout 骨架可達：`/`（空殼）、`/login`、`/register`、`/register/pending`、`/account`、`/dashboard/{admin,teacher,student}` 三角色首頁殼與側欄導覽、`/dashboard/admin/{accounts,cohorts}` 掛載點；未登入直接請求 dashboard 導登入或 401；角色不符 403；每頁有空狀態；系網橘單一主軸與元件基底從 `prototype/` 搬並拆元件。**不含任何功能表單與用例。**
- **spec**：模組 01 §7、§7.1（原型對照的頁面清單）；契約 02 §7（server／client 邊界）；契約 03 §1（授權邊界在用例層，頁面只做導向）；產品區 UI 偏好；S01 卡「頁面」。
- **範圍**：路由、layout、導覽、掛載點、授權導向、空狀態、共用元件基底（按鈕、表格、對話框殼、磚）。**非範圍**：登入表單（S01-05）、註冊表單（S01-09）、各對話框（各功能票）、階段文字（S02-05）、通知匣入口（S02-08）。
- **前置**：S00-11、S01-03。
- **路徑**：未登入→導登入；越權→403（越權核對截圖對照 §7.1「越權核對」）；空狀態顯示。
- **自動測試**：e2e-smoke：每條路由可達且授權導向正確（以測試 session 直接寫入）；直接 HTTP 負向。
- **操作證據**：路由與空狀態截圖。
- **完成定義**：上述路由與導向全部可用；不含功能；Fable review。**本票不等任何功能票。**
- **待 Roy／平台**：無。

### S01-05 seed A1、登入、一次性密碼與強制改密、登出
- **目標**：`pnpm seed:a1` 只建 A1（Email 與一次性密碼由環境變數提供，不入檔）；A1 以一次性密碼登入→導到改密頁→改密後進管理員首頁殼→其他 session 撤銷→登出後以新密碼重登；登入限速 10 次／10 分鐘／IP＋帳號。
- **spec**：模組 01 §3（must-change）、§8；契約 03 §3、§6；劇本 P00 步驟 3–4、§6、§7。
- **範圍**：seed、登入表單（掛在 S01-04 的 `/login`）、改密頁、`SelfAccountCommand.changePassword`（`revokeOtherSessions`）、限速。**非範圍**：Turnstile（S01-15）；Google 登入（S01-14）。
- **前置**：S01-03、S01-04。
- **路徑**：must-change 打其他業務 action `PASSWORD_CHANGE_REQUIRED`；舊一次性密碼再登入失敗；限速拒絕。
- **自動測試**：must-change 矩陣；改密撤其他 session；`SELECT status, must_change_password`；限速。
- **操作證據**：P00 步驟 3–4 截圖、SQL。
- **完成定義**：P00 §6、§7 三項可重現。
- **待 Roy／平台**：A1 一次性密碼由 Roy 經密碼管理器提供。

### S01-06 最小屆別與兩旗標
- **目標**：A1 在 `/dashboard/admin/cohorts`（最小：列表、建立、啟用、兩旗標）建 115-TEST 並設為預設工作屆別與開放註冊屆別；`CohortStatusQuery.registrationOpen()` 可用；旗標部分唯一違反 `CONFLICT`。
- **spec**：契約 01 §4.2；模組 02 §3、§5（`CohortCommand.create／activate／setDefaultWorking／setRegistrationOpen`、`CohortStatusQuery`）、§10；劇本 P00 步驟 5、P01 屆別步驟；案例 COH-01（子步驟；最終 S02）。
- **範圍**：port 最小實作（模組 02 擁有、本票交付）、頁面、`REGISTRATION_COHORT_CODE` 環境變數只作 S02 前備援。**非範圍**：階段、年度結束日、活動、封存；`cohorts` 擴充欄（S02-01）。
- **前置**：S01-05。
- **路徑**：建立→啟用→設旗標；第二個開放註冊屆 `CONFLICT`；非 admin 拒。
- **自動測試**：旗標部分唯一；`registrationOpen()`；授權；requestId 重播。
- **操作證據**：屆別頁截圖；`cohorts` 列。
- **完成定義**：測試綠；頁面可用。
- **待 Roy／平台**：無。

### S01-07 最小檔案能力
- **目標**：`FileStorage.issueUploadTicket→beginUpload（Route Handler）→finalize→attach／release→authorizeDownload` 可用於 `purpose='roster_csv'`；私有下載逐次授權；無 GC。
- **spec**：模組 10 §5、§6、§12（S01 最小檔案能力）、附錄 A；母 spec §4.14；契約 03 §5（類型白名單、magic bytes、大小）；契約 01 §11。
- **範圍**：ticket、上傳 Route Handler（Origin 檢查）、finalize（不在業務交易內）、attach／release、下載授權鏈、上傳元件掛在 S01-04 的殼。**非範圍**：GC、上傳中斷殘留清理（S07／S12）；管理員附件用途（S04）。
- **前置**：S01-01、S01-03、S01-04。
- **路徑**：類型、大小、中斷三種錯誤；finalize 失敗刪暫存；未授權下載 401／403。
- **自動測試**：白名單與 magic bytes 單元；finalize 失敗；下載授權鏈；`file_references` 移除再附回不撞唯一鍵。
- **操作證據**：三種錯誤回應；下載 403。
- **完成定義**：模組 10 §12 S01 列能力齊；GC 列為未涵蓋。
- **待 Roy／平台**：無。

### S01-08 名單匯入
- **目標**：A1 上傳 15 列 CSV（13 唯一＋1 重複 S03＋1 缺姓名，S09 無 email）看預覽摘要，確認後整批一交易寫 `roster_versions`／`roster_entries`，原檔以 S01-07 保存並可下載。
- **spec**：模組 01 §5 `RosterCommand.previewImport／import`、§6、附錄 A；S01 卡進場資料（fixture 格式）；劇本 P01 步驟 6–10；案例 ACC-01。
- **範圍**：正規化、去重與衝突、預覽、匯入、fixture 實檔 `📖 操作與驗證/fixtures/roster-115-test.csv`、匯入 UI（掛在帳號管理掛載點）。**非範圍**：名單比對改狀態（不存在）。
- **前置**：S01-06、S01-07。
- **路徑**：`VALIDATION_FAILED`；重複列跳過並記錄；requestId 重播回同 `rosterVersionId`。
- **自動測試**：正規化、去重與衝突單元；整批一交易；重播。
- **操作證據**：預覽截圖、`roster_entries` 列數、原檔下載。
- **完成定義**：fixture 進 repo；P01 步驟 6–10 名單部分可走。
- **待 Roy／平台**：校方 CSV 樣本不阻塞。

### S01-09 學生密碼註冊、等待審核頁、修改申請
- **目標**：學生在 `/register` 以密碼註冊→pending、申請與 revision、`roster_match`→登入停在 `/register/pending`（看狀態與退回理由、可修改）→登出再登入仍在待審頁；沒有開放註冊屆時顯示「目前未開放註冊」；註冊限速 5 次／小時／IP。
- **spec**：模組 01 §2、§3、§5（`RegistrationCommand.apply／revise`、`SelfAccountCommand.viewMyApplication／reviseMyApplication`）、§8；契約 03 §2（pending 受限 session）；劇本 P02 步驟 1–17；案例 ACC-02、03、05。
- **範圍**：註冊與待審頁表單（掛在 S01-04）、apply／revise（revision 過期 `CONFLICT`）、限速。**非範圍**：核准（S01-10）；Turnstile（S01-15）；Google 註冊（S01-14）。
- **前置**：S01-04、S01-06。
- **路徑**：pending 打其他業務 action `ACCOUNT_PENDING`；未開放註冊；revision 過期。
- **自動測試**：apply→pending；revision 過期；pending 矩陣（登出再登入仍受限、直接請求業務 action 與下載被拒；擴充 S01-02 的路由矩陣 pending 列）。
- **操作證據**：註冊回執、待審頁截圖（Roy 看的畫面之一）、`SELECT status`。
- **完成定義**：P02 步驟 1–17 學生側可走。
- **待 Roy／平台**：無。

### S01-10 核准與退回
- **目標**：A1 在待審清單看到比對標示，審核對話框選核實方式核准（同交易：申請列 `FOR UPDATE`＋revision→`student_identities`→`role_assignments`→`user_profiles.cohort_id`→05 `syncRoster` no-op 介面→回執）或退回（保留 user）；回應遺失可 `getOperationResult` 取回；核准後學生登入進首頁殼。
- **spec**：模組 01 §5 `RegistrationCommand.approve／reject`、§6、附錄 A；契約 03 §3（必選核實方式）；劇本 P02 步驟 1–17、§6；案例 ACC-02、03、04、05、10。
- **範圍**：用例、待審列表與對話框（掛在帳號管理掛載點）、`syncRoster` no-op 介面（模組 05 擁有，S04 換真）。**非範圍**：核准事件投影（S02；依產品規則不投影）；完整帳號列表與磚（S01-16）。
- **前置**：S01-08、S01-09。
- **路徑**：缺核實方式拒；revision 過期 `CONFLICT`；相同學號 `STUDENT_NO_TAKEN` 整體回滾；重播。
- **自動測試**：approve 缺核實方式；占用表衝突；reject 保留 user；回執取回；以 `fju_app` 跑核准正向（契約 01 §5）；`syncRoster` 被呼叫一次（介面層）。
- **操作證據**：核准對話框截圖（Roy 看的畫面）、`student_identities` 列、回執。
- **完成定義**：測試綠；學生核准後登入首頁殼。
- **待 Roy／平台**：無。

### S01-11 狀態事件、停用／恢復／去識別化、`SessionRevocationExecutor`、`readBanned`（D1 主體）
- **目標**：A1 停用學生→`user_status_events`＋`session_revocations` 主工作→commit 後執行器 `banUser`→舊 session 立即被拒；恢復→`unbanUser`；去識別化含影響預覽與二次確認；管理端「重試」插入 `manual_retry` 收斂工作；完成後核對（`after_completion`）以 `BetterAuthUserStateReader.readBanned`（`fju_app` 唯讀 SELECT `users.banned`）比對並依規則 5 插入收斂工作；自動 round 10→failed `RECONCILE_LIMIT`＋audit＋告警事件。
- **spec**：模組 01 v2.4 §3、§5（`AccountCommand.disable／restore／deidentifyWithPreview`）、附錄 A 規則 1–6、兩種保證、逐列序列（b）、§10 D1（1）（2）（4）（5）（6）（7）（8）；母 spec §4.12；契約 03 §2、§3；案例 ACC-09、14、15。
- **範圍**：執行器（排隊、認領租約 30 秒、執行逾時 10 秒、完成 owner fencing、完成後核對、上限）、`readBanned`、三個用例、對應對話框（停用、恢復、去識別化預覽與確認）掛在帳號管理掛載點。**非範圍**：worker 週期核對與觀察集合（S02-09）；告警通道（S14）。
- **前置**：S01-02、S01-03、S01-10。
- **路徑**：（a）正常完成順序；（b）租約過期後舊 ban 晚到，本票以人工觸發 `after_completion`／`manual_retry` 走到步驟 9，週期核對留 S02-09；（c）連續三事件只留最後一筆；（d）兩個核對者只留一筆；（e）round 10 上限。
- **自動測試**：模組 01 §10 D1（1）`readBanned` 正向、（2）反向（`/admin/list-users` GET 與 POST 帶管理員 session 仍被擋——Codex 小項）、（4）逐列序列（b）列級斷言、（5）並發插入、（6）上限與 audit、（7）owner fencing、（8）順序保證；banUser 後舊 session 為 null；deidentify 不斷關聯。
- **操作證據**：停用後直接請求 `UNAUTHENTICATED`（P02 步驟 25）；`session_revocations` 列 SQL。
- **完成定義**：（1）（2）（4）–（8）綠；（3）欄名 gate 已在 S00-02；S02-09 補週期核對。
- **待 Roy／平台**：無。

### S01-12 臨時密碼（`SecretOnce`）與強制改密
- **目標**：A1 對指定帳號核發臨時密碼→回應只顯示一次→帳本、audit、log、trace、fingerprint 無原始密碼→該帳號登入後只能改密與登出→回應遺失時重新核發、舊密碼失效；重播回「已核發，無法取回」。
- **spec**：模組 01 §5 `issueTemporaryPassword→SecretOnce`、§8；母 spec §4.12 一次性秘密；契約 03 §3；契約 04 §4 掃描；劇本 P02 步驟 22–24；案例 ACC-08。
- **範圍**：用例（伺服器端 `setUserPassword`＋`revokeUserSessions`＋`must_change_password=true`）、`SecretOnce` 型別、掃描測試、對話框。**非範圍**：Email 重設。
- **前置**：S01-05、S01-11。
- **路徑**：重播；重新核發。
- **自動測試**：DB／log／trace 掃描無秘密；重播；must-change 矩陣（沿用 S01-05）。
- **操作證據**：一次顯示畫面；重播回應。
- **完成定義**：掃描綠。
- **待 Roy／平台**：無。

### S01-13 老師建立與預授權（沿同一個 user）
- **目標**：A1 建立老師（`createTeacher`：伺服器端 `createUser`＋`role_assignments(teacher)`）或預授權 Email（`preauthorizeTeacher`：同樣建立 `users.status='active'`＋teacher role，profile 待補）；之後 A1 對**同一個**預授權 user 核發臨時密碼（S01-12），老師以臨時密碼登入→強制改密→補資料頁（姓名、手機）→老師首頁；全程 `users` 只有一筆、user ID 不變、teacher role 保留。**沒有「預授權後重新註冊」這條路。**
- **spec**：模組 01 §3「老師建立／預授權」列（`createUser`＋`role_assignments(teacher)`；預授權 `status='active'` 但 profile 待補）、§7.2「老師首次登入補資料頁（預授權路徑）」；劇本 P01 步驟 6–10、P02 步驟 18（同一 user，不新建帳號；`users` 只有一筆 T2）；案例 ACC-06、07。
- **範圍**：兩個用例、補資料頁（老師首次登入且 profile 未補時導向）、老師列表列。**非範圍**：Google 預授權路徑（B01 R0，人工 Roy；受控 adapter 測試在 S01-14 的連結票只證明「同 Email 的 Google 身分連到既有 user」，不新建帳號）；老師填表（範圍外）。
- **前置**：S01-10、S01-12。
- **路徑**：Email 重複拒；預授權 user 以臨時密碼登入→改密→補資料→首頁；補資料前業務 action 拒（`PROFILE_INCOMPLETE` 或依契約 02 錯誤碼，若 spec 未列則沿用 must-change 的處理並在票上標「錯誤碼待補 spec」）。
- **自動測試**：createTeacher；preauthorizeTeacher 後 `users` count=1、`status='active'`、teacher role；核發臨時密碼→登入→改密→補資料後 user ID 不變、role 保留、`users` 仍 1 筆；補資料前拒。
- **操作證據**：P02 步驟 18 截圖與 `SELECT count(*) FROM users WHERE email=…`。
- **完成定義**：P01 步驟 6–10 老師部分與 P02 步驟 18 可走。
- **待 Roy／平台**：無。

### S01-14 本人帳號頁：聯絡資料、改密、連結 Google、設密碼
- **目標**：`/account` 可改手機與聯絡 Email、改密、連結 Google（fresh session 內；已綁他人拒絕）、Google 帳號設密碼（伺服器端 `setPassword`）；同 Email 密碼帳號以 Google 登入→`account_not_linked` 文案；`unlink` 不存在。
- **spec**：模組 01 §2、§5 `SelfAccountCommand`、§8；契約 03 §2（`link-social`、`list-accounts` 限本人 fresh session；`unlink-account` 封鎖）、§3；案例 ACC-13、16、17、18（13／16／18 人工）。
- **範圍**：頁面與用例、`FRESH_SESSION_REQUIRED`、Google OAuth 受控 adapter。**非範圍**：真 Google 實測（Roy）。
- **前置**：S01-05、S01-11。
- **路徑**：非 fresh 拒；已綁他人拒；`account_not_linked`。
- **自動測試**：linkSocial 衝突；fresh session；受控 adapter 拒絕與故障；`updateContact` 重載仍保存、別人不能改。
- **操作證據**：連結入口截圖；Google 三案入口截圖標 BLOCKED（人工）。
- **完成定義**：受控測試綠；三案標 BLOCKED。
- **待 Roy／平台**：Google OAuth client 與測試身分（前置清單 §2.2、§3.2）。

### S01-15 Turnstile 伺服器驗證
- **目標**：登入／註冊達門檻（伺服器計數；連續登入失敗 5 次）必須附 token，後端向 siteverify 驗證並比對 action（`login`／`register`）與 hostname；缺、無效、重播、過期回 `TURNSTILE_REQUIRED`；siteverify 故障 fail closed。
- **spec**：契約 03 §6、§7；母 spec §4.12；模組 01 §10；前置清單 §3.4。
- **範圍**：計數、驗證、受控 adapter、前端 widget 掛在登入與註冊表單。**非範圍**：真實服務（Roy 人工）。
- **前置**：S01-05、S01-09。
- **路徑**：六條路徑。
- **自動測試**：六條路徑受控 adapter。
- **操作證據**：達門檻後拒絕回應。
- **完成定義**：六條綠；真實服務標人工。
- **待 Roy／平台**：Turnstile 金鑰（前置清單 §3.4）。

### S01-16 帳號管理列表與磚
- **目標**：`/dashboard/admin/accounts` 顯示已核准／待審核／已停用磚與可搜尋篩選的表格（姓名、學號、狀態、角色、最近登入方式 `user_profiles.login_method_last`）；`EligibleStudentsQuery`（已核准未停用學生 by cohort）供後續切片；老師無此頁（403）。
- **spec**：模組 01 §5 `UserDirectoryQuery`、`EligibleStudentsQuery`、§7.1「帳號管理」組、§9；案例 ACC-10、11。
- **範圍**：兩個 query、列表頁（整合 S01-08／S01-10／S01-11／S01-12／S01-13 已掛上的入口）。**非範圍**：匯出 CSV（模組 01 §9 提到「只管理員」；spec 未定切片，列為問題）。
- **前置**：S01-10、S01-11。
- **路徑**：非 admin 403；搜尋篩選；空狀態。
- **自動測試**：query 授權；篩選正確；S03（學生）直接請求 S01 資料回應被拒（ACC-11）。
- **操作證據**：列表截圖對照 §7.1；直接請求回應。
- **完成定義**：測試綠；頁面與 §7.1 對照無缺。
- **待 Roy／平台**：無。

## 紙上依賴核對
- S01-01：資料表←自身（在 S00-04 之上）；工具←S00-03；入口←無；劇本：不適用。
- S01-02：資料表←S01-01（`users` 擴充欄在 S00-04）；工具←S00-03、S00-02；入口←`/api/auth/*` 自身；劇本 P00 步驟 7 可走。
- S01-03：資料表←S00-04（audit、ledger）、S01-01（profiles、roles）；工具←S00-03、S00-06（lint 規則）；入口←無。
- S01-04：資料表←無；工具←S00-03（e2e-smoke 在 S00-09 接線）；入口←自身；劇本 P00 步驟 5–6 的頁面存在（空狀態）。
- S01-05：資料表←S00-04；port←S01-02、S01-03；入口←S01-04 的 `/login`；劇本 P00 步驟 3–4、§6、§7 可走。
- S01-06：資料表←S00-04 最小 cohorts；port←S01-03；入口←S01-04 的 `/dashboard/admin/cohorts`；劇本 P00 步驟 5 可走。
- S01-07：資料表←S01-01；port←S01-03；入口←S01-04；劇本：無。
- S01-08：資料表←S01-01；port←S01-06、S01-07、S01-03；入口←S01-04 帳號管理掛載點；劇本 P01 步驟 6–10（名單）可走。
- S01-09：資料表←S01-01；port←S01-06、S01-03；入口←S01-04 的 `/register`、`/register/pending`；劇本 P02 步驟 1–17 學生側可走。
- S01-10：資料表←S01-01；port←S01-03、S01-08、S01-09；入口←S01-04；劇本 P02 步驟 1–17 管理端可走。
- S01-11：資料表←S01-01；port←S01-02、S01-03；入口←S01-04；劇本 P02 步驟 25 可走；序列（b）步驟 6–9 需 S02-09 才由 worker 完成（本票人工觸發）。
- S01-12：port←S01-11（`revokeUserSessions` 經執行器）、S01-05；入口←S01-04；劇本 P02 步驟 22–24 可走。
- S01-13：port←S01-10、S01-12；入口←S01-04；劇本 P01 步驟 6–10、P02 步驟 18 可走。
- S01-14：port←S01-05、S01-11；入口←S01-04 的 `/account`；劇本 B01 連結段可走（Google 人工）。
- S01-15：port←S01-05、S01-09；入口←登入／註冊表單。
- S01-16：port←S01-10、S01-11；入口←S01-04；劇本 P02 步驟 19–21 可走。
無循環；每票的前置編號都更小。E01 出場的整條操作起點在 S01-16 完成後可走。

## 讀 spec 時發現的問題（不改 spec，只列）
- 模組 01 §9「匯出 CSV 只管理員」沒有指定交付切片與 port；本批未拆票，建議 Roy 決定放 S01 追加票或 S12 檔案工作台。
- 預授權老師「補資料前」的業務 action 拒絕錯誤碼，模組 01 與契約 02 §10 未列（S01-13 暫沿 must-change 處理），建議補 spec 一句。
- 模組 01 §10 D1（2）只寫 `POST /api/auth/admin/list-users`；Codex 指出實際路由可能是 GET，S01-02／S01-11 測試同時涵蓋 GET 與 POST，建議 spec 改「依安裝後路由表」。
