---
type: ticket-drafts
slice: S06
project: FJU IM Project
updated: 2026-09-13
status: draft-v0.3-pending-codex-review
---
# S06 票草稿｜主指導：認領、指派、重派、CSV 批次；合作案與連結（v0.3，2026-09-13）

> spec 基準：母 spec v3.6（`f974bce`）通過；v0.3 依票草稿集中 review（F01–F15、R01–R08）局部修訂，spec 局部回寫為母 spec v3.7（契約 01 v2.5、05 v2.4、模組 02／05／07／08／09／10 v2.4、總圖 v2.4）。本檔是票草稿，不是 ticket；待 Roy 與 Codex 集中 review 後才開 GitHub 票。切片卡：[S06 指導與產學](</Users/lubaiyu/Documents/roy422的人生online/專案/🌐 網站與互動/📁 輔大資管系專題網站/🛠️ 工程開發/🧱 實作切片/S06 指導與產學/00 進度與工作包.md>)。
>
> **epic 出場（E06）**：自動測試綠——migration 與 roles 三表（S06-01）、殼授權邊界（S06-02）、`AdvisorQuery` 與 recipients 單元（S06-03）、並發認領同步屏障（S06-04）、指派／解除 audit 與 revision（S06-05）、重派同交易與 stub（S06-06）、批次六類分類與 revision `CONFLICT`（S06-07）、合作案狀態機與私有欄白名單（S06-08）、連結基數與下架不收新連結（S06-09）、類型三條件（S06-10）、匯出安全（S06-11）；切片操作起點（總圖表 S06 列：認領（整合測試並發）→指派／重派（評分清單為空）→CSV 批次六類→合作案建立／連結）留 `steps/S06/`；Fable review；Roy 看的畫面：切片卡未註明（無）。環境 staging（未就緒可 local 並標明）。案例 PASS 在完整年度。前置切片：`E03 出場`（G1／G2／G3、`groups` 表、`/dashboard/admin/groups` 頁）。

## 建議順序（每張票只用編號更前的前置）
| 票 | 目標 | 前置 | 提供給後面的票什麼 |
|---|---|---|---|
| S06-01 | 增量 migration：`advisor_assignments`、`industry_opportunities`、`opportunity_links` | S00-04、S00-05、E03 出場 | 三表、部分唯一、GRANT、roles 測試列 |
| S06-02 | 殼：老師組別頁、產學頁、老師合作案頁、管理員合作案頁、管理員組別頁按鈕掛載點、授權邊界 | S01-04、E03 出場 | 路由、layout、掛載點、空狀態、403／導登入 |
| S06-03 | `AdvisorQuery`、事件型別登錄、06／07 no-op stub | S06-01、S02-03、S02-08 | `advisorOf`／`groupsOf`／`unassignedIndustryGroups`、event-catalog 條目、stub 接點 |
| S06-04 | 老師認領產學組（並發只一人成功） | S06-02、S06-03、S01-03、S02-02 | `claimGroup`、`ALREADY_CLAIMED` 映射、認領通知 |
| S06-05 | 管理員逐組指派與解除 | S06-04 | `assignAdvisor`／`unassignAdvisor`、指派對話框 |
| S06-06 | 管理員重派（對話框評分清單為空並註明；同交易呼叫 07／06 stub） | S06-05 | `reassignAdvisor`、重派對話框、原老師通知 |
| S06-07 | CSV 批次指派：六類預覽與逐列執行 | S06-06 | `batchAssign`、預覽表、逐列結果 |
| S06-08 | 合作案建立／編輯／發布／下架／重新發布；登入者列表與詳情；私有欄位 | S06-02、S01-03、S02-02 | `OpportunityCommand.create…republish`、`/industry` 頁 |
| S06-09 | 組長連結、換案；案主與管理員解除；通知 | S06-08、S06-03 | `link`／`relink`／`unlink`、連結歷史 |
| S06-10 | 組別類型變更（組長三條件；衝突警告交管理員） | S06-09、S02-04 | `changeType` |
| S06-11 | 屆別名單篩選、排序與 CSV／XLSX 匯出 | S06-05、S06-09 | 管理員匯出 Route Handler |

## 票

### S06-01 增量 migration：指導與產學三表
- **目標**：空庫逐切片升級後 `advisor_assignments`、`industry_opportunities`、`opportunity_links` 三表存在，欄位、CHECK、FK RESTRICT、兩條部分唯一（`advisor_assignments(group_id) WHERE valid_to IS NULL`、`opportunity_links(group_id) WHERE valid_to IS NULL`）與契約一致；GRANT 依逐表矩陣；`roles.test.ts` 對三表逐格證明。
- **spec**：模組 03 附錄 A（`advisor_assignments`、`industry_opportunities`、`opportunity_links`）；契約 01 §3「03」、§5（`advisor_assignments` S I U(`valid_to`, `reason`)；`industry_opportunities` S I U；`opportunity_links` S I U(`valid_to`, `ended_by_user_id`, `reason`)）、§12（每支 migration 的 FK 目標必須已存在）；案例 GRP-10、GRP-15（追溯：唯一約束）。
- **範圍**：純 SQL migration（增量，不重建 S03 的 `groups` 等表）、索引另檔、GRANT 產生器矩陣列、`roles.test.ts` 擴充、不可變欄位以欄級 UPDATE 限制。**非範圍**：任何用例與頁面（S06-03 起）；模組 03 其餘表（S03）。
- **前置**：S00-04、S00-05、E03 出場。
- **路徑**：空庫升級成功／重跑 no-op；`fju_app` 對 `advisor_assignments` 更新 `teacher_user_id` 被拒；同組插入第二筆有效主指導或第二筆有效連結被唯一鍵拒。
- **自動測試**：空庫與前版升級 migration（S00-03 harness）；逐表逐欄存在性；`roles.test.ts` 三表矩陣；部分唯一違反整合（兩筆 `valid_to IS NULL` 同 `group_id`）。
- **操作證據**：migration 輸出、`\d` 三表摘要（存 `steps/S06/S06-01/`）。
- **完成定義**：票上附模組 03 附錄 A 三表逐欄核對表；roles 測試綠；Fable review。
- **待 Roy／平台**：無。

### S06-02 殼：老師組別頁、產學頁、合作案管理頁與管理員組別頁掛載點
- **目標**：`/dashboard/teacher/groups`（我指導的組別、可認領產學組兩區塊掛載點）、`/industry` 與 `/industry/[id]`（登入內容；訪客導 `/login?returnTo=`）、`/dashboard/teacher/industry`（我的合作案殼）、`/dashboard/admin/industry`（全部合作案殼）存在並掛在導覽；`/dashboard/admin/groups` 每組列有「指派／重派／解除」與「批次指派 CSV」按鈕掛載點（此票只掛載，不做動作）；學生開老師與管理員路由 403 帶下一步；每頁有空狀態文案。
- **spec**：模組 03 §7 路由表（`/dashboard/teacher/groups`、`/dashboard/admin/groups`、`/industry`、`/dashboard/teacher/industry`）與 §7.1「產學合作」組（管理員 `/dashboard/admin/industry` 原型）；契約 03 §1（登入內容：active 使用者；產學私有欄位：案主、管理員）；契約 02 §4（`UNAUTHENTICATED` 導 `/login?returnTo=`）；S01-04 殼約定；劇本 P04 §7（訪客直接開 `/industry` 導登入）；案例 GRP-08（追溯：訪客邊界）。
- **範圍**：路由、layout、導覽項、掛載點、授權邊界（未登入導登入、越權 403、空狀態）。**非範圍**：任何資料寫入與清單內容（S06-04 起）；`/dashboard/admin/groups` 頁本體（S03）。
- **前置**：S01-04、E03 出場。
- **路徑**：訪客→導登入；學生→403（老師／管理員路由）；老師→200 空狀態；管理員→200；pending／must-change 依契約 03 §2 受限。
- **自動測試**：路由授權矩陣整合（四身分 × 五路由）；空狀態渲染單元。
- **操作證據**：四身分截圖（含 403 下一步文案）（`steps/S06/S06-02/`）。
- **完成定義**：路由與導覽項與模組 03 §7 一致；授權矩陣測試綠；Fable review。
- **待 Roy／平台**：無。

### S06-03 `AdvisorQuery`、事件型別登錄、06／07 no-op stub
- **目標**：`AdvisorQuery.advisorOf(groupId)`／`groupsOf(teacherId)`／`unassignedIndustryGroups(cohortId)` 只回有效（`valid_to IS NULL`、組 active）關係；`application/notifications/event-catalog.ts` 登錄 advisor.claimed／advisor.assigned／advisor.reassigned／advisor.unassigned／opportunity.linked／opportunity.relinked／opportunity.unlinked 的 recipients 規則；07 `SignoffCommand.supersedeForParticipantChange(tx, groupId, cause)` 與 06 `GradingCommand.applyAdvisorReassignment(tx, groupId, selections[])` 以 no-op stub 進 composition，測試標記 `pending S11`／`pending S10`。
- **spec**：模組 03 §5（`AdvisorQuery`；依賴 06、07 command）；模組 08 §4（event-catalog 對照產品模組 08 §4 矩陣：「指導指派／認領：該組全員與新老師；重派另通知原老師」「合作案解除／換案：受影響的組員與案主」）、§10（recipients 計算、去重鍵單元）；S06 卡「責任範圍」（S11 前 no-op stub 並以測試標記 `pending S11`；06 同理）；案例 GRP-09、GRP-19（追溯）。
- **範圍**：query 實作、catalog 條目、兩個 stub 與 composition 接線、recipients 純函式。**非範圍**：真實 supersede（S11）、真實評分連動（S10）；任何 command（S06-04 起）。
- **前置**：S06-01、S02-03、S02-08。
- **路徑**：`groupsOf` 對已解散組不回；`unassignedIndustryGroups` 排除已有有效指派與非 active 組；stub 被呼叫時只記錄不改任何表。
- **自動測試**：三個 query 的整合（有效／已結束／已解散三種列）；recipients 計算單元（全組 ∪ 新老師去重；換案＝該組全員 ∪ 兩案主）；stub spy 單元標 `pending S10`／`pending S11`。
- **操作證據**：測試輸出；catalog 條目清單（`steps/S06/S06-03/`）。
- **完成定義**：catalog 條目與產品模組 08 §4 逐列對照寫進票；測試綠；Fable review。
- **待 Roy／平台**：無。

### S06-04 老師認領未指派產學組
- **目標**：T2 在 `/dashboard/teacher/groups` 對未指派產學組 G2 按認領→成功回執、`advisor_assignments` 一列 `source='claim'`、G2 全員與 T2 收到通知；T3 再認領同組得 `ALREADY_CLAIMED`；兩位老師以同步屏障並發只一人成功；一般組不列於可認領且直接請求 `FORBIDDEN`。
- **spec**：模組 03 §3「主指導｜認領」（產學組且無有效指派；部分唯一→`ALREADY_CLAIMED`）、§6（認領：部分唯一）、§7（`/dashboard/teacher/groups` 成功／`ALREADY_CLAIMED` 文案／查回執）、§10（兩老師認領整合）；母 spec 附錄 B §5.3「兩老師同時認領」；契約 04 §4（精確並行由同步屏障證明）；劇本 P04 步驟 11–12、§7（T3 直接請求 G2 指派變更 `FORBIDDEN`）、B02 步驟 9（`advisor.claim.concurrency.test`）；案例 GRP-09、GRP-10、GRP-07 的老師搶占拒絕（追溯）。
- **範圍**：`GroupCommand.claimGroup(teacher)` 用例（`groups` `FOR SHARE`＋屆別 active 檢查、insert 主指導列、`OperationLedger` requestId、audit、事件 advisor.claimed）、可認領清單、認領對話框、成功／衝突畫面、查回執。**非範圍**：指派／重派（S06-05／06）；評分與簽核連動（S10／S11）。
- **前置**：S06-02、S06-03、S01-03、S02-02。
- **路徑**：正常→回執含 requestId 與 serverTime；拒絕→`ALREADY_CLAIMED`（唯一違反映射；文案「剛由另一位老師認領成功，需請系辦重派」）、一般組／非 active 組／已有指派 `FORBIDDEN`、學生或管理員呼叫 `FORBIDDEN`、屆別封存 `COHORT_ARCHIVED`；重試→同 requestId 回原回執，回應遺失查 `getOperationResult`。
- **自動測試**：整合 `advisor.claim.concurrency.test`（同步屏障：兩交易同組，一成功一 `ALREADY_CLAIMED`，表只一列有效）；一般組 `FORBIDDEN`；同 requestId 重播回原回執；學生直接請求；事件 recipients＝G2 有效成員 ∪ T2；audit 列；重登入後老師頁仍列 G2（保存）。
- **操作證據**：認領回執 JSON、先後認領的 `ALREADY_CLAIMED` 回應、通知匣截圖、`SELECT teacher_user_id, source, valid_to FROM advisor_assignments WHERE group_id=<G2>`（`steps/S06/S06-04/`）。
- **完成定義**：並發測試綠（切片出場明列）；四欄證據齊；Fable review。
- **待 Roy／平台**：無。

### S06-05 管理員逐組指派與解除主指導
- **目標**：A1 在 `/dashboard/admin/groups` 對一般組 G1 選 T1 並填理由→指派成功、`source='admin'`、G1 全員與 T1 收到通知、學生首頁顯示同一老師；A1 對有主指導的組按解除（理由）→有效列設 `valid_to`、通知；老師直接請求指派變更 `FORBIDDEN`；舊 revision `CONFLICT`。
- **spec**：模組 03 §3「主指導｜指派／重派／解除：管理員（理由）」、§5（`assignAdvisor`、`unassignAdvisor`）、§7（`/dashboard/admin/groups` 指派回執、查回執）、§9（指派歷史可查）；契約 01 §5（`advisor_assignments` 只能更新 `valid_to`、`reason`）；產品模組 08 §4「指導指派／認領：該組全員與新老師」；劇本 P04 步驟 13、§7（T3 直接請求 `FORBIDDEN`）；案例 GRP-07（追溯）。
- **範圍**：兩個用例（`groups` `FOR UPDATE`＋revision、理由必填、老師須為 active 老師角色）、指派對話框、解除確認、audit、事件 advisor.assigned／advisor.unassigned。**非範圍**：重派（S06-06）；批次（S06-07）。
- **前置**：S06-04。
- **路徑**：正常→回執；拒絕→理由空或老師非 active 老師 `VALIDATION_FAILED`、已有有效主指導時 assign 回 `CONFLICT` 並提示改用重派（錯誤碼待 spec 補，見問題清單）、非管理員 `FORBIDDEN`、舊 revision `CONFLICT`；重試→同 requestId 回原回執。
- **自動測試**：指派 audit 含理由與操作者；revision 過期 `CONFLICT`；解除後 `groupsOf(T1)` 不含 G1、`advisorOf(G1)` 為空；學生查詢看到同一老師；T3 直接請求 `FORBIDDEN`；重載／重登仍保存；解除事件 recipients（暫依本稿：該組全員與原老師，待 Roy 定）。
- **操作證據**：指派回執、audit 列、學生首頁老師名截圖、解除後 SQL（`steps/S06/S06-05/`）。
- **完成定義**：P04 步驟 13 可走；測試綠；Fable review。
- **待 Roy／平台**：解除指派的通知收件人（產品模組 08 §4 矩陣無此列）由 Roy 定，時點在本票 review。

### S06-06 管理員重派主指導（對話框評分清單為空並註明）
- **目標**：A1 對 G3（已指派 T2）按重派選 T3 填理由→對話框列「原老師在本組的評分指派」（本切片評分尚未存在，清單為空並顯示「評分功能尚未交付，無指派可勾選」）→執行後 `advisor_assignments` G3 兩列（T2 有 `valid_to`、T3 有效）、同交易呼叫 07 `supersedeForParticipantChange` 與 06 `applyAdvisorReassignment`（stub）、G3 全員與 T3 通知、T2 另收原老師通知；同一老師重派為無實際變更，不產生事件。
- **spec**：模組 03 §2（主指導改變→07 supersede；重派不自動動評分指派）、§3「指派／重派／解除：重派對話框列評分指派預設不勾；勾選者呼叫 06；07 supersede」、§5（`reassignAdvisor(groupId, revision, teacherId, reason, gradingSelections[])`）、§6（重派：`groups` `FOR UPDATE`→呼叫 07／06（同 tx）→事件 recipients 聯集＋主指導）、§8（重派序列）；母 spec §4.8（同交易編排）；切片總圖「S06 的重派對話框在評分不存在時列空清單」；產品模組 08 §4「重派另通知原老師；沒有實際變更或只是重試不重複通知」；劇本 P04 步驟 14；案例 GRP-11 子步驟（最終 S09）、GRP-07（追溯）。
- **範圍**：用例（同交易；`gradingSelections` 此時必為空陣列）、重派對話框（清單區塊、註明文案、理由必填）、事件 advisor.reassigned 兩種收件人、audit。**非範圍**：真實評分清單與逐筆採計（S10 回補對話框）、真實簽核失效（S11）、預覽期間 `CONFLICT`（S09／S10）。
- **前置**：S06-05。
- **路徑**：正常→回執；拒絕→理由空 `VALIDATION_FAILED`、舊 revision `CONFLICT`、非管理員 `FORBIDDEN`、無有效主指導的組回 `VALIDATION_FAILED`（應走指派）；同老師→回執但無事件、無新列；stub 失敗注入→整體回滾（無新列、無事件）。
- **自動測試**：整合：兩列狀態（舊 `valid_to`、新有效）；stub spy 被呼叫且標 `pending S10`／`pending S11`；stub 注入失敗整體回滾；事件 recipients（全組 ∪ T3）與原老師 T2 各一則；同老師無事件；revision `CONFLICT`；重登入後老師頁 T2 不含 G3、T3 含 G3。
- **操作證據**：重派對話框截圖（清單為空並註明）、回執、`advisor_assignments` G3 兩列 SQL（P04 §6）、三方通知匣（`steps/S06/S06-06/`）。
- **完成定義**：P04 步驟 14 可走；同交易測試綠；Fable review。
- **待 Roy／平台**：無。

### S06-07 CSV 批次指派：六類預覽與逐列執行
- **目標**：A1 先選屆別，上傳 UTF-8 CSV（固定欄 `group_code,teacher_login_email`）→預覽逐列分類（可新增指派／已是同一老師不需變更／已有不同老師屬於重派／組別不存在或未成立或已解散／老師不存在或停用（含用聯絡 Email 而非登入 Email）／同一組在檔案中重複），每列帶 `groups.revision`；有錯誤列不能執行；重派列需明確確認並填理由；執行時每列一交易、revision 不符的列回 `CONFLICT` 要求重新預覽；逐列結果表；失敗列可重跑；通知只發真正變更的組與老師。
- **spec**：模組 03 §3「批次指派｜CSV：先選屆別；六類預覽；每列帶 `groups.revision`；revision 不符→該列 `CONFLICT`」、§5（`batchAssign(cohortId, rows[])`）、§6（批次：每列一交易；預覽 revision 比對）、§10（CSV 六類單元；批次 revision 整合）；產品模組 03 §5.4「行政指派的輸入（Q-GRP04）」；契約 03 §5（匯入：UTF-8、固定欄位、每列長度上限）；S06 卡「恢復：批次逐列結果，失敗列可重跑」；劇本 B02 步驟 14；案例 GRP-17（追溯）。
- **範圍**：CSV 解析與驗證、預覽分類純函式、預覽頁（分類表、錯誤列標示、重派確認與理由）、執行用例（沿用 S06-05／06 的單組用例逐列呼叫）、結果表、重跑、批次 requestId。**非範圍**：CSV 原檔保存（`stored_files.purpose` 無對應值，見問題清單）；老師登入 Email 以外的辨識方式。
- **前置**：S06-06。
- **路徑**：正常→逐列 ok；拒絕→含錯誤列直接送執行 `VALIDATION_FAILED`、屆別未選 `VALIDATION_FAILED`、非管理員 `FORBIDDEN`、單列 `CONFLICT`（預覽後另一管理員改了該組）；重試→同批次 requestId 回原逐列結果，不重複通知；失敗列以新預覽重跑。
- **自動測試**：單元：六類分類（含聯絡 Email 誤用、重複列、已解散組、停用老師）；整合：預覽後改動一組→該列 `CONFLICT` 其他列成功；第 3 列失敗不影響第 2 列（每列一交易）；成功筆數＝audit 筆數；通知只發變更列、重跑不重複；學生直接請求 `FORBIDDEN`。
- **操作證據**：六類預覽截圖、逐列結果、`CONFLICT` 列回應、audit 與通知清單（`steps/S06/S06-07/`）。
- **完成定義**：B02 步驟 14 可走；分類與整合測試綠；Fable review。
- **待 Roy／平台**：批次六類的定義（產品 §5.4 與劇本 B02 步驟 14 不一致，見問題清單）由 Roy 定，時點在本票 review；本稿依產品 §5.4。

### S06-08 合作案建立、發布、下架、重新發布與登入者頁
- **目標**：T2 在 `/dashboard/teacher/industry` 新增 C1（公司、部門、內容、條件、備註；私有欄地址／聯絡人／電話／Email 標「只有你與系辦看得到」）→發布後登入學生在 `/industry` 與 `/industry/[id]` 看到公開欄、聯絡資訊只有負責老師姓名與系辦；訪客導登入；T3 建 C2 草稿只有 T3 與 A1 看得到；案主下架後列表不顯示、重新發布恢復；A1 在 `/dashboard/admin/industry` 管理全部案；學生直接請求私有欄位得 DTO 無該欄、直接 action `FORBIDDEN`。
- **spec**：模組 03 §3「合作案｜draft→published→withdrawn→published：案主／管理員；下架不收新連結」、§5（`OpportunityCommand.create／update／publish／withdraw／republish`）、§7（`/industry`、`/dashboard/teacher/industry`：移除「網際網路公開」、下架啟用）、附錄 A `industry_opportunities`（`notes_visibility`、私有四欄只案主與管理員）；契約 03 §1（登入內容；產學私有欄位：案主、管理員）、§4（最小揭露）；契約 02 §8（私有查詢不快取）；產品模組 03 §6.1（登入後內容）；劇本 P04 步驟 9–10、§7（S07 直接請求 C1 私有欄）；案例 GRP-08、GRP-15 的下架與重新發布部分（追溯）。
- **範圍**：五個用例（案主或管理員；revision；requestId；audit）、三頁（登入者列表可篩「尚未指派／已有組別」、詳情、案主管理頁、管理員全部案頁）、DTO 白名單（學生／非案主老師的 DTO 不含私有欄）、`notes_visibility` 兩值的顯示規則。**非範圍**：連結／換案／解除（S06-09）；「已下架」對既有連結的顯示（S06-09）。
- **前置**：S06-02、S01-03、S02-02。
- **路徑**：正常→建立／發布／下架／重新發布回執；拒絕→非案主且非管理員 `FORBIDDEN`、訪客 `UNAUTHENTICATED` 導登入、必填欄空 `VALIDATION_FAILED`、舊 revision `CONFLICT`；重試→同 requestId 回原回執。
- **自動測試**：狀態機單元（draft→published→withdrawn→published；draft 直接下架拒）；整合：非案主 update `FORBIDDEN`；學生 JSON／HTML 掃描無 `address`／`contact_*`；草稿只案主與管理員可讀；訪客 401；revision `CONFLICT`；重載／重登仍保存。
- **操作證據**：案主視角 vs 學生視角截圖對照（私有欄）、訪客導登入截圖、C2 草稿三身分回應、publications 狀態 SQL（`steps/S06/S06-08/`）。
- **完成定義**：P04 步驟 9–10 可走；白名單掃描綠；Fable review。
- **待 Roy／平台**：`notes_visibility='internal'` 的可見範圍（附錄 A 未定義）由 Roy 定，時點在本票 review；本稿暫依「只案主與管理員」。

### S06-09 組長連結、換案；案主與管理員解除；通知
- **目標**：G2 組長 S06 把已成立產學組 G2 連結已發布的 C1→成功、一組一案；非組長 S07 連結 `FORBIDDEN`；連結未發布的 C2 得 `OPPORTUNITY_NOT_LINKABLE`；一般組 G1 連結 `FORBIDDEN`；一案可多組；S06 換案到 C2→舊列結束、新列有效並保留原因，通知 G2 全員與 C1、C2 案主；T2 解除某組與 C1 的連結需理由並通知該組；下架 C1 後既有連結顯示「合作案已下架」、不收新連結；重新發布不恢復已解除關係；連結不使案主成為主指導。
- **spec**：模組 03 §2（最多一個有效合作案連結）、§3「連結｜建立／換案／解除：組長或管理員；案主與管理員解除（理由）；已成立產學組、案已發布；`OPPORTUNITY_NOT_LINKABLE`」、§5（`link`、`relink`、`unlink(reason)`）、§9（連結歷史可查）、§10（連結基數、下架不收新連結）、附錄 A `opportunity_links`；契約 01 §5（只更新 `valid_to`、`ended_by_user_id`、`reason`）；產品模組 03 §6「連結」段、§4 矩陣「合作案解除／換案：受影響的組員與案主」；劇本 P04 步驟 15–16、B02 步驟 15、17、18、§7（非組長 S07 換案 `FORBIDDEN`）；案例 GRP-15、GRP-19（追溯）。
- **範圍**：三個用例（`groups` `FOR SHARE`、組長或管理員、組別 active 且產學、案 published、部分唯一）、詳情頁的連結／換案／解除按鈕、連結歷史區塊、「合作案已下架」標示、事件 opportunity.linked／relinked／unlinked。**非範圍**：合作案本身狀態（S06-08）；類型變更引起的關聯處理（S06-10）。
- **前置**：S06-08、S06-03。
- **路徑**：正常→連結／換案／解除回執；拒絕→非組長非管理員 `FORBIDDEN`、一般組 `FORBIDDEN`、案為 draft 或 withdrawn `OPPORTUNITY_NOT_LINKABLE`、已有有效連結再 link 回 `CONFLICT` 提示用換案（錯誤碼待 spec 補）、解除理由空 `VALIDATION_FAILED`、非案主非管理員解除 `FORBIDDEN`、舊 revision `CONFLICT`；重試→同 requestId 回原回執。
- **自動測試**：整合：連結基數（同組第二筆有效連結被唯一鍵拒；一案兩組成功）；下架後 link `OPPORTUNITY_NOT_LINKABLE`、既有連結仍有效且標示；重新發布後已解除列仍為結束；換案歷史兩列與原因；事件 recipients（換案：G2 有效成員 ∪ T2 ∪ T3；解除：該組成員 ∪ 案主）；`advisorOf(G2)` 不因連結改變；非組長；重載／重登仍保存。
- **操作證據**：連結回執、兩個拒絕回應、換案前後連結歷史、下架後 G2 所見截圖、通知匣三方（`steps/S06/S06-09/`）。
- **完成定義**：P04 步驟 15–16 與 B02 步驟 15、17、18 可走；基數與下架測試綠；Fable review。
- **待 Roy／平台**：無。

### S06-10 組別類型變更：組長三條件與衝突警告
- **目標**：組長在成組期內、組無有效主指導、無有效合作案連結時可自行改組別類型（全組可見、留 audit）；三條件任一不符時組長得 `FORBIDDEN` 並看到警告「與指導／合作案關聯衝突，請交由系辦處理」，關係不變；管理員對有關聯的組改類型時必須先處理關聯（解除連結或解除主指導）才可改，不靜默刪關係。
- **spec**：模組 03 §3「類型｜組長改：成組期內、無主指導、無合作案；否則管理員改並處理關聯；`FORBIDDEN`」、§5（`changeType`）、§10（類型三條件單元）；產品模組 03 §5「組長可自行修改組別類型的條件（2026-09-12 定案）」；模組 02 §5（`StageQuery.currentStage` 判定成組期）；劇本 B02 步驟 11；案例 GRP-14（追溯）。
- **範圍**：用例（`groups` `FOR UPDATE`＋revision；三條件；管理員路徑的關聯檢查）、學生組別頁類型變更入口與警告、管理員組別詳情類型變更（有關聯時列出關聯並拒絕）。**非範圍**：S03 卡未列 `changeType`，本票視為 S06 新做（見問題清單）；例外組人數規則（S03）。
- **前置**：S06-09、S02-04。
- **路徑**：正常→回執、audit、全組可見；拒絕→組長三條件不符 `FORBIDDEN`（details 列出衝突項）、管理員在關聯存在時 `VALIDATION_FAILED`（details 列出關聯）、非組長學生 `FORBIDDEN`、舊 revision `CONFLICT`。
- **自動測試**：單元：三條件組合；整合：有連結時組長 `FORBIDDEN` 且 `opportunity_links` 不變；成組期外 `FORBIDDEN`；管理員關聯存在拒絕、解除後成功；audit 列；重載保存。
- **操作證據**：組長警告截圖、管理員拒絕與解除後成功回執、前後關係 SQL（`steps/S06/S06-10/`）。
- **完成定義**：B02 步驟 11 可走；測試綠；Fable review。
- **待 Roy／平台**：「管理員改並處理關聯」的具體動作（見問題清單）由 Roy 定，時點在本票 review；本稿暫依「關聯存在即拒，先解除再改」。

### S06-11 屆別名單篩選、排序與 CSV／XLSX 匯出
- **目標**：A1 在 `/dashboard/admin/groups` 依屆別／類型／老師／狀態篩選、排序、勾選部分或全選→下載 CSV 與 XLSX，檔案含組別代碼、類型、成員學號與姓名、組長、主指導、合作案；筆數與畫面範圍一致；學號為文字（XLSX 文字儲存格；CSV 公式字首前綴）；只有本屆不混舊屆；非管理員 403；每次下載重新授權並留 audit。
- **spec**：模組 03 §9（匯出只管理員）；產品模組 03 §5.5（可搜尋、排序、依屆別／類型／老師／狀態篩選；勾選部分或全選下載 CSV／Excel）；契約 03 §5（匯出：XLSX 文字儲存格；CSV `= + - @ \t \r` 字首加 `'` 並雙引號包裹）；契約 02 §2（匯出走 Route Handler，每次授權）；劇本 P04 步驟 17；案例 GRP-16（追溯）。
- **範圍**：篩選與排序（server 端）、勾選狀態、匯出 Route Handler（串流、`attachment`、`nosniff`）、兩種格式、audit。**非範圍**：匯出檔保存為 `stored_files`（模組 03 §5 未列 port，見問題清單）；帳號匯出（S01-16）。
- **前置**：S06-05、S06-09。
- **路徑**：正常→兩種檔案；拒絕→非管理員 403、訪客 401、篩選值非法 `VALIDATION_FAILED`。
- **自動測試**：公式字首、分隔符、換行、引號單元（契約 03 §5）；XLSX 存檔重開學號為文字；整合：筆數＝篩選結果；跨屆不混；老師與學生請求 403；audit 列。
- **操作證據**：表格截圖、兩種匯出檔與 `sha256sum`、拒絕回應（`steps/S06/S06-11/`）。
- **完成定義**：P04 步驟 17 可走；匯出安全測試綠；Fable review。
- **待 Roy／平台**：匯出 port 名稱與是否保存原檔（見問題清單）由 Roy 定，時點在本票 review；不擋本票以 Route Handler 直接呼叫用例的做法。

## 紙上依賴核對
- S06-01：資料表←自身（`groups` 由 E03）；port／工具←S00-03、S00-05；頁面入口←無；劇本步驟可走：不適用。
- S06-02：資料表←S06-01 不需（只殼）；port／工具←S01-04；頁面入口←自身；劇本步驟可走：P04 §7 訪客導登入（是）。
- S06-03：資料表←S06-01；port／工具←S02-03（EventPublisher）、S02-08（渲染重驗框架）；頁面入口←無；劇本步驟可走：否（只 port）。
- S06-04：資料表←S06-01；port／工具←S06-03、S01-03（ledger、audit、ActorResolver）、S02-02（業務鐘）；頁面入口←S06-02；劇本步驟可走：P04 步驟 11–12（是）、B02 步驟 9（是，含整合測試）。
- S06-05：資料表←S06-01；port／工具←S06-03、S06-04 的用例基底；頁面入口←S06-02（管理員組別頁按鈕掛載點）；劇本步驟可走：P04 步驟 13（是）。
- S06-06：資料表←S06-01；port／工具←S06-03 stub、S06-05；頁面入口←S06-02；劇本步驟可走：P04 步驟 14（是，評分清單空）。
- S06-07：資料表←S06-01；port／工具←S06-05、S06-06；頁面入口←S06-02；劇本步驟可走：B02 步驟 14（是）。
- S06-08：資料表←S06-01；port／工具←S01-03、S02-02；頁面入口←S06-02；劇本步驟可走：P04 步驟 9–10（是）。
- S06-09：資料表←S06-01；port／工具←S06-03（recipients、`advisorOf`）、S06-08；頁面入口←S06-02、S06-08 詳情頁；劇本步驟可走：P04 步驟 15–16（是）、B02 步驟 15、17、18（是；步驟 16 用 S06-08）。
- S06-10：資料表←S06-01（連結）、E03（`groups`）；port／工具←S02-04（`StageQuery`）、S06-09；頁面入口←E03 學生組別頁、S06-02；劇本步驟可走：B02 步驟 11（是）。
- S06-11：資料表←S06-01、E03；port／工具←S06-05、S06-09；頁面入口←S06-02；劇本步驟可走：P04 步驟 17（是）。
無循環；每票的前置編號都更小或為 S00–S02 票號／`E03 出場`。

## 讀 spec 時發現的問題（不改 spec，只列）
- 票號對照：簡報的 S00–S02 票號表與 `🧱 實作切片/02 實作票草稿 第一批 S00–S02.md`（v0.1）的編號不同（例如第一批 S01-06 最小檔案能力／S01-09 核准／S02-05 EventPublisher／S02-06 通知匣；簡報為 S01-07／S01-10／S02-03／S02-08）。本稿依簡報；正式開票前需統一一份。
- 模組 03 §5 沒有名單匯出的 port 或 Route Handler；§9 只寫「匯出只管理員」。產品 §5.5 為 CSV／Excel，P04 步驟 17 只寫 CSV。GRP-16 責任在 S06，需補 port 名與檔案是否保存為 `stored_files(purpose='export')`。
- 批次六類定義三處不一致：產品 §5.4（可新增／同一老師／不同老師屬重派／組不存在或未成立或已解散／老師不存在或停用／重複列）、模組 03 §3 只寫「六類預覽」、劇本 B02 步驟 14（正常、組不存在、老師不存在、已指派、重複列、revision 過期——把執行期的 `CONFLICT` 當成預覽分類）。
- 批次 CSV 原檔：`stored_files.purpose` 列舉（模組 10 附錄 A）沒有批次指派 CSV 的用途值；是否要像名單 CSV 一樣保存原檔未定。
- 產品模組 08 §4 事件矩陣沒有「解除指派」列；S06 卡「通知：指派、認領、重派、解除、換案」多了解除。收件人待定。
- 模組 03 §7 路由表只列 `/industry`、`/dashboard/teacher/industry`，但 §7.1 有管理員 `/dashboard/admin/industry` 原型且產品 §6.1 寫「管理員可管理全部」；管理員入口路由未定。
- 老師 `/dashboard/teacher/groups` 原型有「下方全部組別表可搜尋篩選」；契約 03 §1 授權矩陣沒有老師對非指導組別（成員名單）的讀取列。老師能否看全部組別與成員未定；本稿殼票只掛「我指導的組別」與「可認領產學組」。
- `changeType`（GRP-14）：S03 卡與 S06 卡的 API 清單都沒列，但 GRP-14 最終責任 S06；模組 03 §3「否則管理員改並處理關聯」的具體動作（解除連結？解除主指導？自動或手動？）未定；B02 步驟 11 只寫「管理員路徑處理關聯後改」。
- 指派對已有有效主指導的組（應走重派）與已有有效連結再 link（應走換案）的錯誤碼，模組 03 §3 與契約 02 §1 都沒定；本稿暫用 `CONFLICT`。
- 認領通知：產品矩陣「該組全員與新老師」，P04 步驟 11「G2 全員通知」，原型文案「已通知全組與系辦」；管理員是否收認領通知未定。
- `industry_opportunities.notes_visibility` 的 `'internal'` 值可見範圍附錄 A 未定義（只寫 signed_in／internal）。
- `advisor_assignments.valid_from` 用真實時間或業務時間，附錄 A 未標（其他有效區間表同）。
