---
type: ticket-decisions
project: FJU IM Project
updated: 2026-09-13
status: draft-v0.3-pending-codex-review
---
# 票草稿 v0.3｜修訂差異、決策表、依賴圖與行為覆蓋表（2026-09-13）

> 回覆 Codex 對 158 張票草稿（`1233665`）的集中 review F01–F15。只改受影響的票與四處 spec 局部回寫；不重產整套、不按票數配額重拆。逐字差異見 PR #7 的 commit（`git diff 1233665...<本輪 head> -- "docs/engineering/slices/🎫 票草稿" docs/engineering/contracts docs/engineering/modules docs/ARCHITECTURE.md`）；本文是四項交付：§1 每項修訂位置、§2 決策表（三類）、§3 有效依賴圖、§4 行為子項覆蓋表。仍不開票、不合併、不開工；145 NOT_RUN、4 DEFERRED。

## 1. F01–F15 的修訂位置與新決策依賴

| F | 類型 | 修訂位置 | 新決策依賴 |
|---|---|---|---|
| F01 組長自行換組長 | 已定需求直接修 | S03-09（原 S03-08）：只剩管理員路徑，學生（含現任組長）直接請求 `FORBIDDEN`；spec 欄註明模組 03 §3「類型｜組長改」是改類型不是換組長；移除三條件與學生路徑 | 無 |
| F02 未交付 handler 提前 failed／done | 工程可決（E-02～E-04）＋spec 局部回寫 | 模組 08 §6 v2.4 新增「到期工作生命週期表」（每 kind 的排程者、首筆、週期、identity、handler 供應切片、停用／重試）與「白名單內未註冊→pending 不累計、`defer`→pending 不累計、例外才累計」；契約 01 §4.7 v2.5 同步；S02-07 目標與測試改寫並加跨切片測試；S04-05、S05-05、S08-06、S08-11、S12-12 對齊（首筆排程者：`overdue_digest` 由 `deadline_snapshot` handler 排、`file_gc`／`receipt_purge` 由 S12 插首筆） | 無 |
| F03 S04 建表缺 S03 schema 前置 | 工程可決（E-12） | S04-01 前置加 S03-01（只依賴 migration 票，不等 E03 出場）；總圖 v2.4 註明 schema 前置與 migration 順序；核對腳本改以總圖切片依賴＋schema 例外判斷 | 無 |
| F04 提案期限缺設定票 | 已定需求＋工程可決（E-13）＋spec 回寫 | 新增 S03-04「本屆提案預設天數設定」；S03 其餘票重新編號 04–08→05–09；模組 02 v2.4 附錄 A 加 `cohorts.proposal_default_days`、§5 加 `setProposalDefaultDays`；S03-05（原 S03-04）移除環境變數 | D-10（成組截止推導確認，不阻塞） |
| F05 指定重開的去重鍵互撞 | 工程可決（E-01）＋spec 回寫 | 契約 01 §4.7 v2.5：global `(item, deadline_version)`、targeted `(targeted_reopens.id, 1)`；模組 08 附錄 A `digest_events.subject_id` 理由同步；S08-07、S08-11 目標與測試改寫（兩收件者同版重開各到期一次、互不吞通知） | 無 |
| F06 評分老師讀材料無票 | 已定需求直接修（E-18 放 S10-03） | S10-03 範圍加 policy 分支接 S07-06／S07-07；S10-04 工作台加「檢視正式繳交」入口；測試：非主指導評分老師可讀被指派正式版本與附件、不能讀草稿／他組、解除指派即拒 | 無 |
| F07 新成員補通知 | 已定需求直接修 | S09-01 範圍加同交易呼叫 05 `syncRosterForMember`（E-07）為新成員發「新收件」，舊成員與被移出者不收；測試補 | 無 |
| F08 評分並行測試預期相反 | 已定需求直接修 | S10-08 自動測試改寫：初始 counted c1；先 submit→`CONFLICT`、c1 仍可退回；先 return→c1 returned、submit 建 c2 | 無 |
| F09 快照時刻早於允許 | 已定需求直接修 | S08-05 路徑改 00:01:00 到期；晚 commit 測試明寫請求在 23:59:59 已取樣、00:00:00 取樣仍 `DEADLINE_PASSED` | 無 |
| F10 異動後 complete 簽核仍有效 | 已定需求直接修 | S11-08 範圍：不論 state（含 complete）→superseded，內容與 approvals 歷史保留、`current_version_id` 不變、`isValid=false`；測試補 complete→發布→異動→停 worker 直接請求公開端點皆拒（E12 後補跑）；通知拆到 S11-13 | D-11（通知收件人） |
| F11 同版重開與不可變唯一投票衝突 | Roy 產品決策 | S11-07 移除 reopen；新增 S11-12 [BLOCKED D-01]，推薦沿新版本模型；不刪 approvals、不解除唯一鍵 | D-01 |
| F12 reset 清帳號與演練紀錄 FK 互斥 | Roy 產品決策（資料保存）＋工程可決（E-11） | S14-11 [BLOCKED D-09]（含目標環境 guard）；S14-05 `AGE_RECIPIENTS` 空→啟動前置檢查失敗、不寫 `backup_runs` | D-09 |
| F13 首次部署前置與健康條件 | 工程可決（E-19） | S14-02 只驗啟動前可做的檢查；S14-03 依部署階段分「有限健康條件」（E02 前四項、worker 欄 null）與完整判定（`--expect-worker`） | 無 |
| F14 案例 ID 涵蓋但行為缺票 | 已定需求＋工程可決（E-16、E-17） | 新增 S12-16 公開搜尋頁（模組 09 v2.4 `PublicContentQuery.search`）、S11-11 提醒未同意者（模組 07 v2.4 `remind`）；登入產學列表由 S06-08 供應（§4 覆蓋表）；S12-08 非範圍改指向 S12-16 | 無 |
| F15 未決模型寫成暫定實作 | Roy 產品決策＋工程可決（E-05） | 模組 10 v2.4 `file_references.ref_type` 加 `showcase_draft`，S11-01／S11-03／S12-04 對齊；S13-07 [BLOCKED D-02]；S14-07 只留量測，告警拆 S14-12 [BLOCKED D-04]；S13-04 [BLOCKED D-07]、S13-05 [BLOCKED D-08]、S09-03 [BLOCKED D-03]、S08-16 [BLOCKED D-05、D-06]、S08-17 [BLOCKED D-13]、S11-13 [BLOCKED D-11] | D-02、D-03、D-04、D-05、D-06、D-07、D-08、D-11、D-13 |
| 總圖 S13 依賴 | 工程可決（E-12）＋spec 回寫 | 總圖 v2.4 加 S10、S11 → S13；索引 §5 不再列為問題 | 無 |
| §6 疑問清單 | — | 各檔「讀 spec 時發現的問題」去重後映射到本文 §2 的 D／E 編號；已由其他票交付者標已解；「簡報票號與舊稿不同」等噪音從索引 §6 過濾 | — |

票數：158 → 166（新增 S03-04、S08-16、S08-17、S11-11、S11-12、S11-13、S12-16、S14-12；無刪除）。BLOCKED 10 張：S08-16、S08-17、S09-03、S11-12、S11-13、S13-04、S13-05、S13-07、S14-11、S14-12（其中 S13-04 阻塞其後 S13-05、S13-08 的開工）。

## 2. 決策表（去重；每項連受阻票）

### 2.1 已定需求，直接修票（不需 Roy 再決）
| 項 | 依據 | 落點 |
|---|---|---|
| 換組長只有管理員 | 產品模組 03「組長」定案 | S03-09 |
| 評分老師可讀被指派組的正式版本與附件 | 契約 03 §1 | S10-03、S10-04 |
| 新成員只補新成員適用的「新收件」通知 | 產品模組 08 §4 | S09-01 |
| 送出／退回並行以「同指派已有 counted 則送出 `CONFLICT`」為準 | 模組 06 §3、§8 | S10-08 |
| 快照到期＝截止分鐘結束＋60 秒；寬限不是可晚交 | 母 spec §4.10、模組 05 §2 | S08-05 |
| 成員／主指導異動使目前版本（含 complete）superseded | 模組 07 §2 | S11-08 |
| 提案預設天數由管理員設定 | 產品模組 03「提案終止」 | S03-04 |
| 解散結束成員／組長／指導有效區間；`freezeForDissolution` 為守門 | 產品 GRP-13(b)；模組 05 附錄 A 無凍結欄 | S09-03（E-14） |
| 解散組評分讀取 policy 依產品 GRP-20 | 產品模組 03 §5.5 | S09-04；模組 06 §5 待補一句（實作 PR 時回寫） |

### 2.2 工程可決（Fable 定，已回寫或記錄；Roy 不需逐項決）
| ID | 決定 | 回寫位置 | 受影響票 |
|---|---|---|---|
| E-01 | `due_work`／`digest_events` identity：global `(item, deadline_version)`；targeted `(targeted_reopens.id, 1)` | 契約 01 §4.7 v2.5；模組 08 附錄 A v2.4 | S08-07、S08-11 |
| E-02 | 白名單內未註冊 handler→pending、退避 5 分鐘、attempts 不累計；`defer`→同；例外才累計 | 模組 08 §6 v2.4；契約 01 §4.7 v2.5 | S02-07、S04-05、S05-05、S08-06 |
| E-03 | `overdue_digest` 由 `deadline_snapshot` handler 完成後同交易排 | 模組 08 §6 生命週期表 | S08-11 |
| E-04 | `file_gc`／`receipt_purge` 首筆由 S12 migration／啟動腳本插入，handler 完成時排下一筆 | 模組 08 §6 生命週期表 | S12-12、S07-04 |
| E-05 | `file_references.ref_type` 加 `showcase_draft`；發布 attach 版本引用並 release 草稿引用 | 模組 10 附錄 A v2.4 | S11-01、S11-03、S12-04 |
| E-06 | `storage_stats` 寫入者＝worker 每小時（契約 01 §5 既有）；SOP 05 步驟 2 文字同步改（實作 PR 時） | 契約 01 §5（不變）；SOP 05 待改一句 | S14-07 |
| E-07 | `syncRoster(tx, {cohortId, userId}, cause)`，05 內部依 cohort 掃；新增 `syncRosterForMember(tx, groupId, userId)`；停用→`removeFromRoster` 經 S01-11 預留 hook | 模組 05 §5（實作 PR 時回寫簽章） | S08-03、S09-01、S01-11 |
| E-08 | 事件型別正式名稱在 `event-catalog.ts` 對照產品矩陣；spec 只寫中文事件名 | 模組 08 §4（既有） | 全部發事件票 |
| E-09 | `OpsCommand` 為同一 port 兩段（08 retry、10 record） | 模組 08／10 §5（既有） | S12-11、S14-05 |
| E-10 | 備份失敗站內通知由 worker 掃 `backup_runs` 新 failed 列產生 `backup_failed` digest | 模組 08 §6（實作 PR 時補一列） | S12-15 |
| E-11 | `AGE_RECIPIENTS` 空→啟動前置檢查失敗，不寫 `backup_runs`（維持 `recipients_count ≥ 1`） | 契約 05 §7（實作 PR 時補一句） | S14-05 |
| E-12 | 總圖加 S10、S11 → S13；S03-01→S04-01 為 schema 前置 | 總圖 v2.4 | S13-02、S13-05、S04-01 |
| E-13 | `cohorts.proposal_default_days integer NOT NULL DEFAULT 7 CHECK > 0`；`CohortCommand.setProposalDefaultDays` | 模組 02 v2.4 | S03-04、S03-05 |
| E-14 | 模組 05 `UnfinishedWorkQuery` 由 S08-15 交付；解散結束三張有效區間表 | S08-15、S09-03、S13-02 | — |
| E-15 | 影響預覽 token＝`groups.revision`＋異動集合 hash，10 分鐘到期，過期 `CONFLICT` | 模組 03 §7（實作 PR 時補） | S09-01、S09-03 |
| E-16 | `PublicContentQuery.search(q, cohort?)` | 模組 09 v2.4 §5 | S12-16 |
| E-17 | `SignoffCommand.remind(versionId, requestId)` | 模組 07 v2.4 §5 | S11-11 |
| E-18 | 評分老師讀材料 policy 由 S10-03 供應，S07-06／07 只加分支 | — | S10-03 |
| E-19 | `deploy.sh --expect-worker` 切換有限／完整健康判定 | 契約 05 §3（實作 PR 時補一句） | S14-03、S00-10 |

### 2.3 Roy 產品決策（未決前相關票 BLOCKED；附推薦）
| ID | 問題 | 候選與推薦 | 受阻票 | 解除證據 |
|---|---|---|---|---|
| D-01 | 簽核 revision 版本「重開」的語意 | A（推薦）沿新版本模型：`reopen`＝以目前內容建新 collecting 版本、`cause='reopen'`、舊 approvals 留歷史；B 同版輪次（需設計輪次欄與唯一鍵） | S11-12 | 模組 07 §3 加轉移列、附錄 A 同步 |
| D-02 | 跨屆調整政策：已在有效組別的學生能否換屆；port 歸屬 | A（推薦）只允許無有效組別者換屆，port 放模組 01 `AccountCommand.changeCohort` 並呼叫 05；B 允許並先解除 membership | S13-07 | 模組 01／05 §5 補 port 與規則 |
| D-03 | 解散時 complete 的 `final_document` 版本是否 void（會撤已公開精選） | A（推薦）complete 版本改 superseded（歷史保留），公開精選依 S11-10 `isValid` 即擋並由管理員決定是否撤稿；B 一律 void；C 保留 complete 有效 | S09-03 | 模組 07 §3「any→void：解散」補註 |
| D-04 | 磁碟 ≥80％ 告警：`digest_events.kind`、事件型別、是否站內通知 | A（推薦）加 kind `disk_warn`，站內通知管理員＋webhook，同小時去重 | S14-12（S14-10 磁碟段） | 模組 08 附錄 A 加 kind |
| D-05 | 「期限異動」通知是否含免填者 | A（推薦）含免填者（仍在名單、需知道期限），不含已移出者 | S08-16 | 產品模組 08 §4 補註 |
| D-06 | 改結構、受眾變更是否通知、通知誰 | A（推薦）受眾變更以「加入／移出收件名單」既有事件涵蓋；改結構通知目前有效名單成員（needs_review 者文案不同） | S08-16 | 產品模組 08 §4 加兩列 |
| D-07 | 解封後被封存取消的 pending `due_work` 是否補跑或重排 | A（推薦）解封不恢復已取消工作；到期日已過者以既有規則 `DEADLINE_PASSED`、快照由管理員手動觸發（S12-11 `retryDueWork` 類）；B 解封時重排未到期者 | S13-04（連帶 S13-05、S13-08 開工） | 模組 02 §3 解封列補註 |
| D-08 | 封存後匯出（新增 `stored_files`／`signoff_exports`）是否為允許例外 | A（推薦）允許，視為讀取類寫入，不受 `COHORT_ARCHIVED` 擋 | S13-05 | 母 spec §4.7 封存規則補註 |
| D-09 | 操作紀錄／操作者保存模型與 reset 目標環境 guard | A（推薦）`restore_drills.performed_by_user_id` 改 nullable ON DELETE SET NULL＋`performed_by_label text NOT NULL`（快照）；guard 只認 `APP_ENV=staging` 副本 | S14-11 | 模組 10 附錄 A、SOP 06 步驟 3 |
| D-10 | 成組截止＝成組階段結束（下一階段開始日） | 確認即可（推薦如左） | 無（S03-05 已依此） | 模組 03 §3 補一句 |
| D-11 | signoff.superseded 與「被移出者本人說明」的收件集合與文案 | A（推薦）失效通知給該版本快照參與者＋管理員、文案帶原因；被移出者只收本人說明、不帶組別入口 | S11-13 | 產品模組 08 §4 加列 |
| D-12 | S05、S08 的 Roy 看畫面落點 | 推薦：S05 名單頁與完成率；S08 免填與指定重開對話框 | 無（切片卡補 gate 即可） | S05、S08 卡出場條件 |
| D-13 | 組別收件在成組後自動加入名單的規則 | A（推薦）發布後才成立的組在截止前自動進名單（同個人「截止前核准」）；截止後成組不進 | S08-17 | 模組 05 §3 補列 |

## 3. 有效依賴圖（schema 前置、功能前置、epic gate 分開）

- **schema 前置**（只依賴對方的 migration 票，不等對方 epic）：S00-04→S01-01→S02-01→S03-01→S04-01→S05-01／S06-01／S07-01→S08-01→S10-01／S11-01→S12-01；例外：S03-01→S04-01（F03）。migration 順序即此鏈。
- **功能前置**（票級，只引用共用前置票）：索引 §4 表；被最多切片引用：S01-03（ActorResolver／audit／帳本）、S01-04（殼）、S02-03（事件與排程 port）、S00-05（角色）、S02-02（業務鐘）、S00-04、S00-03（測試工具）、S01-01、S02-07（handler 註冊表）、S02-04、S01-07（檔案）、S02-08（通知匣）。
- **epic gate**（`E0x 出場`）：總圖 v2.4——S00→S01→S02→{S03,S04}；S04→S05；S03→S06；{S03,S05,S06}→S07；S07→{S08,S10,S11}；{S06,S10,S11}→S09；S11→S12；{S08,S10,S11}→S13；{S12,S13}→S14（S14-01／02 可在 E01 後提前）。
- **handler 供應**：排程者票在前、handler 票在後時，工作保持 pending（E-02），不構成循環；生命週期表列出每 kind 的兩端。
- **逐票紙上核對**：每份票草稿末段列資料表、port／工具、頁面入口、劇本步驟的供應票；核對腳本（`check_tickets.py`）驗未定義引用 0、同切片順序 0 違反、循環 0、九欄齊、責任表 145 案覆蓋 0 缺漏；跨切片票引用只允許總圖前置切片（S04 對 S03 為 schema 例外）。

## 4. 行為子項覆蓋表（本輪指出者；狀態＝完整／部分／BLOCKED／DEFERRED）

| 行為子項 | 來源 | 供應票 | 狀態 |
|---|---|---|---|
| 評分老師讀被指派組的正式版本與附件 | 契約 03 §1；F06 | S10-03（policy）、S10-04（入口）、S07-06／07（分支） | 完整 |
| 新成員補「新收件」通知 | 產品 08 §4；F07 | S09-01 | 完整 |
| 公開搜尋頁 | P09 步驟 8、SHW-10；F14 | S12-16 | 完整 |
| 登入者產學列表與詳情 | 模組 09 §7 路由表、§5 `industryList` | S06-08（`/industry` 列表與詳情；模組 09 的 query 委派 03） | 完整 |
| 提醒未同意者 | 產品 07；F14 | S11-11 | 完整 |
| 定時維運：health cron、每日備份、磁碟量測、還原演練 | SOP 04／05 | S14-04、S14-06、S14-07、S14-09 | 完整（磁碟告警通知 S14-12 BLOCKED D-04） |
| 提案預設天數設定 | 產品 03；F04 | S03-04 | 完整 |
| 換組長 | 產品 03 | S03-09（管理員） | 完整 |
| 期限異動／改結構／受眾變更通知 | 產品 08 §4 | S08-16 | BLOCKED D-05、D-06 |
| 組別收件成組後自動入名單 | 模組 05 §3 | S08-17 | BLOCKED D-13 |
| 簽核重開 | 模組 07 §5 | S11-12 | BLOCKED D-01 |
| 簽核失效通知、被移出者說明 | 產品 08 | S11-13 | BLOCKED D-11 |
| 跨屆調整 | COH-08 | S13-07 | BLOCKED D-02 |
| 解封後 due_work 補跑 | 模組 02 §3 | S13-04 | BLOCKED D-07 |
| 封存後匯出例外 | 母 spec §4.7 | S13-05 | BLOCKED D-08 |
| 解散對 complete 簽核版本 | 模組 07 §3 | S09-03 | BLOCKED D-03 |
| staging→正式預演與 reset | SOP 06 | S14-11 | BLOCKED D-09 |
| Email 通知、外部日曆、NTF-09–12 | 產品延後 | — | DEFERRED |

## 5. 審後怎麼走
1. Codex 集中核對本文 §1 的局部差異與 §2 決策表；Roy 逐項回覆 D-01～D-13（可先只回阻塞多的 D-07、D-03、D-01）。
2. 通過後一次開齊 GitHub backlog：E00–E14 epic、166 張票，BLOCKED 票帶 `blocked:D-xx` 標籤與解除證據欄；可開始票依 §3 順序。
3. 開工仍按既定 gate：S00 開工前 GitHub 方案、T3、T2；每張 epic 由 Roy 放行。
