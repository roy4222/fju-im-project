---
type: ticket-drafts
slice: S00
project: FJU IM Project
updated: 2026-09-13
status: draft-v0.3-pending-codex-review
---
# S00 票草稿｜工程骨架、分層 lint、Compose、CI、第一支 migration（v0.3，2026-09-13）

> spec 基準：母 spec v3.6（`f974bce`）通過；v0.3 依票草稿集中 review（F01–F15）局部修訂，spec 局部回寫為母 spec v3.7（契約 01 v2.5、模組 02／07／08／09／10 v2.4、總圖 v2.4）。本檔是票草稿，不是 ticket；待 Roy 與 Codex 集中 review 後才開 GitHub 票。切片卡：[S00 工程骨架與 CI](<../S00 工程骨架與 CI.md>)。v0.2 對第一批 review 的修正：T01——Better Auth／drizzle 釘版與 schema 產生（S00-02）、本機測試工具（S00-03）提前到 migration 之前；CI 接線（S00-09）只做接線並明列它驗證的票；T04——GitHub 方案確認恢復為 S00 開工前。
>
> **epic 出場（E00）**：自動測試綠——lint fixtures（S00-06）、空庫 migration（S00-04）、`roles.test.ts` 逐表矩陣（S00-05）、health 整合（S00-08）、CI 七道（S00-09）；切片操作起點（總圖表 S00 列：`docker compose up`、`/api/health`、lint 反例與合法例、空庫 migration、roles 測試）留 `steps/S00/`；Fable review；Roy 看 CI 綠燈。環境 local＋CI。不要求案例 PASS。

## 開工前 gate（不是票；依前置清單 §7 與 §2.1）
- **T3 自動測試主要接縫**在 S00-03 前定（預設 Codex 建議：application 用例＋真 PostgreSQL）。
- **T2 CSP 方案**在 S00-11 前定（預設 A）。
- **T1 runtime 角色是否拆 app／worker**在 S00-05 前定；未決時依預設共用 `fju_app`，之後以增量角色加入，不阻塞建表。
- **GitHub 方案確認（Free／Pro／Team／Enterprise）在 S00 開工前**由 Roy 回報（前置清單 §2.1 第 3 點（0）、第 6 點）；branch protection 與 required checks 的**套用**在 S00-09 的 `ci.yml` 第一次跑過、check 名稱出現後設定（方案支援時），最晚在 S00 第一個 `web/` PR 合併前；方案不支援則改書面約定。方案確認與保護設定是兩件事，票稿不放寬既定 gate。

## 建議順序（每張票只用編號更前的前置）
| 票 | 目標 | 前置 | 提供給後面的票什麼 |
|---|---|---|---|
| S00-01 | 骨架、分層目錄、shared | — | 目錄、paths、`shared/time`／`result`／`errors` |
| S00-02 | Better Auth＋drizzle 釘版與 schema 產生 | S00-01 | 套件版本、drizzle schema（Auth 四表＋業務擴充欄＋admin plugin 套件欄）、欄名 gate 結果 |
| S00-03 | 本機測試工具 | S00-01 | Compose `postgres` 服務、整合 harness、同步屏障、故障注入約定、`pnpm test` |
| S00-04 | 第一支 migration | S00-02、S00-03 | Auth 四表、最小 `cohorts`、`schema_meta`、五基礎表 |
| S00-05 | DB 角色、GRANT、不可變 trigger、`roles.test.ts` | S00-04 | 三角色、矩陣驅動的 GRANT 產生器、可擴充的逐表測試 |
| S00-06 | 分層 lint 與 fixtures | S00-01 | `pnpm lint`、`lint:boundaries-test` |
| S00-07 | Compose 六服務、Caddyfile、`.env.example` | S00-04、S00-05 | app／worker／migrate／caddy／backup 服務定義、local override |
| S00-08 | `/api/health` 與空殼首頁 | S00-04、S00-07 | health 端點、版本注入、`schemaVersion` 讀取 |
| S00-09 | CI 七道接線 | S00-03、S00-05、S00-06、S00-08 | `ci.yml`（required checks 名稱）、CI 內 Compose postgres |
| S00-10 | `cd.yml` dry-run 與 `deploy.sh --dry-run` | S00-08、S00-09 | 部署腳本骨架（S14 實跑） |
| S00-11 | CSP 與安全標頭基線 | S00-08 | nonce 注入、標頭、限速骨架位置 |

## 票

### S00-01 骨架、分層目錄、shared
- **目標**：`web/` 以 Next 16、pnpm、Node 22 建立，六層目錄與 `tsconfig` paths 到位，`pnpm dev` 起得來、`pnpm typecheck` 綠，`shared/time`／`result`／`errors` 可被其他層引用。
- **spec**：母 spec §4.3、§4.11（RealClock／BusinessClock、+08:00 日界與分鐘界）；契約 02 §7、§10（錯誤碼）；S00 卡「責任範圍」。
- **範圍**：`web/src/{domain,application,infrastructure,app,composition,shared}`、`tsconfig` paths、`shared/time`（兩個時鐘介面、`timestamptz` UTC、日界／分鐘界換算）、`shared/result`、`shared/errors`（契約 02 錯誤碼列舉）；`domain` 允許 `decimal.js`；`next.config` 不改 `bodySizeLimit`。**非範圍**：任何頁面、任何 DB、lint 規則（S00-06）。
- **前置**：無。
- **路徑**：無業務路徑；`shared/time` 邊界（00:00、年度結束含當天、截止分鐘 `receivedBusinessAt < deadline_minute_start + 1 分鐘`）為單元測試對象。
- **自動測試**：`shared/time` 邊界單元；`shared/result`／`errors` 型別單元（`pnpm test:unit`，不需 DB）。
- **操作證據**：`pnpm dev` 空殼截圖；`pnpm typecheck` 輸出（`steps/S00/S00-01/`）。
- **完成定義**：單元測試綠；目錄與 paths 與母 spec §4.3 一致；Fable review。
- **待 Roy／平台**：無。

### S00-02 Better Auth＋drizzle 釘版與 schema 產生
- **目標**：Better Auth（含 admin plugin）與 drizzle／drizzle-kit 以固定版本安裝；Better Auth schema 產生器輸出納入 repo 並加上業務擴充欄；產生的 drizzle schema 含 admin plugin 套件欄，欄名記入票（D1 的欄名 gate）。
- **spec**：契約 01 §4.1（`users` 擴充 `status`／`must_change_password`／`deidentified_at`，`sessions.login_method`；admin plugin 套件欄 `banned`／`ban_reason`／`ban_expires` 以安裝後 schema 為準）；模組 01 §12（安裝後欄名覆核）；母 spec §7.3；ADR 0004。
- **範圍**：`package.json` 釘版、`composition/auth.ts` 的最小 config（只到能產 schema，不掛路由）、`infrastructure/db/schema/*`、產生器輸出與差異紀錄。**非範圍**：路由白名單、hooks、內部呼叫辨識（S01-02、S01-03）；migration SQL（S00-04）。
- **前置**：S00-01。
- **路徑**：`pnpm auth:generate` 可重跑且輸出穩定；欄名與契約 01 §4.1 不同時，先回 Vault 改 spec 再繼續（票上留紀錄）。
- **自動測試**：schema 快照測試（產生器輸出＝repo 檔）；欄位存在性單元（`users.status`、`must_change_password`、`deidentified_at`、`sessions.login_method`、admin plugin 三欄）。
- **操作證據**：版本號、產生器輸出摘要、欄名對照表。
- **完成定義**：三個欄名寫進票並回填模組 01 §12（若一致則只記「已核對」）；快照測試綠。
- **待 Roy／平台**：無。

### S00-03 本機測試工具
- **目標**：`pnpm test` 能對真 PostgreSQL 跑整合測試：Compose 只起 `postgres` 服務、每個測試獨立 schema（或交易回滾）、同步屏障工具、故障注入點約定、run manifest 最小欄位輸出。
- **spec**：母 spec §5（主要接縫、同步屏障、注入點）；契約 04 §2、§7；S00 卡。
- **範圍**：`docker-compose.yml` 的 `postgres` 服務（其他服務在 S00-07 補齊）、harness、屏障、注入約定、`web/test/README`。**非範圍**：CI 接線（S00-09）；任何被測對象。
- **前置**：S00-01。**T3 在本票前定案。**
- **路徑**：測試 DB 不存在時 harness 自建；並行測試不互相污染。
- **自動測試**：harness 自測（兩個測試並行寫同名表不衝突；屏障能讓兩個交易在指定點會合）。
- **操作證據**：`pnpm test` 輸出；`docker compose ps postgres`。
- **完成定義**：harness 自測綠；README 寫清楚用法；T3 決定記在票上。
- **待 Roy／平台**：T3。

### S00-04 第一支 migration
- **目標**：空庫跑第一支純 SQL migration 成功，表、欄、順序與契約 01 §4／§12 逐項一致；`schema_meta` 記版本；重跑為 no-op。
- **spec**：契約 01 §4.1（Auth 四表含擴充欄與套件欄）、§4.2 最小 `cohorts`、§4.3 `audit_events`、§4.4 `operation_records`、§4.5 `domain_events`、§4.6 `event_projections`、§4.7 `due_work`（`kind` 含 `test_noop`）、§4.8 `schema_meta`、§12 順序與 expand／contract 政策。
- **範圍**：drizzle-kit 由 S00-02 的 schema 產 SQL；`migrate` 服務入口（owner 連線）；migration README。**非範圍**：模組 01 表與 `stored_files`／`file_references`（S01-01）；角色與 GRANT（S00-05）；seed。
- **前置**：S00-02、S00-03。
- **路徑**：空庫升級；重複執行 no-op；失敗即中止且不留半套（單一交易或可重入）。
- **自動測試**：空庫 migration（S00-03 harness）；`schema_meta` 版本斷言；逐表逐欄存在性；不可變表暫無 trigger（S00-05）。
- **操作證據**：migration 輸出、`\d` 摘要。
- **完成定義**：票上附契約 01 §4／§12 逐欄核對表；測試綠。
- **待 Roy／平台**：無（T1 不擋本票）。

### S00-05 DB 角色、逐表權限 GRANT、不可變 trigger、`roles.test.ts`
- **目標**：`fju_owner`／`fju_app`／`fju_backup` 存在；GRANT 由契約 01 §5 矩陣**產生**並套用；不可變表 trigger 擋 UPDATE／DELETE；`infrastructure/db/roles.test.ts` 對矩陣每一格逐列證明（含 `fju_app` 對 `audit_events` UPDATE 被拒、欄級 UPDATE 只允許列出的欄、`fju_backup` 只能 INSERT `backup_runs`——該表在 S12 前不存在時測試以「表不存在」跳過並標記）。
- **spec**：契約 01 §5；契約 04 §4；母 spec §4.6；S00 卡。
- **範圍**：角色、矩陣檔（表驅動）、GRANT 產生腳本、trigger、測試。**非範圍**：後續切片新增表的矩陣列（各切片 migration 票補列並擴充本測試）。
- **前置**：S00-04。**T1 在本票前定**；若拆 app／worker，加第四角色與矩陣欄；未決依預設。
- **路徑**：允許操作成功；矩陣外被拒；trigger 擋不可變表。
- **自動測試**：`roles.test.ts`；trigger 測試。
- **操作證據**：測試輸出（每表一行）。
- **完成定義**：矩陣每格有測試；票模板加「新增表必擴充 roles.test」一條。
- **待 Roy／平台**：T1（可延後）。

### S00-06 分層 lint 與 fixtures
- **目標**：`pnpm lint` 對 `web/lint-fixtures/` 七個反例全部報錯、三個合法例不報錯；`pnpm lint:boundaries-test` 斷言此結果。
- **spec**：母 spec §4.3；契約 02 §7（`actions.ts` 頂層 `'use server'`、只匯出 async、Client 匯入 `actions.ts` 是例外、`composition`／`infrastructure` 檔頭 `server-only`）；S00 卡。
- **範圍**：`eslint-plugin-boundaries`、`no-restricted-imports`、`import/no-cycle`、`server-only`、`actions.ts` 規則；fixtures；斷言腳本；直接引用 `auth.api` 的禁止規則（供 S01-03 的包裝器成為唯一入口）。**非範圍**：CI 接線（S00-09）。
- **前置**：S00-01。
- **路徑**：反例被擋、合法例通過。
- **自動測試**：`lint:boundaries-test`。
- **操作證據**：lint 輸出。
- **完成定義**：七反例三合法例結果與 S00 卡一致；規則清單寫進 `web/README`。
- **待 Roy／平台**：無。

### S00-07 Compose 六服務、Caddyfile、`.env.example`
- **目標**：`docker compose up` 起 app、worker、migrate、postgres、caddy、backup（worker 此時是空進程，S02-06 換真）；`docker-compose.local.yml`；`.env.example` 列契約 05 §6 三組憑證變數名（無值）；Caddyfile 上傳上限。
- **spec**：S00 卡；契約 05 §6；契約 03 §5；SOP 01–03 的 Compose 約定。
- **範圍**：Compose 檔、Caddyfile、`migrate` 用 owner 連線、app／worker 用 app 連線、`depends_on` 條件（migrate 失敗 app 不起）。**非範圍**：VM 部署（S14）、插槽 host（SOP 04／S14）。
- **前置**：S00-04、S00-05。
- **路徑**：`compose up` 成功；migrate 失敗時 app 不啟動。
- **自動測試**：Compose config 驗證（`docker compose config` 通過、服務名固定）；migrate 失敗路徑以注入測。
- **操作證據**：`docker compose ps`。
- **完成定義**：六服務定義齊；`.env.example` 無任何值。
- **待 Roy／平台**：無。

### S00-08 `/api/health` 與空殼首頁
- **目標**：`/api/health` 回 `{ok, version, commit, imageDigest, schemaVersion, worker:{version:null,lastTickAt:null}}`；DB 不可用回 503 且不洩漏設定；`/` 為空殼。
- **spec**：S00 卡；契約 05 §3 健康判定欄位；劇本 P00 步驟 1–2。
- **範圍**：Route Handler、build 時注入 commit／digest、`schemaVersion` 讀 `schema_meta`。**非範圍**：worker 真值（S02-06）。
- **前置**：S00-04、S00-07。
- **路徑**：200；DB 掛 503。
- **自動測試**：health 整合（含 DB 不可用注入）。
- **操作證據**：兩種回應。
- **完成定義**：欄位名與契約 05 §3 逐字一致。
- **待 Roy／平台**：無。

### S00-09 CI 七道接線
- **目標**：`ci.yml` 的 `typecheck`、`lint`、`unit`、`integration`、`build`、`e2e-smoke`、`audit` 七道對 PR 綠；`integration` 用 S00-03 的 Compose postgres；job 層 `permissions` 最小。
- **spec**：契約 05 §2；前置清單 §2.1 第 5 點（check 名稱固定）；S00 卡。
- **範圍**：workflow 檔、快取、七道接線；本票驗證的對象＝S00-05 roles 測試、S00-06 lint 測試、S00-08 health 整合、S00-01 單元。**非範圍**：branch protection 與 required checks 的套用（Roy；本票完成後 check 名稱才存在）。
- **前置**：S00-03、S00-05、S00-06、S00-08。
- **路徑**：任一道失敗 PR 紅。
- **自動測試**：本票就是接線；以一個故意失敗的 fixture PR 證明紅燈（證據留 URL 後關掉）。
- **操作證據**：CI run URL（綠與紅各一）。
- **完成定義**：七道綠；check 名稱與前置清單一致；把「required checks 可設定」通知 Roy。
- **待 Roy／平台**：GitHub 方案已在 S00 開工前確認；保護規則在本票後由 Roy 套用（方案支援時）。

### S00-10 `cd.yml` dispatch-only dry-run 與 `deploy.sh --dry-run`
- **目標**：`cd.yml` 只允許 `workflow_dispatch` 且預設 `--dry-run`；`deploy.sh --dry-run` 印出 flock→previous_tag→pull→migrate→up→健康判定→回滾各步而不執行。
- **spec**：契約 05 §3；SOP 03；S00 卡。
- **範圍**：兩個檔案與 dry-run 測試。**非範圍**：Secrets、environment、實際部署（S14）。
- **前置**：S00-08、S00-09。
- **路徑**：dry-run 不改狀態；非 dispatch 觸發被拒。
- **自動測試**：CI 跑 `deploy.sh --dry-run` 並斷言步驟輸出。
- **操作證據**：dry-run 輸出。
- **完成定義**：步驟與契約 05 §3 逐項對應。
- **待 Roy／平台**：無。

### S00-11 CSP 與安全標頭基線
- **目標**：全站回應帶 CSP（依 T2；預設 A：動態渲染＋nonce＋`strict-dynamic`）與 cookie 屬性；hydration 正常、無 nonce 的注入腳本被擋；限速的骨架位置就緒（規則在 S01-05／S01-09）。
- **spec**：母 spec §4.15；契約 03 §6；契約 02 §9。
- **範圍**：nonce 注入、標頭、限速中介層位置。**非範圍**：Turnstile（S01-15）。
- **前置**：S00-08。**T2 在本票前定案。**
- **路徑**：合法 script 執行；注入 inline script 被擋。
- **自動測試**：e2e-smoke 檢查標頭與注入被擋。
- **操作證據**：回應標頭、瀏覽器 console。
- **完成定義**：標頭字串與契約 03 §6 一致；T2 決定記在票上。
- **待 Roy／平台**：T2。

## 紙上依賴核對
- S00-01：資料表←無；工具←無；入口←無；劇本：不適用。
- S00-02：資料表←無（只產 schema）；工具←S00-01；入口←無；劇本：不適用。
- S00-03：資料表←無；工具←S00-01；入口←無；劇本：不適用。
- S00-04：資料表←自身；工具←S00-03（harness）、S00-02（schema）；入口←無；劇本：不適用。
- S00-05：資料表←S00-04；工具←S00-03；入口←無；劇本：P00 步驟 7 的 `/api/auth/admin/*` 403 屬 S01-02。
- S00-06：資料表←無；工具←S00-01；入口←無。
- S00-07：資料表←S00-04；工具←S00-03（postgres 服務）；入口←無。
- S00-08：資料表←S00-04（`schema_meta`）；工具←S00-03；入口←自身；劇本：P00 步驟 1（`worker` 欄為 null，步驟 1 的 worker 一致性在 S02-06 後才成立）。
- S00-09：資料表←S00-04；工具←S00-03、S00-06；入口←S00-08；劇本：不適用。
- S00-10：工具←S00-09；入口←S00-08。
- S00-11：入口←S00-08；工具←S00-03（e2e-smoke 在 S00-09 接線後跑）。
無循環；每票的前置編號都更小。

## 讀 spec 時發現的問題（不改 spec，只列）
- 契約 01 §5 的 `fju_backup` 只 INSERT `backup_runs`：該表由模組 10 附錄 A 定義、S12 才建；S00-05 的測試需標「表不存在則跳過」，待 S12 migration 票補齊。建議責任表或契約 01 §12 註明 `backup_runs` 建表切片。
