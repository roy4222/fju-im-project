---
type: ticket-drafts
slice: S13
project: FJU IM Project
updated: 2026-09-13
status: draft-v0.3-pending-codex-review
---
# S13 票草稿｜封存／解封、多屆並行、下一屆、跨屆隔離、通知匣跨屆（v0.3，2026-09-13）

> spec 基準：母 spec v3.6（`f974bce`）通過；v0.3 依票草稿集中 review（F01–F15、R01–R08）局部修訂，spec 局部回寫為母 spec v3.7（契約 01 v2.5、05 v2.4、模組 02／05／07／08／09／10 v2.4、總圖 v2.4）。本檔是票草稿，不是 ticket；待 Roy 與 Codex 集中 review 後才開 GitHub 票。切片卡：[S13 封存、多屆與下一屆](</Users/lubaiyu/Documents/roy422的人生online/專案/🌐 網站與互動/📁 輔大資管系專題網站/🛠️ 工程開發/🧱 實作切片/S13 封存、多屆與下一屆/00 進度與工作包.md>)。
>
> **epic 出場（E13）**：（1）本切片自動測試綠——封存×送出競態（同步屏障）、兩屆並行佇列不混、解封不變更期限與關係、鐘回撥不復活（已作廢提案、已做快照）、封存後各模組寫入 `COHORT_ARCHIVED` 而讀取與匯出仍可、帳本重播封存後只回 `COHORT_ARCHIVED`、跨屆直接請求 `FORBIDDEN`、通知匣跨屆篩選與封存屆已讀保存；（2）切片操作起點（總圖 S13 列：封存預覽→封存→解封→116 建立→跨屆隔離→通知匣跨屆）走通並留 `steps/S13/`；（3）Fable review；（4）Roy 看封存預覽對話框、屆別切換器與兩屆並行的屆別列表。案例 PASS 在完整年度（S13 出場是完整年度的前置）。

## 建議順序（每張票只用編號更前的前置）
| 票 | 目標 | 前置 | 提供給後面的票什麼 |
|---|---|---|---|
| S13-01 | 老師與管理員屆別切換器（`ui.cohort` 只選畫面）、進入該屆的狀態橫幅（籌備中／進行中／已封存／已解封）、`CohortStatusQuery.listForActor` | S01-04、S01-06、S02-05 | 三角色殼的屆別脈絡與狀態顯示，供封存／解封／並行票掛載 |
| S13-02 | `UnfinishedWorkQuery` 三實作接線（05 補齊、06／07 從 E10／E11 接入）與封存預覽對話框 | S13-01、E10 出場、E11 出場 | 封存前的未完成清單與 `unfinished_summary` 結構 |
| S13-03 | 封存 `CohortCommand.archive`（`FOR UPDATE`、狀態事件含 `unfinished_summary`、pending `due_work` 取消、audit、帳本）與封存回執 | S13-02、S01-03、S02-03 | `archived` 狀態與紀錄，供解封與唯讀測試 |
| S13-04 | 解封 `CohortCommand.unarchive`（理由、`unarchived_count`、不動期限與關係、不重送通知）與再封存第二筆 | S13-03 | 封存↔active 往返的完整狀態事件 |
| S13-05 | 封存唯讀全站驗證：封存×送出競態、各模組寫入用例 `COHORT_ARCHIVED`、讀取與匯出仍可、帳本重播、解封後舊截止 `DEADLINE_PASSED` | S13-04、S00-03、E11 出場 | 契約 04 §4「封存×送出」證明；P09 步驟 13–14、B06 步驟 2、7 |
| S13-06 | 下一屆 116-TEST 建立（籌備中→進行中、旗標切換）、兩屆並行與跨屆隔離（學生不混待辦、直接請求 `FORBIDDEN`） | S13-01、S01-06、S02-05、E08 出場 | 兩屆並行資料與隔離證據；116 供跨屆調整與通知匣票用 |
| S13-07 | 跨屆調整（學生 116→115：名單移除、待辦消失、歷史保留） | S13-06、S01-10、E08 出場 | COH-08 的最終責任行為 |
| S13-08 | 通知匣跨屆篩選、預設全部屆別＋全站、封存屆通知「已封存（唯讀）」且可標已讀 | S13-04、S02-08 | NTF-20 |
| S13-09 | 業務鐘倒退：回撥不復活已作廢提案、快照不重做、截止判定用當前業務時間、`deadline_version` 不變 | S02-02、S02-07、E08 出場 | COH-10；手冊 §5 的回撥與重置操作 |

## 票

### S13-01 屆別切換器與屆別狀態橫幅（殼）
- **目標**：老師與管理員在側欄可切換工作屆別（cookie `ui.cohort`，只選畫面、不作授權），學生固定看自己屆別、無切換器；進入任一屆別的頁面在頂部明確顯示該屆狀態（籌備中／進行中／已封存／已解封，解封顯示「已解封」並附最近一筆理由），`CohortStatusQuery.listForActor(actor)` 只回該角色可見的屆別。
- **spec**：模組 02 §2「封存與解封不另發通知，進入該屆明確顯示狀態」、§5 `CohortStatusQuery.listForActor`、§7.2「兩屆並行的屆別切換」（缺口，新做）、§9「老師／管理員切換屆別（UI 狀態）」；契約 02 §7、「屆別切換 `ui.cohort` 只選畫面，不作授權」；契約 03 §1 通用規則「`cohort_id` 必比對」；母 spec §4.13「角色切換與屆別切換只是 UI 狀態」；劇本 P09 步驟 12、15、B06 步驟 1、6；案例 COH-05、06、09（追溯）。
- **範圍**：切換器元件（掛在 S01-04 的老師／管理員殼側欄）、`ui.cohort` cookie 讀寫、`listForActor`、狀態橫幅元件（三角色 dashboard layout）、切換後各頁以 cookie 為預設篩選值。**非範圍**：任何用例（封存 S13-03、解封 S13-04）；屆別列表頁本體（S01-06 已有）；學生待辦隔離的伺服端過濾（各模組 policy；驗證在 S13-06）。
- **前置**：S01-04、S01-06、S02-05。
- **路徑**：正常／切換後重載仍保留（cookie）、切到已封存屆顯示「已封存」橫幅；拒絕／學生無切換器、老師切到不在 `listForActor` 內的屆別 → cookie 被忽略回預設工作屆別（不 403，因只選畫面）、以 cookie 指向他屆直接請求他屆資料仍由用例 policy 拒 `FORBIDDEN`（本票只驗「cookie 不放寬授權」）；重試／無寫入。
- **自動測試**：單元：`listForActor` 三角色可見集合（管理員全部、老師有指派或指導關係的屆別＋預設工作屆別、學生只本屆）；整合：`ui.cohort` 指向無權屆別時用例仍 `FORBIDDEN`（直接請求）；e2e-smoke：切換後重載保留、學生頁無切換器、封存屆橫幅文案。
- **操作證據**：老師與管理員切換器截圖（兩屆）、封存屆橫幅截圖、`ui.cohort` cookie 值（存 `steps/S13/S13-01/`）。
- **完成定義**：三角色殼有屆別脈絡；上述測試綠；Fable review；**本票不等任何功能票**。
- **待 Roy／平台**：模組 02 §7 沒有切換器的路由列與樣式，Roy 看畫面時定位置（側欄或頂列）；見問題 4。

### S13-02 `UnfinishedWorkQuery` 三實作接線與封存預覽
- **目標**：管理員在屆別管理頁對 active 屆按「封存預覽」看到未完成清單：05（未送出或未達最低份數的收件、進行中草稿）、06（未送出評分、未完成階段）、07（收集中或老師待簽的簽核版本），每項含類型、對象、數量與校方採認狀態；預覽只讀、不改任何資料；三個實作以模組 02 的 `UnfinishedWorkQuery.list(tx, cohortId)` 介面由 composition 注入。
- **spec**：模組 02 §3「active → archived 前置：影響預覽（05／06／07 `UnfinishedWorkQuery`）＋理由」、§5 `UnfinishedWorkQuery`（由 05／06／07 實作）、§7「封存預覽對話框：列未完成項」、§7.2 缺口「封存預覽」；模組 05 §5、06 §5、07 §5 `UnfinishedWorkQuery.list(tx, cohortId)`；母 spec §4.8「02 封存前要問 05／06／07 的未完成清單，走各模組實作的 port，由 composition 注入」；劇本 P09 步驟 11；案例 COH-05（追溯）。
- **範圍**：05 的 `UnfinishedWorkQuery` 實作（見問題 2）、06／07 實作的注入與合併、預覽對話框（掛在 S01-06 的 `/dashboard/admin/cohorts`）、`unfinished_summary` 的 jsonb 結構定義（供 S13-03 寫入狀態事件）。**非範圍**：封存本身（S13-03）；06／07 的 `UnfinishedWorkQuery` 本體（E10 出場、E11 出場交付）。
- **前置**：S13-01、E10 出場、E11 出場。
- **路徑**：正常／active 屆列出三類未完成、全部完成時顯示「無未完成項」仍可封存；拒絕／非 admin `FORBIDDEN`、preparing 屆無「封存預覽」入口、已 archived 屆入口改為「解封」；重試／純讀取，重整重算。
- **自動測試**：整合：三個實作各自對 seed 資料回正確數量與 ID；合併結果順序固定（05→06→07）且不重複；同交易讀取（`tx` 傳遞）不外洩另一屆資料（帶錯 `cohortId` 回空）；非 admin 直接請求 `FORBIDDEN`；預覽前後三模組資料 hash 不變。
- **操作證據**：預覽對話框截圖（含未完成項與採認狀態）、預覽 JSON（存 `steps/S13/S13-02/`）。
- **完成定義**：P09 步驟 11 可走；`unfinished_summary` 結構寫進票並被 S13-03 引用。
- **待 Roy／平台**：05 的實作歸屬（S08 或 S13）與封存預覽的對外 query 名稱待定，見問題 2、3。

### S13-03 封存 `CohortCommand.archive`
- **目標**：管理員填理由後封存 active 屆：`cohorts` 列 `FOR UPDATE`、`status='archived'`、`archived_real_at`／`archived_by_user_id`、`revision+1`；同交易寫 `cohort_status_events(active→archived, reason, unfinished_summary, actor, real_at, business_at)`、該屆 pending `due_work` 改 `cancelled`、audit、帳本；不發任何通知事件；回執顯示封存時間與未完成摘要；封存後進入該屆顯示「已封存」（S13-01 橫幅）。
- **spec**：模組 02 §2「封存後該屆業務資料只讀；封存與解封不另發通知」、§3 active → archived 列（`FOR UPDATE`；status event 含 `unfinished_summary`；pending `due_work` 改 cancelled；拒絕 `CONFLICT`）、§5 `CohortCommand.archive(cohortId, revision, reason, requestId)`、§6「封存 `FOR UPDATE`」、§8、附錄 A `cohorts`（`archived_real_at`、`archived_by_user_id`）、`cohort_status_events`（不可變）；契約 01 §5（`cohort_status_events` S I、`due_work` S I U）、§7「封存後重播只會得到 `COHORT_ARCHIVED`」；母 spec §4.7「封存 × 任何寫入」、§4.8「封存只改自己的表」；劇本 P09 步驟 12；案例 COH-05、COH-09（追溯）。
- **範圍**：用例、`due_work` 取消（走 S02-03 `DueWorkScheduler.cancel` 或同交易 UPDATE，依契約 01 §5 權限）、對話框（理由必填、顯示 S13-02 預覽摘要後確認）、回執、audit、帳本 requestId。**非範圍**：解封（S13-04）；各模組寫入端的 `FOR SHARE` 拒絕（各模組已做，全站驗證 S13-05）；通知匣的封存遮罩（S13-08）。
- **前置**：S13-02、S01-03、S02-03。
- **路徑**：正常／封存成功→列表狀態 archived、狀態事件一筆、`due_work` 該屆 pending 全部 cancelled、其他屆 pending 不動；拒絕／revision 過期 `CONFLICT`（要求重載預覽）、理由空 `VALIDATION_FAILED`、preparing 屆 `VALIDATION_FAILED`、非 admin `FORBIDDEN`；重試／同 `requestId` 回原回執，不寫第二筆狀態事件。
- **自動測試**（T05）：整合：封存後 `cohorts.status`、`archived_*`、狀態事件與 `unfinished_summary` 保存並在重載／重登後可查；該屆 pending `due_work` 全 cancelled、他屆不變；同 `requestId` 重播不重複；revision 過期 `CONFLICT`；非 admin 直接請求 `FORBIDDEN` 且狀態不變；`fju_app` 對 `cohort_status_events` UPDATE／DELETE 被拒；封存不產生 `domain_events`（無通知）。
- **操作證據**：封存對話框與回執截圖、`cohort_status_events` SQL、`due_work` 前後 SQL（存 `steps/S13/S13-03/`）。
- **完成定義**：P09 步驟 12 可走；上述測試綠；Fable review。
- **待 Roy／平台**：預覽後資料變動是否要求重新預覽（無 basis hash），見問題 11。

### S13-04 [BLOCKED D-07] 解封 `CohortCommand.unarchive` 與再封存
- **目標**：管理員填理由解封 archived 屆：狀態回 active、`unarchived_count+1`、寫第二筆狀態事件（archived→active，理由）；不延長任何期限、不恢復已撤銷的關係（membership、指派、名單移除）、不重送通知、原封存紀錄保留；進入該屆顯示「已解封」；再封存留第三筆事件且 `unarchived_count` 不減。
- **spec**：模組 02 §2「解封填理由，不延長期限、不恢復撤銷關係、不重送通知」、§3 archived → active 列、§5 `CohortCommand.unarchive`、§8「解封填理由→再封存留第二筆」、§9「封存與解封紀錄可查」、附錄 A `cohorts.unarchived_count`、`cohort_status_events.reason`；S13 卡「恢復：解封不改期限與關係；非管理員無解封入口」；劇本 B06 步驟 1、7；案例 COH-09（追溯）。
- **範圍**：用例、解封對話框（理由必填）、回執、audit、帳本；「已解封」橫幅資料（S13-01 顯示）。**非範圍**：解封後舊截止仍 `DEADLINE_PASSED` 的送出端行為（模組 05；驗證在 S13-05）；`due_work` 的恢復（spec 未寫，見問題 5）。
- **前置**：S13-03。
- **路徑**：正常／解封→active、`unarchived_count=1`、事件兩筆；再封存→archived、事件三筆；拒絕／非 admin `FORBIDDEN` 且無入口、active 屆解封 `VALIDATION_FAILED`、理由空 `VALIDATION_FAILED`；重試／同 `requestId` 回原回執。
- **自動測試**（T05）：整合：解封前後所有 `items.deadline`、`cohort_stages.start_date`、`group_memberships.valid_to`、`evaluator_assignments.valid_to`、`response_rosters` 移除列逐欄 hash 不變；不產生 `domain_events`／`notifications`；重載與重登後狀態與計數保存；另一管理員同時解封同屆只成功一次（`FOR UPDATE`）；非 admin 直接請求 `FORBIDDEN`；老師與學生頁面無解封入口（e2e 掃描）。
- **操作證據**：解封回執、`cohort_status_events` 三筆 SQL、解封前後 hash 對照（存 `steps/S13/S13-04/`）。
- **完成定義**：B06 步驟 1、7 可走；上述測試綠。
- **待 Roy／平台**：BLOCKED D-07：封存時取消的 pending `due_work`（快照、對帳、彙整）解封後是否補跑或重排；未決前不以「不恢復」施工。**解除條件（Codex 最後核對）**：候選方案雖用「另建 `deadline_version`」，但 targeted identity 固定為 `targeted_reopens.id`／1、global 的期限版本又有業務含義（改期）；解除 D-07 前必須先分清「重排輪次」與「業務期限版本」兩個計數，並明定新工作如何引用來源快照、targeted 重開列與 digest identity；不得直接拿決策表目前的候選實作施工。

### S13-05 [BLOCKED D-08] 封存唯讀全站驗證：競態、各模組寫入拒絕、讀取與匯出仍可
- **目標**：以自動測試證明封存後所有業務寫入用例回 `COHORT_ARCHIVED` 且資料不變、封存與在途送出的競態只有一方成功、讀取／歷史／匯出在封存後仍可用、帳本重播封存前的 requestId 只回 `COHORT_ARCHIVED`、解封後舊截止仍 `DEADLINE_PASSED`（不是 `COHORT_ARCHIVED`）；不新增功能。
- **spec**：母 spec §4.7「封存 × 任何寫入：寫入用例對 `cohorts` 列 `FOR SHARE` 並檢查 `status='active'`；封存用例 `FOR UPDATE`；封存等待在途寫入；封存後的寫入看到 archived 而拒絕」；模組 02 §10「封存×送出競態」；模組 05 §2 送出守門順序（`COHORT_ARCHIVED` 在授權之後、時間之前）、§10「封存×送出」；契約 01 §7「封存後重播只會得到 `COHORT_ARCHIVED`」；契約 02 §3 錯誤碼 `COHORT_ARCHIVED`；契約 03 §1「已封存只讀」；契約 04 §4「精確並行（封存×送出）由整合測試同步屏障證明」；S13 卡「各模組寫入用例的 `FOR SHARE` 檢查在 S07 已做，此切片補完整測試」；劇本 P09 步驟 13、14；B06 步驟 2、7；案例 COH-05、COH-09（追溯）。
- **範圍**：測試套件（表驅動列出各模組寫入 command：03 提案／確認／終止／指派、04 建立／發布／撤回、05 草稿／送出／重送／重開、06 送分／退回／更正、07 建版／同意／退回、09 草稿編輯）、同步屏障競態測試、讀取與匯出測試、操作證據。**非範圍**：修各模組（若發現漏 `FOR SHARE` 檢查，開缺陷回原切片，不在本票修）。
- **前置**：S13-04、S00-03、E11 出場。
- **路徑**：正常／封存後讀取、歷史、成績匯出與簽核匯出成功；拒絕／表列每個寫入 `COHORT_ARCHIVED` 且該模組資料 hash 不變；重試／封存前成功的 requestId 於封存後重播回 `COHORT_ARCHIVED`（不回原回執、不重做）。
- **自動測試**：整合（同步屏障，S00-03）：送出用例已取 `FOR SHARE` 且通過檢查→封存等待→送出 commit→封存成功，`submission_versions` 多一筆且封存事件在後；反向：封存先取 `FOR UPDATE`→送出取 `FOR SHARE` 等待→封存 commit→送出 `COHORT_ARCHIVED`；表驅動：每個寫入 command 在 archived 屆回 `COHORT_ARCHIVED`；封存後 `SubmissionQuery`、成績表、簽核歷史可讀；成績與簽核匯出可產生（見問題 12）；帳本重播；解封後過期項目送出 `DEADLINE_PASSED`；學生舊頁（封存前載入）送出 `COHORT_ARCHIVED` 且回執頁顯示契約 02 §3 的「home 或 reload」。
- **操作證據**：S01 舊頁存草稿的 `COHORT_ARCHIVED` 回應與畫面、A1 查 115 成績與匯出檔、B06 步驟 2 的 `DEADLINE_PASSED` 回應、競態測試輸出（存 `steps/S13/S13-05/`）。
- **完成定義**：P09 步驟 13–14、B06 步驟 2、7 可走；表驅動清單與各模組 §5 的寫入 command 一一對照附在票上。
- **待 Roy／平台**：BLOCKED D-08：封存後匯出（成績、簽核）會新增 `stored_files`／`file_references`／`signoff_exports` 列，是否算允許的寫入例外。

### S13-06 下一屆 116-TEST 建立、兩屆並行與跨屆隔離
- **目標**：管理員建 116-TEST（籌備中）→設階段與年度結束日→啟用（進行中）→把預設工作屆別與開放註冊屆別切到 116（同交易舊清新設）；115 與 116 同時 active：115 學生首頁與待辦沒有 116 項目、116 組別 0、新註冊者進 116；學生對他屆項目直接請求 `FORBIDDEN`；兩屆的 `due_work` 與通知各自佇列不混。
- **spec**：模組 02 §2「同時可多個 active；兩旗標各最多一個 true（部分唯一）」、§3 create／activate／設旗標列、§5 `CohortCommand.create／activate／setDefaultWorking／setRegistrationOpen`、§9「學生預設看自己屆別」、§10「兩個 active 屆並存佇列不混、旗標部分唯一」；契約 01 §4.2 部分唯一索引；契約 03 §1「`cohort_id` 必比對」、§4 直接請求；模組 04 §5 `ItemQuery.listForActor`；劇本 P09 步驟 15；B06 步驟 0、6；案例 COH-06（追溯）、COH-01（子步驟）。
- **範圍**：屆別管理頁的完整流程（沿 S01-06 的建立／啟用／旗標、S02-04 的階段、S13-01 的切換器）、學生首頁與待辦的跨屆隔離測試、直接請求測試、`due_work`／`notifications` 分屆測試、P09 步驟 15 與 B06 步驟 0、6 的操作證據。**非範圍**：新的 port（沒有）；跨屆調整（S13-07）；通知匣篩選 UI（S13-08）。
- **前置**：S13-01、S01-06、S02-05、E08 出場。
- **路徑**：正常／116 建立→啟用→旗標切換，`cohorts` 兩列旗標各只在 116；115 學生首頁無 116 項目；SX1 註冊落在 116；拒絕／S01 直接請求 116 項目 `FORBIDDEN`；SX1（116）直接請求 115 項目 `FORBIDDEN`；116 未設階段就啟用 `VALIDATION_FAILED`（S02 起規則）；重試／旗標切換同 `requestId` 重播。
- **自動測試**：整合：兩 active 屆各發一個項目→兩屆學生的 `listForActor` 互不包含；兩屆各排一筆 `due_work`→推鐘後各自 done、彼此 subject 不混；兩屆各發事件→通知只到各自收件人；旗標切換後 `registrationOpen()`／`defaultWorking()` 回 116 且部分唯一無違反；跨屆直接請求 `FORBIDDEN`；116 組別數為 0 的 query。
- **操作證據**：116 建立回執、屆別列表截圖（兩屆並行、旗標在 116）、S01 首頁截圖（無 116 項目）、`cohorts` 兩列 SQL、直接請求回應（存 `steps/S13/S13-06/`）。
- **完成定義**：P09 步驟 15、B06 步驟 0、6 可走；上述測試綠。
- **待 Roy／平台**：學生是否可能同時屬兩屆（`Actor.cohortMemberships[]` 是陣列、`user_profiles.cohort_id` 單值），見問題 9。

### S13-07 [BLOCKED D-02] 跨屆調整（學生換屆）
- **目標**：管理員填理由把學生從 116 調到 115（目標屆 active 或 preparing、已解封亦可）：同交易更新學生所屬屆別與占用（同屆同學號唯一）、呼叫 05 `removeFromRoster` 移出原屆有效名單、新屆截止前名單依規則加入；原屆待辦消失、新屆待辦出現；原屆歷史（回答、版本、名單移除紀錄）保留可查；audit 與帳本；跨屆後對原屆項目直接請求 `FORBIDDEN`。
- **spec**：模組 05 §3「名單成員 removed：管理員／受眾變更／停用／跨屆」、§6「停用／跨屆同交易 `removeFromRoster`」；模組 01 §2「同屆同學號只能有一個 active 學生（占用表唯一）」、附錄 A `user_profiles.cohort_id`、`student_identities (cohort_id, student_no)` UNIQUE；契約 01 §5（`student_identities` D 給 `fju_app`）；契約 03 §1「被移出後依當下有效關係判斷，歷史關係只用於追溯」；劇本 B06 步驟 3；B03 步驟 10（子步驟）；案例 COH-08（追溯；責任表歸模組 02）。
- **範圍**：用例（port 名待 spec，見問題 1）、帳號管理列表的「調整屆別」對話框（掛在 S01-16 的帳號頁）、名單同步呼叫、audit、帳本、隔離測試。**非範圍**：組別成員的跨屆處理（spec 未寫；本票對已在有效組別的學生拒 `VALIDATION_FAILED` 並列問題 1）；名單移除本體（E08 出場的 `removeFromRoster`）。
- **前置**：S13-06、S01-10、E08 出場。
- **路徑**：正常／116→115 成功：`user_profiles.cohort_id` 改、`student_identities` 舊列刪新列插、116 名單列 `removed`、115 截止前名單加入、待辦切換；拒絕／目標屆 archived `COHORT_ARCHIVED`、目標屆同學號已占用 `STUDENT_NO_TAKEN`、理由空 `VALIDATION_FAILED`、學生在有效組別 `VALIDATION_FAILED`（待定）、非 admin `FORBIDDEN`；重試／同 `requestId` 回原回執。
- **自動測試**（T05）：整合：調整後重載／重登學生首頁只見 115 待辦、116 待辦消失；116 的回答與版本列不變（hash）且管理員可查；`student_identities` 唯一鍵在並發兩次調整只成功一次；SX1 直接請求 116 項目 `FORBIDDEN`；非 admin 直接請求 `FORBIDDEN` 且資料不變；同交易失敗注入（名單同步失敗）整體回滾。
- **操作證據**：調整回執、SX1 兩屆待辦前後截圖、`user_profiles`／`student_identities`／`response_rosters` SQL、直接請求回應（存 `steps/S13/S13-07/`）。
- **完成定義**：B06 步驟 3 可走；上述測試綠。
- **待 Roy／平台**：BLOCKED D-02：跨屆調整政策——已在有效組別的學生能否換屆、port 與歸屬模組（模組 01／02／05 皆未定義）；未決前不以「拒 `VALIDATION_FAILED`」施工。

### S13-08 通知匣跨屆篩選與封存屆已讀
- **目標**：通知匣預設顯示本人所有屆別的通知與全站通知，可依屆別篩選（預設值跟 S13-01 的 `ui.cohort`，可改回「全部」）；已封存屆的通知列渲染為「已封存（唯讀）」遮罩但仍可標已讀；`InboxQuery.list(userId, {cohortId?, cursor})` 的 `cohortId` 篩選生效。
- **spec**：模組 08 §2「通知匣預設顯示本人所有屆別與全站通知，可篩屆別；封存後仍可標已讀」、「渲染時逐筆重驗來源；…封存唯讀」、§5 `InboxQuery.list(userId, {cohortId?, cursor})`、§7「跨屆篩選」、失權顯示「已封存（唯讀）」、§7.2 缺口、附錄 A `notifications.scope`／`cohort_id`（跨屆篩選）、`read_at`；模組 02 §2「封存後只讀」；劇本 B06 步驟 8；P09 步驟 16（子步驟）；案例 NTF-20（追溯）。
- **範圍**：`cohortId` 篩選、篩選 UI（掛在 S02-08 的通知匣頁）、封存屆的遮罩態（在 S02-08 渲染重驗框架內加「來源屆別 archived → 已封存（唯讀）」判斷）、已讀行為在封存屆的測試。**非範圍**：失權「此項目目前無法存取」與撤回「來源已撤回」兩態（S12-10）；已讀本體（S02-08）；投影（S02-06）。
- **前置**：S13-04、S02-08。
- **路徑**：正常／不帶篩選列 115＋116＋global；篩 115 只列 115＋global（global 是否隨篩選顯示依模組 08 §2「全站通知」預設顯示，本票篩選時仍顯示 global）；封存屆通知顯示遮罩、點入導向該屆唯讀頁；拒絕／篩他人不可見屆別回空；重試／已讀同 S02-08。
- **自動測試**（T05）：整合：篩選只回該屆＋global；封存 115 後其通知列 `read_at` 可寫且重載／重登保存、`unreadCount` 減一；另一帳號的列不受影響；用別人的通知 ID `markRead` 不改 `read_at`；直接 HTTP 無 session 被拒；封存屆通知渲染文案為「已封存（唯讀）」且不改通知列本身。
- **操作證據**：通知匣兩屆全部、篩 115、篩 116 三張截圖；封存屆通知標已讀前後截圖與 `SELECT read_at`（存 `steps/S13/S13-08/`）。
- **完成定義**：B06 步驟 8、P09 步驟 16 可走；上述測試綠。
- **待 Roy／平台**：「已封存（唯讀）」遮罩在 S12-10 與本票重疊，且 S12 不是 S13 前置，需 Roy 定一處；見問題 7。

### S13-09 業務鐘倒退
- **目標**：staging 管理員把業務鐘設回較早時刻後：已由 `proposal_expiry` 作廢的提案不復活、已做的截止快照與逾期彙整不重做（同 `deadline_version` 的 `due_work` 已 done 不重跑）、送出的截止判定用當前（回撥後）業務時間所以可再送出、`deadline_version` 不變、`business_clock_overrides` 記前後值與原因；重置回真實時間仍留紀錄。
- **spec**：模組 02 §2 `BusinessClock`、§3 clock override 列、§6「override insert-only 取最新；每次記操作者、原因、真實時間、前後業務時間」、§10「鐘換算」、§11「B06（模擬鐘倒退）」、附錄 A `business_clock_overrides`；模組 08 §6 到期迴圈、§10「due_work 跳月與回撥」；契約 01 §4.7「回撥不重跑 done」、§9；母 spec §4.10「回撥：done 的不會重跑；同一 `deadline_version` 不重複產生快照或通知；再次跨越只有在期限被改成新版本時才有新列」、§4.11「準時只看 `receivedBusinessAt`」；模組 03 §3 終止與 `due_work(proposal_expiry)`；模組 05 §2 送出守門 `DEADLINE_PASSED`；手冊 §5；劇本 B06 步驟 4、5；案例 COH-10（追溯）。
- **範圍**：跨模組整合測試（03 提案作廢、05 快照、08 彙整）、回撥後再送出的行為測試、B06 步驟 4–5 的操作證據（含 Fable 停／啟 worker）、手冊 §5 重置操作證據。**非範圍**：模擬鐘頁與 override 寫入（S02-02）；tick 與回撥不重跑 done 的基礎測試（S02-07，本票以真實 handler 重驗）；production 404（S02-02；SOP 06 預演證明由 S14）。
- **前置**：S02-02、S02-07、E08 出場。
- **路徑**：正常／設 11-16 23:59:50→FX 截止 11-16 23:59→停 worker→推 11-17 00:02→啟 worker→快照與彙整各一次→回撥 11-16 23:00→提案不復活、快照不重做、FX 可再送、`deadline_version` 不變；拒絕／production 無路由（S02-02）；重試／再推過截止不再產生第二筆快照（同版本去重）。
- **自動測試**：整合：作廢提案在回撥後 `status` 不變且 `proposal_occupancy` 不重建；`due_work(deadline_snapshot|overdue_digest)` done 列在回撥後不被再次認領、`roster_snapshots` 仍一筆；回撥後送出成功且 `receivedBusinessAt` 早於截止、`deadline_version` 不變；再推過截止無新 `due_work` 列；`business_clock_overrides` 兩筆含 `previous_business_at` 與 `reason`；`fju_app` 對 `business_clock_overrides` UPDATE 被拒。
- **操作證據**：鐘紀錄 SQL、worker 日誌（停啟窗口）、`due_work` 兩筆 done、回撥後送出回執、提案狀態 SQL（存 `steps/S13/S13-09/`）。
- **完成定義**：B06 步驟 4、5 可走；上述測試綠；回撥後「快照之後的送出」如何對帳未定義者記入問題 8，不算缺陷（B06 §9）。
- **待 Roy／平台**：無（回撥後送出與快照對帳的產品規則見問題 8）。

## 紙上依賴核對
- S13-01：資料表←（無新表；`cohorts` S00-04／S02-01）；port／工具←（`CohortStatusQuery` S01-06、狀態事件 S02-05）；頁面入口←（S01-04 殼、S01-06 屆別頁）；劇本步驟可走：否（只有切換與橫幅，封存在 S13-03）。
- S13-02：資料表←（05／06／07 表由 E08／E10／E11）；port／工具←（`UnfinishedWorkQuery` 06 E10 出場、07 E11 出場、05 本票）；頁面入口←（S01-06 屆別頁＋S13-01）；劇本步驟可走：是（P09 步驟 11）。
- S13-03：資料表←（`cohort_status_events`、`cohorts` 擴充欄 S02-01；`due_work` S00-04）；port／工具←（`AuditWriter`／`OperationLedger` S01-03、`DueWorkScheduler.cancel` S02-03）；頁面入口←（S13-02 對話框）；劇本步驟可走：是（P09 步驟 12）。
- S13-04：資料表←（S02-01）；port／工具←（S13-03）；頁面入口←（S13-01 橫幅、S01-06 屆別頁）；劇本步驟可走：是（B06 步驟 1、7）。
- S13-05：資料表←（各模組表 E11 出場前全部存在）；port／工具←（同步屏障 S00-03、各模組 command E11 出場）；頁面入口←（各模組頁）；劇本步驟可走：是（P09 步驟 13–14、B06 步驟 2、7）。
- S13-06：資料表←（`cohorts` S00-04／S02-01、`cohort_stages` S02-01）；port／工具←（`CohortCommand` S01-06、`StageQuery` S02-04、`ItemQuery.listForActor` E08 出場）；頁面入口←（S01-06、S13-01）；劇本步驟可走：是（P09 步驟 15、B06 步驟 0、6）。
- S13-07：資料表←（`user_profiles`、`student_identities` S01-01；`response_rosters` E08 出場）；port／工具←（`removeFromRoster` E08 出場、核准流程 S01-10、帳本 S01-03；本票 port 待 spec）；頁面入口←（S01-16 帳號列表）；劇本步驟可走：是（B06 步驟 3；port 名定案後）。
- S13-08：資料表←（`notifications` S02-01）；port／工具←（`InboxQuery`／`InboxCommand` S02-08、封存狀態 S13-04）；頁面入口←（S02-08 通知匣頁）；劇本步驟可走：是（B06 步驟 8、P09 步驟 16）。
- S13-09：資料表←（`business_clock_overrides` S02-01、`due_work` S00-04）；port／工具←（`setBusinessClock` S02-02、tick S02-07、handler E08 出場）；頁面入口←（S02-02 模擬鐘頁）；劇本步驟可走：是（B06 步驟 4–5；停啟 worker 由 Fable）。

## 讀 spec 時發現的問題（不改 spec，只列）
1. **跨屆調整（COH-08）沒有任何 port**：責任表把 COH-08 歸模組 02、S13，但模組 02 §5 `CohortCommand` 沒有換屆方法；模組 01 §5 也沒有（資料 `user_profiles.cohort_id`、`student_identities` 在模組 01 附錄 A）；模組 05 §3／§6 只寫「跨屆同交易 `removeFromRoster`」。另外學生已在有效組別（`group_memberships`）時能否換屆、換屆後原屆組別如何處理，模組 03 沒寫。S13-07 暫依 B06 步驟 3 的預期行為起草，port 名與歸屬模組請 Roy 定。
2. **模組 05 的 `UnfinishedWorkQuery` 實作沒有切片交付**：S10 票草稿問題 8 已指出 06 在 S10-13、07 在 S11-08；05 的版本 S08 卡與 S13 卡都沒列（S13 卡只在「schema／API／狀態依賴」寫「三實作」）。本稿放 S13-02，若改放 S08 請同步改 S13-02 前置為 `E08 出場`。
3. **封存預覽沒有對外 query 名稱**：模組 02 §3 只寫封存前置「影響預覽」、§7 有「封存預覽對話框」、§5 只有內部 `UnfinishedWorkQuery.list(tx, cohortId)`；管理員讀預覽走哪個 query（`CohortStatusQuery` 擴充或新 query）未寫。S13-02 不命名，待 spec 補。
4. **屆別切換器位置與可見集合**：模組 02 §9「老師／管理員切換屆別（UI 狀態）」、契約 02「`ui.cohort` 只選畫面」、§7.2 列為缺口，但 §5 `CohortStatusQuery.listForActor(actor)` 的「老師可見哪些屆別」（有指派的屆？預設工作屆？全部 active？）未定義；S13-01 暫寫「有指派或指導關係的屆別＋預設工作屆別」。
5. **封存取消 pending `due_work`，解封不恢復**：模組 02 §3 封存「該屆 pending `due_work` 改 cancelled」，§2 解封「不延長期限」但沒寫 `due_work` 是否恢復；B06 步驟 1 解封後步驟 4 只用新建的 FX，所以劇本測不到舊項目。若不恢復，解封後既有項目的截止快照與逾期彙整不會再產生；S13-04 依字面「不恢復」起草，請 Roy 確認。
6. **`cohort_status_events.reason` NOT NULL 但 create／activate 沒有理由**：附錄 A 寫「封存與解封必填」、欄位 NULL＝NO；S02-05 的籌備→進行中事件必須填某個值。屬 S02 範圍，此處只記錄影響 S13-06（116 啟用）。
7. **「已封存（唯讀）」遮罩重疊且依賴方向不符**：模組 08 §7 遮罩三態含「已封存（唯讀）」；S12-10 範圍寫「遮罩三態」、非範圍寫「跨屆篩選與封存已讀（S13）」；總圖 S13 前置只有 S08（不含 S12）。S13-08 只能在 S02-08 框架上自己加封存態；請定一處（建議封存態歸 S13-08、S12-10 只做失權與撤回）。
8. **回撥後再送出與快照對帳的關係未定義**：B06 步驟 5「回撥後可再送但 `deadline_version` 不變，快照不重做」；模組 05 的 `snapshot_reconcile` 只處理「快照已讀未 commit／送出已 commit 未後處理／worker 中斷」，沒有「快照之後（業務時間倒退）再送出」的分類——這筆送出算準時（`receivedBusinessAt` 早於截止）但不在快照內。B06 §9 說規格未寫回產品，不算缺陷；請 Roy 決定是否要寫進模組 05 §2。
9. **學生同時屬兩屆的資料形狀**：模組 01 §5 `Actor{cohortMemberships[]}` 是陣列，附錄 A `user_profiles.cohort_id` 單值、`student_identities` 每屆一列；跨屆調整後「歷史保留」是指舊屆資料仍可查，還是 `cohortMemberships` 保留舊屆？影響 S13-06／07 的隔離測試與 `listForActor`。
10. **COH-08 的責任模組**：責任表寫模組 02，但換屆改的表全在模組 01（與 05 的名單）；母 spec §4.4 模組邊界規定表由擁有者寫。請確認歸 01 還是 02（影響 port 放哪個 command）。
11. **封存沒有預覽基準**：`archive(cohortId, revision, reason, requestId)` 只帶 `cohorts.revision`；預覽到封存之間若有新送出或新草稿，`unfinished_summary` 會與實際不符（模組 06 的改派用 `basis_hash` 防這種情況）。S13-03 依字面起草（只檢 revision），請確認是否要求重新預覽。
12. **封存後匯出是否算寫入**：P09 步驟 14「查 115 匯出：可查可匯出」，但匯出會新增 `stored_files`（purpose `export`）、`file_references`、`signoff_exports` 列；契約 03 §1「已封存只讀」與母 spec §4.7「封存 × 任何寫入」沒有把匯出列為例外。S13-05 依 P09 預期「可匯出」測，請 Roy 確認例外清單（匯出、已讀、audit）。
13. **B06 步驟 0 的註冊時點**：SX1 在 P09 副本內註冊到 116 需要 116 已是開放註冊屆別（P09 步驟 15 已設）且 116 的名單含 SX1 學號（模組 01 核准需名單或當面證件）；B06 §2 沒寫 116 名單怎麼來，S13-06 假設 A1 匯入 116 名單或以「當面證件」核准。
