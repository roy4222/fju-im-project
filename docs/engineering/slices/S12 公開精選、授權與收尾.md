---
type: slice
slice: S12
project: FJU IM Project
updated: 2026-09-12
status: written-v2.1-pending-review
---
# S12｜公開精選與授權閘門、素材核閱、歷屆補登、SEO、共用 UI、完整事件矩陣、GC、檔案工作台與備份狀態頁

## 成果
精選發布經公開閘門（當屆簽核版本或歷屆外部授權、素材 checksum 與人工核閱一致）與文字個資檢查；素材登記與核閱；歷屆補登外部依據；授權失效即擋並自動撤稿；sitemap／robots／OG；403 下一步、空頁、表格一致、手機 360、鍵盤；補齊事件矩陣剩餘事件與失權遮罩；檔案 GC handler；管理員檔案工作台真實用量、引用保護、稽核查詢、備份狀態頁；備份失敗通知。

## 模組與前置
09、08、10；前置 S11。

## 進場資料與產生方式
主線 G1 成果；示範素材（標示為模擬且已核閱才可用；不用系網照片）。

## schema／API／狀態依賴
模組 09 附錄 A；`ShowcaseCommand／PublicationGate／PublicContentQuery／PageMetaQuery`；07 `PublicationAuthorizationQuery`；`due_work(file_gc, receipt_purge)`；`OpsStatusQuery`。

## 責任範圍、公開介面、跨模組協議
閘門每請求同步判定；授權失效由 07 事件觸發 worker 撤稿與 tag 失效。頁面：公開精選、`/dashboard/admin/showcase`、檔案工作台、通知匣完整。

## 正常與拒絕路徑；重試／回滾／恢復
拒絕：`AUTHORIZATION_NOT_COVERED`、`PII_DETECTED`、`ASSET_NOT_APPROVED`、`FILE_REFERENCED`。恢復：重新發布重檢；GC 只碰無引用且超過 24 小時者。

## 最終責任案例、子步驟、自動測試、Codex、四欄
- 最終責任案例：SHW-03、04、06、07、08、09、10、11；NTF-07、14；FIL-01、02、07。SHW-08 手機真機部分為人工 Roy。
- 子步驟案例：無。
- 自動：涵蓋判定、閘門兩種來源、素材 checksum 不符、個資正則、GC 條件、白名單 DTO、停 worker 直接請求、匯出安全（XLSX 文字儲存格）。
- Codex：劇本 P09、B07、B08；證據：公開頁 HTML 掃描、拒絕回應、GC 結果、檔案工作台截圖、稽核查詢。
- 切片出場操作起點（v2.1）：總圖「切片出場與年度案例 PASS 的分界」表的 S12 列；證據存 `steps/S12/`；不以案例 PASS 為出場條件。

## 出場條件（環境：staging＋人工）
本切片自動測試綠；操作起點走通並留 `steps/S12/`；Fable review；Roy 看公開頁與素材來源欄（SHW-08 真機由 Roy）。案例 PASS 在完整年度判定。

## 未涵蓋
真實素材（校方）。

## 依賴
校方：SHW-01a／b／c（阻塞正式用途 G5，不阻塞測試素材）。

## 狀態
文件已寫｜待 review｜可實作：否（待前置切片完成與 review）｜實作完成：否｜自動驗證：否｜瀏覽器：否｜VM：否｜Roy 接受：否
