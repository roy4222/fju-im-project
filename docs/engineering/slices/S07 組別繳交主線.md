---
type: slice
slice: S07
project: FJU IM Project
updated: 2026-09-12
status: written-v2.1-pending-review
---
# S07｜組別共用草稿、上傳／下載、正式送出、歷史、老師矩陣（主線終點）

## 成果
G1 成員 S01 儲存共用草稿，S02 看到同一草稿；同版競爭保存被拒；上傳 PDF（合法／偽裝／超限）；S03 正式送出產生 v1，全組、T1（主指導）、A1 看同一版本與附件；S01 重送 v2，v1 不變；G2 的 S06 直接讀 G1 被拒；老師矩陣只列有效指導關係的組別；成組後個人 F1 回答仍各自保留；其他組員收到站內通知；本人唯讀頁與私有附件存取。

## 模組與前置
05、10、08、03（membership、advisor）；前置 S03、S04、S05、**S06**（老師矩陣與主指導閱覽需要有效指導關係）。

## 進場資料與產生方式
S04 發布 MID1（組別收件、PDF 20 MiB）；S03 的 G1／G2／G3；S06 的主指導 T1／T2／T3。

## schema／API／狀態依賴
`submission_files`、`file_references`、`stored_files`、`advisor_visibility_settings`；`FileStorage` 補齊（S01 最小能力之上：attach 鎖、繳交檔案下載授權鏈、GC 排程但 handler 在 S12）；`SubmissionCommand.submit`（組別）；事件 submission.submitted；`AdvisorQuery.groupsOf`。

## 責任範圍、公開介面、跨模組協議
母 spec §4.17 全流程；Caddy 上傳上限；下載 Route Handler；老師矩陣依授權矩陣（契約 03 §1）。頁面：`/items/[id]` 組別版（共用草稿、上傳進度、收件章回執、繳交歷史）、`/dashboard/teacher/submissions`。

## 正常與拒絕路徑；重試／回滾／恢復
拒絕：`NOT_MEMBER`、`FILE_NOT_OWNED`、`FILE_NOT_READY`、`FILE_TYPE_REJECTED`、`FILE_TOO_LARGE`、`CONFLICT`、`DEADLINE_PASSED`。恢復：上傳中斷殘留由 GC；finalize 後 DB 失敗使用者重送；回應遺失查回執。

## 最終責任案例、子步驟、自動測試、Codex、四欄
- 最終責任案例：SUB-03、05、06、07、08、09、10、17、25；FIL-03、04；NTF-02。
- 子步驟案例：SUB-18 完成率顯示（最終 S08）。
- 自動：兩人同版、同 requestId、封存×送出（S13 前以人工封存旗標測）、移出×送出（S09 前以直接改 DB 模擬）、GC×attach、檔案不屬本組、下載授權鏈。
- Codex：劇本 P05、P06；證據：回執、版本 hash、S06 直接請求回應、通知匣。
- 切片出場操作起點（v2.1）：總圖「切片出場與年度案例 PASS 的分界」表的 S07 列；證據存 `steps/S07/`；不以案例 PASS 為出場條件。

## 出場條件（環境：staging）
本切片自動測試綠；操作起點走通並留 `steps/S07/`；Fable review；Roy 看繳交與回執。案例 PASS 在完整年度判定。

## 未涵蓋
截止快照與重開（S08）。

## 依賴
無。

## 狀態
文件已寫｜待 review｜可實作：否（待前置切片完成與 review）｜實作完成：否｜自動驗證：否｜瀏覽器：否｜VM：否｜Roy 接受：否
