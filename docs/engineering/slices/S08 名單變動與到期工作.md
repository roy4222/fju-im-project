---
type: slice
slice: S08
project: FJU IM Project
updated: 2026-09-12
status: written-v2.1-pending-review
---

> **2026-09-16 導覽更新**：本頁保存原切片規格與歷史狀態。即時進度與 session 分工看 [進度與工作包](</Users/lubaiyu/Documents/roy422的人生online/專案/🌐 網站與互動/📁 輔大資管系專題網站/🛠️ 工程開發/🧱 實作切片/S08 名單變動與到期工作/00 進度與工作包.md>)；開工依 [並行規則](</Users/lubaiyu/Documents/roy422的人生online/專案/🌐 網站與互動/📁 輔大資管系專題網站/🛠️ 工程開發/🧱 實作切片/02 Session 並行與交付規則.md>)。原有產品接受條件不變。

# S08｜名單變動、截止快照、指定重開、改期、改結構、逾期彙整、到期工作全套

## 成果
截止前新核准者自動進名單；截止後不自動追加；管理員加入（可一併重開）、免填／恢復、移出（分子分母重算）、加回（版本檢查）；截止當刻快照與寬限、晚 commit 對帳註記；指定個人／組別重開與過期頁面拒絕；改期與通知；改結構遷移預覽與草稿 needs_review；逾期彙整；完成率與徽章最終；主指導閱覽設定；worker 到期工作全套與補建。

## 模組與前置
05、08、04；前置 S07。

## 進場資料與產生方式
主線資料；分支帳號 SX1／SX2／SX3（隔離副本內註冊核准）。

## schema／API／狀態依賴
`roster_snapshots`、`snapshot_annotations`、`targeted_reopens`、`digest_events`；`due_work` handlers：`deadline_snapshot`、`snapshot_reconcile`、`overdue_digest`、`stage_end_unassigned`；`SubmissionCommand` 名單方法與 `reopen`、`setAdvisorVisibility`；`ItemCommand.changeDeadline／previewSchemaChange／applySchemaChange／previewAudienceChange`。

## 責任範圍、公開介面、跨模組協議
01 核准用例同交易呼叫 `syncRoster`（S01 的 no-op 在此換成真實作）；快照寬限 60 秒；`snapshot_reconcile` 快照不存在時保持 pending；彙整依實際有效期限排除免填與移出。頁面：名單頁完整（免填、移出、加回、快照）、重開對話框（含通知範圍）、改期、改結構預覽。

## 正常與拒絕路徑；重試／回滾／恢復
拒絕：對停用者重開不給填寫權；恢復應填已截止不顯示可填；加回遇新版顯示需補交；截止後舊頁送出 `DEADLINE_PASSED`。恢復：worker 停機補跑；同 deadline_version 不重複；毒事件重跑。

## 最終責任案例、子步驟、自動測試、Codex、四欄
- 最終責任案例：SUB-11、12、13、16、18、19、20、21、22、23、24；COH-07；PUB-08、09；NTF-03、04、16、18、19。
- 子步驟案例：無。
- 自動：分母重算、快照寬限與對帳三情況、彙整排除、handler 冪等、改期取消舊 due_work、投影補建。
- Codex：劇本 P06、P07、B03、B08（NTF-19）；證據：名單頁前後、`roster_snapshots`、`snapshot_annotations`、通知內容、`due_work` 列。
- 切片出場操作起點（v2.1）：總圖「切片出場與年度案例 PASS 的分界」表的 S08 列；證據存 `steps/S08/`；不以案例 PASS 為出場條件。

## 出場條件（環境：staging）
本切片自動測試綠（含快照對帳、worker 中斷恢復）；操作起點走通並留 `steps/S08/`；Fable review。案例 PASS 在完整年度判定。

## 未涵蓋
解散導致的名單變動（S09）。

## 依賴
無。

## 狀態
文件已寫｜待 review｜可實作：否（待前置切片完成與 review）｜實作完成：否｜自動驗證：否｜瀏覽器：否｜VM：否｜Roy 接受：否
