---
type: slice
slice: S05
project: FJU IM Project
updated: 2026-09-12
status: written-v2.1-pending-review
---
# S05｜個人收件：草稿、送出、版本、名單頁、完成率

## 成果
合資格學生填個人收件（F1）、儲存草稿（revision）、正式送出、截止前重送；管理員看名單頁（目前／免填／已移出分類的讀取）、完成率與回答明細；本人在 `/dashboard/student/my-submissions` 看自己的版本歷史；結果未知可查回執；尚未開放的項目不可填。

## 模組與前置
05、10（ledger、audit）；前置 S04（名單表已由 S04 建立）。

## 進場資料與產生方式
S04 發布的 F1；13 位有效學生。

## schema／API／狀態依賴
模組 05 附錄 A：`drafts`、`submission_versions`（個人）；`response_rosters` 已存在；`SubmissionCommand.saveDraft／submit`、`SubmissionQuery`（含 `completion`）、`getOperationResult`。

## 責任範圍、公開介面、跨模組協議
母 spec §4.9 帳本與 §4.17 守門（個人版）；接收時間取樣；分母＝目前有效且非免填（免填與移出的寫入在 S08，本切片只讀）。頁面：`/dashboard/student/items`、`/items/[id]`（個人）、`/dashboard/student/my-submissions`、`/dashboard/admin/items/[id]` 名單與明細。

## 正常與拒絕路徑；重試／回滾／恢復
拒絕：`NOT_IN_ROSTER`、`ITEM_NOT_OPEN`、`DEADLINE_PASSED`、`CONFLICT`、`SCHEMA_INVALID`、`REQUEST_MISMATCH`。恢復：斷線查回執；連點回原回執。

## 最終責任案例、子步驟、自動測試、Codex、四欄
- 最終責任案例：SUB-01、02、04、14、15。
- 子步驟案例：SUB-19 的個人重送（最終 S08）；SUB-22 免填練習（最終 S08）。
- 自動：截止分鐘含入、帳本重播、兩人同 requestId、schema 驗證、分母讀取。
- Codex：劇本 P03、P07（SUB-04）；證據：回執、`submission_versions`、名單頁、重登入後同版本。
- 切片出場操作起點（v2.1）：總圖「切片出場與年度案例 PASS 的分界」表的 S05 列；證據存 `steps/S05/`；不以案例 PASS 為出場條件。

## 出場條件（環境：staging）
本切片自動測試綠；操作起點走通並留 `steps/S05/`；Fable review。案例 PASS 在完整年度判定。

## 未涵蓋
組別、附件（S07）、名單變動與快照（S08）。

## 依賴
無。

## 狀態
文件已寫｜待 review｜可實作：否（待前置切片完成與 review）｜實作完成：否｜自動驗證：否｜瀏覽器：否｜VM：否｜Roy 接受：否
