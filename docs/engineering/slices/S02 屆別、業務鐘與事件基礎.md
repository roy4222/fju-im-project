---
type: slice
slice: S02
project: FJU IM Project
updated: 2026-09-13
status: written-v2.2-pending-review
responsibility: 01 案例責任表
---

> **2026-09-16 導覽更新**：本頁保存原切片規格與歷史狀態。即時進度與 session 分工看 [進度與工作包](</Users/lubaiyu/Documents/roy422的人生online/專案/🌐 網站與互動/📁 輔大資管系專題網站/🛠️ 工程開發/🧱 實作切片/S02 屆別、業務鐘與事件基礎/00 進度與工作包.md>)；開工依 [並行規則](</Users/lubaiyu/Documents/roy422的人生online/專案/🌐 網站與互動/📁 輔大資管系專題網站/🛠️ 工程開發/🧱 實作切片/02 Session 並行與交付規則.md>)。原有產品接受條件不變。

# S02｜屆別、階段、業務鐘、事件表與 worker、通知匣

## 成果
A1 建 115-TEST（籌備中→進行中）、四個階段開始日與年度結束日、預設工作與開放註冊旗標；staging 可設模擬業務日期到秒並留紀錄；worker 服務投影事件到通知匣（以管理端測試事件 `test.notification` 驗證；帳號核准事件依產品規則不投影，驗證其不出現）；三角色首頁顯示「尚未開始／階段 n／已結束」。

## 模組與前置
02、08；前置 S01。

## 進場資料與產生方式
由 UI 建屆別；模擬鐘只在 staging 環境變數開啟。

## schema／API／狀態依賴
契約 01 §3「02」「08」表；`BusinessClock`、`CohortStatusQuery`、`StageQuery`、`CohortCommand`、`EventPublisher`、`DueWorkScheduler`、`InboxQuery／Command`；worker 服務與 advisory lock；`/api/health` 加 `worker.lastTickAt`。

## 責任範圍、公開介面、跨模組協議
S01 的 `REGISTRATION_COHORT_CODE` 改讀屆別旗標；`OperationContext` 加 `receivedBusinessAt`；通知列渲染重驗權限的框架（此時只有系統事件）；到期工作 tick 與 handler 註冊表（handler 本切片只有 `test_noop`：契約 01 §4.7 的合法 kind，只在模擬鐘環境註冊）；`session_revocations` 的 worker 重試。

## 正常與拒絕路徑；重試／回滾／恢復
開始日不遞增 `VALIDATION_FAILED`；production 模擬鐘 404；旗標唯一違反 `CONFLICT`；worker 崩潰重啟後投影補跑不重複；業務鐘回撥不刪已 done 的 due_work。

## 最終責任案例、子步驟、自動測試、Codex、四欄
- 最終責任案例（唯一維護處：[01 案例責任表](<01 案例責任表.md>)）：COH-01–04、NTF-08。
- 自動：階段判定、鐘換算、advisory lock、投影冪等、部分唯一旗標。D1（模組 01 v2.4 附錄 A 規則 5，worker 週期收斂核對）：觀察集合（24 小時內有工作列、7 天內 `outcome_unknown`）、過期 executing 列回收為 failed／`lease_expired`／`outcome_unknown`、兩個 worker tick 同時核對只插入一筆收斂工作（部分唯一鍵③）、自動 round 到 10 後不再插入直到新狀態事件或 `manual_retry`、告警經 SOP 05 通道。
- Codex：劇本 P01（屆別、階段、活動）、P03（已讀）、P04／P05（階段推進）、P07（活動改期取消）；手冊 §5 業務鐘操作。
- 切片出場操作起點（v2.1）：總圖「切片出場與年度案例 PASS 的分界」表的 S02 列；證據存 `steps/S02/`；不以案例 PASS 為出場條件。
- 四欄證據：`business_clock_overrides`、`cohort_stages`、`event_projections`、通知匣截圖、首頁階段文字。

## 出場條件（環境：staging）／未涵蓋／依賴
出場：本切片自動測試綠；操作起點走通（`test_noop` 到期工作與核准事件投影，不需收件）並留 `steps/S02/`；Fable review；Roy 看首頁階段文字。案例 PASS 在完整年度判定（NTF-08 於 P03）。未涵蓋：封存／解封（S13）、真實業務事件（S03 起）。依賴：無外部。

## 狀態
文件已寫｜待 review｜可實作：否（待 S00–前置切片完成與 review）｜實作完成：否｜自動驗證：否｜瀏覽器：否｜VM：否｜Roy 接受：否
