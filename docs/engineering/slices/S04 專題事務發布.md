---
type: slice
slice: S04
project: FJU IM Project
updated: 2026-09-12
status: written-v2.1-pending-review
---
# S04｜專題事務建立與發布、收件名單表（歸 05）、公開前台內容頁

## 成果
管理員快速建立→完整編輯（Tiptap、欄位、附件、受眾、收件單位、綁屆別與階段、開放／截止）→發布檢查→發布；撤回（無回答）、下架、重新發布（不重設實際開放時間）、小幅修改（選擇是否通知）；訪客在公開前台看到公告與規則（讀 DB、快取與失效）；發布時同交易建收件名單並發通知；站內日曆顯示截止。

## 模組與前置
04、05（`response_rosters` 表與 `buildRoster` 最小實作：本切片交付、模組 05 擁有）、09（公開內容頁與白名單）、10（附件）；前置 S02（S03 供指定組別，未完成時指定組別功能以無組別驗證）。

## 進場資料與產生方式
A1 從 UI 建立；附件用測試檔。

## schema／API／狀態依賴
模組 04 附錄 A 全部表；模組 05 的 `response_rosters`（本切片 migration 建表）；`ItemCommand／Query`、`AudienceResolver`、`ReceiverResolver`、`PublicContentQuery`、`CalendarQuery`（02 提供、讀 04 截止）；`FileStorage` attachment 用途（沿用 S01 的最小上傳／下載；S07 補 attach 鎖與 GC）；`due_work(deadline_snapshot)` 只排程，handler 在 S08。

## 責任範圍、公開介面、跨模組協議
發布同交易呼叫 05 `buildRoster`（只建名單列，分母計算與名單頁在 S05）；公開頁 `'use cache'`＋`revalidateTag(tag,'max')`。頁面：`/dashboard/admin/items` 列表、三步驟、編輯器、詳情（含名單預覽展開）；公開 `/news`、`/rules`、`/files`（登入資源）。

## 正常與拒絕路徑；重試／回滾／恢復
拒絕：空收件、截止早於開放、指定組別未選、有回答撤回 `ITEM_HAS_RESPONSES`、revision `CONFLICT`、收件單位有回答後鎖定。恢復：發布回應遺失查回執；下架後重新發布不重設 `actual_opened_at`。

## 最終責任案例、子步驟、自動測試、Codex、四欄
- 最終責任案例：PUB-01、02、03、04、05、06、07、10、11、12、13、14；NTF-01、06、13；SHW-01、02、05。
- 子步驟案例：SUB-01 的名單存在（最終 S05）；PUB-08、09 的改期與改結構入口（最終 S08）。
- 自動：發布檢查、名單建立同交易、快取失效、富文字清理、下架重發布不重設開放時間。
- Codex：劇本 P03、P05、P06（NTF-06）；證據：發布回執、`item_publications`、名單預覽、公開頁 HTML、`notifications` 列。
- 切片出場操作起點（v2.1）：總圖「切片出場與年度案例 PASS 的分界」表的 S04 列；證據存 `steps/S04/`；不以案例 PASS 為出場條件。

## 出場條件（環境：staging）
本切片自動測試綠；操作起點走通並留 `steps/S04/`；Fable review；Roy 看發布流程。案例 PASS 在完整年度判定。

## 未涵蓋
學生填答（S05／S07）、名單變動與改結構遷移（S08）。

## 依賴
無。

## 狀態
文件已寫｜待 review｜可實作：否（待前置切片完成與 review）｜實作完成：否｜自動驗證：否｜瀏覽器：否｜VM：否｜Roy 接受：否
