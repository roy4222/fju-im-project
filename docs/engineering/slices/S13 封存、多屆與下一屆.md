---
type: slice
slice: S13
project: FJU IM Project
updated: 2026-09-12
status: written-v2.1-pending-review
responsibility: 01 案例責任表
---
# S13｜封存／解封、多屆並行、下一屆、跨屆隔離、通知匣跨屆

## 成果
封存預覽（未完成清單）與封存、解封填理由、116-TEST 建立（籌備中→進行中）與 115 並行、預設工作屆別切換、學生不混待辦、老師與管理員切換屆別、通知匣跨屆篩選、業務鐘倒退分支。

## 模組與前置
02、08、05／06／07（UnfinishedWorkQuery）；前置 S08、S10、S11。

## 進場資料與產生方式
主線完成後的資料；116 由 UI 建。

## schema／API／狀態依賴
`CohortCommand.archive／unarchive`；`UnfinishedWorkQuery` 三實作；封存 × 寫入鎖。

## 責任範圍、公開介面、跨模組協議
各模組寫入用例的 `FOR SHARE` 檢查在 S07 已做，此切片補完整測試。頁面：屆別管理、封存預覽、屆別切換器。

## 正常與拒絕路徑；重試／回滾／恢復
拒絕：封存後寫入 `COHORT_ARCHIVED`；非管理員無解封入口。恢復：解封不改期限與關係。

## 最終責任案例、子步驟、自動測試、Codex、四欄
- 最終責任案例（唯一維護處：[01 案例責任表](<01 案例責任表.md>)）：COH-05–06、COH-08–10、NTF-20。
- 自動：封存×送出競態、兩屆並行佇列、解封不變更、鐘回撥不復活。
- Codex：劇本 P09（封存、116 建立）、B06（解封、跨屆、鐘回撥、通知匣跨屆）。
- 切片出場操作起點（v2.1）：總圖「切片出場與年度案例 PASS 的分界」表的 S13 列；證據存 `steps/S13/`；不以案例 PASS 為出場條件。
- 四欄證據：狀態事件、拒絕回應、切換畫面、`business_clock_overrides`。

## 出場條件（環境：staging）／未涵蓋／依賴
出場：本切片自動測試綠；操作起點走通並留 `steps/S13/`；Fable review。出場後才開始完整年度主線（S13 出場是完整年度的前置，不是反過來）。未涵蓋：無。依賴：無。

## 狀態
文件已寫｜待 review｜可實作：否（待 S00–前置切片完成與 review）｜實作完成：否｜自動驗證：否｜瀏覽器：否｜VM：否｜Roy 接受：否
