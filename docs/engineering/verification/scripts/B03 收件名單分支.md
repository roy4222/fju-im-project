---
type: codex-script
script: B03
kind: branch
project: FJU IM Project
updated: 2026-09-12
revision: v1.1
status: NOT_RUN
---
# B03｜收件名單分支（隔離副本）

> v1.1（2026-09-12，Codex 第二輪）：起點 P05 時 F1 已截止，名單變動改在副本內新建的個人收件 FX 上做（步驟 0），並固定每步的前置與新增資料（RR07）；個人重送改在截止前（步驟 2）。

## 1. 目的與案例
截止前晚加入自動進名單、截止後不自動追加與快照、免填、移出重算、加回版本檢查、跨屆調整、個人重送與個別重開、主指導閱覽與換老師、名單事件通知。案例：SUB-19、20、21、22、23、24；COH-07；NTF-16；補證 COH-08 子步驟（最終 S13 於 B06）。

## 2. 環境、版本、前置、角色
隔離副本 `fju-branch-b03`：起點主線 P05 完成後（MID1 已發布、F1 已截止且有回答與快照）；業務鐘 2026-10-05；角色 A1、S01、S03、S09、T1、T2，副本內新註冊 SX1（步驟 1 前註冊、截止前核准）、SX2（截止後核准）；副本內由 A1 新建個人收件 FX（步驟 0），所有名單變動都在 FX 上做，F1 只用於主指導閱覽（步驟 11–12）。

## 3. 起點
A1：`/dashboard/admin/accounts` 待審。

## 4–5. 逐步操作與預期
| 步驟 | 角色 | 操作 | 預期 |
|---|---|---|---|
| 0 | A1 | 建個人收件 FX（受眾 115-TEST 學生、階段成組、截止 2026-10-06 23:59）→發布 | 名單 13 |
| 1 | A1 | 截止前核准 SX1（115-TEST） | FX 名單自動加入 SX1；分母 14（COH-07） |
| 2 | S01、S09 | 各送出 FX 版本 1；S09 修改再送 | S09 版本 2（個人重送，截止前） |
| 3 | Codex | 推鐘 2026-10-07 00:00:30 | FX 快照產生；名單頁鎖定 |
| 4 | A1 | 截止後核准 SX2 | 不自動加入 FX；名單不變 |
| 5 | A1 | 對 FX 加入 SX2 並一併重開至 10/08 23:59（理由）；SX2 送出 | 一則通知；SX2 版本 1；分母 15 |
| 6 | A1 | 對 S09 個別重開 FX 至 10/08 23:59（理由）；S09 再送 | 只 S09 通知；S09 版本 3 |
| 7 | A1 | 免填 S03（FX，未交）→恢復 | 分母 14→15；恢復時已截止不顯示可填 |
| 8 | A1 | 移出 S01（已交）→分子分母同時減 | 完成率重算 |
| 9 | A1 | 改結構（新欄位）後加回 S01 | 顯示「舊回答為舊版，需補交」；未補交前不計完成 |
| 10 | A1 | 跨屆調整 SX1 到 116（副本建 116） | 115 的名單移除、待辦消失；116 待辦不含 115（子步驟） |
| 11 | A1 | 對 F1 設「主指導可閱覽」自版本 n 起 | T1 可看 G1 成員 F1 版本≥n；舊版本不可 |
| 12 | A1 | 重派 G1→T2 | T1 立即看不到；T2 可看≥n |

## 6. 持久化序列
每步重載名單頁；SQL：`response_rosters` 區間列、`roster_snapshots`、`snapshot_annotations`（若晚 commit）、`targeted_reopens`、`advisor_visibility_settings`。

## 7. 授權與直接請求
- SX2 在步驟 5 前直接 POST submit FX：`NOT_IN_ROSTER`。
- 移出後 S01 直接 POST：`NOT_IN_ROSTER`。
- T1 在步驟 12 後直接請求：`FORBIDDEN`。
- 跨屆後 SX1 直接請求 115 項目：`FORBIDDEN`。

## 8. 證據
名單頁前後截圖、通知、回執、SQL 輸出、T1／T2 對照。

## 9. 判定
COH-07（1）、SUB-19（2、6）、SUB-21（3–5）、SUB-22（7）、SUB-20（8）、SUB-23（9）、SUB-24（11、12）、NTF-16（各步通知）；COH-08 子步驟 `steps/B03-10.json`。

## 10. 失敗後
副本丟棄重建；不回填主線。
