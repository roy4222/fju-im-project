---
type: codex-script
script: B04
kind: branch
project: FJU IM Project
updated: 2026-09-12
revision: v1.1
status: NOT_RUN
---
# B04｜評分分支（隔離副本）

> v1.1（2026-09-12，Codex 第二輪）：起點改 P08 完成後（最終結果更正已在 P08 步驟 8）；步驟 6 改為 basis_hash 衝突（另一指派送分也使預覽失效，RR10）。

## 1. 目的與案例
改派三種採計與分母、授權分別撤銷、暫存失效與停用、更正復核與過期預覽（含另一指派送分使預覽失效）、評分事件通知。案例：GRD-13、14、15；補證 NTF-17、GRD-08（更正復核）。

## 2. 環境、版本、前置、角色
隔離副本 `fju-branch-b04`：起點主線 P08 完成後（G1 期中 T1 80、T3 84.29；G3 期末 T3 80、最終結果更正 84.8→85）；角色 A1、T1、T2、T3、S01。

## 3. 起點
A1：評分指派頁。

## 4–5. 逐步操作與預期
| 步驟 | 角色 | 操作 | 預期 |
|---|---|---|---|
| 1 | A1 | 移除 G1 的 T1 指派：預覽三選一「保留」 | 80 仍採計；2／2；T1 不能再寫 |
| 2 | A1 | 對 G3 期末移除 T3 選「替換」為 T2 | T3 的期末 80→historical；期末 0／1；最終結果更正變 pending_review；管理員通知 |
| 3 | T2 | 送 G3 期末 90 | 期末 1／1；最終重算 88×0.6＋90×0.4＝88.8；待復核清單顯示 |
| 4 | A1 | 復核：沿用更正或新更正 | effective |
| 5 | A1 | 對 G2 期末選「新增」T3 | required 2；1／2 |
| 6 | A1 | 預覽移除 G2 期末的 T2（記 `basis_hash`）；預覽期間 T3（步驟 5 新增）對 G2 期末送正式 | 執行預覽→`CONFLICT`（basis_hash 已變，雖然 T2 那列未動）；重新預覽後可執行 |
| 7 | T1 | 舊頁對 G1 送分 | `NOT_ASSIGNED` |
| 8 | A1 | 停用 T3（副本） | T3 停用期間不能暫存或送出，草稿保留、已送採計、分母不變；重新啟用且原指派仍有效可接續草稿，停用期間已改派則不恢復原指派 |
| 9 | S01 | 學生頁掃描 | 無分數 |

## 6. 持久化序列
SQL：`evaluator_assignments` valid_to 與 removal_choice、`evaluation_status` 狀態、`stage_requirements.required_count`、`override_review_state`。

## 7. 授權與直接請求
T1 直接 POST submitFinal（步驟 7）；T3 停用後舊 cookie；S01 直接請求評分 API。

## 8. 證據
三選一預覽截圖、狀態變化 SQL、待復核清單、`CONFLICT`、`NOT_ASSIGNED`、掃描報告、通知。

## 9. 判定
GRD-13（1、2、5）、GRD-15（2–4、6）、GRD-14（7、8）；NTF-17、GRD-08（2–4 復核）補證。

## 10. 失敗後
副本丟棄重建；不回填主線。
