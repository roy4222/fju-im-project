---
type: codex-script
script: B01
kind: branch
project: FJU IM Project
updated: 2026-09-12
revision: v1.2
status: NOT_RUN
---
# B01｜帳號分支（隔離副本）

> v1.1（2026-09-12，Codex 第二輪）：Roy 的 Google 三案改用副本內新帳號 SG1／SG2 與 Roy 自備的 Google 測試身分 A、B（RR04、RR07），不再用主線 S02／S04／S05；新增 R0 老師 Google 預授權（身分 C，可選）。
>
> v1.2（2026-09-13，Codex 第三輪）：副本對外入口改為插槽 host `https://b1.fju.roy422.dev`（Google callback 與 Turnstile 已登記；O1）。

## 1. 目的與案例
還原與去識別化、批次停用與匯出（Codex）；Google 註冊、連結與搶綁、老師 Google 預授權（Roy 用自備測試身分，副本內新帳號 SG1／SG2／TG1，不用主線帳號）。案例：ACC-14、ACC-15；人工 Roy：ACC-13、16、18（R1–R3）與 ACC-07 的 Google 預授權路徑（R0，可選）。

## 2. 環境、版本、前置、角色
隔離副本：從主線 P02 完成後的 `pg_dump`＋附件目錄複本建 `fju-branch-b01`（獨立 DB、附件目錄、worker 指向副本；對外入口是插槽 host `https://b1.fju.roy422.dev`，副本 `BETTER_AUTH_URL` 指向它，Google callback 與 Turnstile hostname 已登記該 host；每個角色用獨立瀏覽器 profile）；同版本；業務鐘沿用；角色 A1、S15（已停用）、S04、S10、S12；Roy 的 Google 測試身分 A、B（、C）與副本內新帳號 SG1、SG2、TG1；R 步驟與 Codex 步驟 1–6 互不使用同一帳號，先後皆可。

## 3. 起點
A1：`/dashboard/admin/accounts`。

## 4–5. 逐步操作與預期
| 步驟 | 角色 | 操作 | 預期 |
|---|---|---|---|
| 1 | A1 | 還原 S15（理由） | active；`student_identities` 重插；S15 可登入 |
| 2 | A1 | 先讓另一人以 S15 學號核准（不可能：占用）→改測：停用 S15 後核准同學號新申請 SX0→再還原 S15 | 還原被拒 `STUDENT_NO_TAKEN` |
| 3 | A1 | 對 S04 去識別化：影響預覽（提案、組別、回答、簽核參與） | 預覽列出關聯；二次確認 |
| 4 | A1 | 確認 | 姓名、Email、手機清空置換；歷史回答與 audit 仍存在且關聯到匿名 ID |
| 5 | A1 | 批次停用（勾 S10、S12）預覽→執行 | 兩人停用；各自 session 撤銷 |
| 6 | A1 | 匯出帳號 CSV | 學號文字、無密碼、無秘密 |
| R0 | Roy（可選） | A1 預授權老師 TG1（Email＝身分 C）→Roy 以 C 的 Google 首次登入 | 補資料頁→老師首頁；ACC-07 Google 路徑 |
| R1 | Roy | 以身分 A 在 `/register` 用 Google 註冊 SG1→等待審核頁→A1 核准→登出→再以 Google 登入 | 同一帳號；ACC-13 |
| R2 | Roy | 以密碼註冊 SG2（登入 Email＝身分 B 的信箱）→A1 核准→SG2 登入後在 `/account` 連結 Google（身分 B） | 兩種方式都能登入；ACC-16 |
| R3 | Roy | SG2 嘗試連結身分 A（已綁 SG1） | `ACCOUNT_LINK_CONFLICT`；SG1 在 `/account` 設密碼後兩種登入；ACC-18 |

## 6. 持久化序列
步驟 1、4、5：重載列表；S15 登入；SQL：`user_status_events`、`student_identities`、`user_profiles`（去識別後）。

## 7. 授權與直接請求
- S10 停用後舊 cookie：`UNAUTHENTICATED`。
- S05 直接請求去識別 action：`FORBIDDEN`。
- 未登入 `/api/auth/link-social`：401；非 fresh session：`FRESH_SESSION_REQUIRED`。

## 8. 證據
還原回執、`STUDENT_NO_TAKEN`、預覽截圖、去識別後列表與 SQL、批次預覽、匯出檔、Roy 的三案截圖（信箱遮罩；帳號頁兩種方式、衝突文案）與 `accounts` 表 provider 欄輸出（無 token）。

## 9. 判定
ACC-14（1–4）、ACC-15（5、6）→PASS 需四欄；ACC-13／16／18 由 Roy 提供證據（R1–R3），Codex 只記 BLOCKED（人工）；R0 完成時補 ACC-07 的 Google 路徑證據。

## 10. 失敗後
副本可整個丟棄重建；不回填主線；主線來源樣本 hash 不變。
