---
type: codex-script
script: B05
kind: branch
project: FJU IM Project
updated: 2026-09-12
revision: v1.1
status: NOT_RUN
---
# B05｜簽核分支（隔離副本）

> v1.1（2026-09-12，Codex 第二輪）：起點 P08 時「期中結果確認」已存在，改以重置產生 v2 再重派（RR07）；重派後不自動建版，S03 舊頁看到「等待管理員建立新版」（RR05）。

## 1. 目的與案例
重置後的新版、參與者改變與舊頁（不自動建版）、同意紀錄內容與匯出、改字被拒。案例：SGN-09、SGN-11（SGN-07 重置補證）。

## 2. 環境、版本、前置、角色
隔離副本 `fju-branch-b05`：起點主線 P08 完成後（G1「期中結果確認」v1 complete、「最終文件授權」v1 complete）；角色 A1、T1、T2、S01–S05。

## 3. 起點
A1：G1 的「期中結果確認」package（已有 v1 complete）。

## 4–5. 逐步操作與預期
| 步驟 | 角色 | 操作 | 預期 |
|---|---|---|---|
| 1 | A1 | 對 G1「期中結果確認」重置（理由）→v2 collecting；S01、S02 同意 | v1 保留 complete 歷史；v2 進度 2／5 |
| 2 | S03 | 開簽核頁保持不動 | — |
| 3 | A1 | 重派 G1 主指導 T1→T2 | v2 superseded（cause=advisor_change）；**不自動建 v3**；package 頁顯示「等待管理員建立新版」 |
| 4 | S03 | 在舊頁按同意 | `VERSION_SUPERSEDED`；顯示「此版本已失效（主指導變更），等待管理員建立新版」，無新版入口 |
| 5 | A1 | 建 v3 | 進度 0／5；參與者含 T2；S03 重載後看到新版入口 |
| 6 | T1 | 開 G1 簽核 | 拒（失權） |
| 7 | T2 | 開 | 可讀；學生未全同意前同意→`STUDENTS_PENDING` |
| 8 | 全員 | 同意→T2 | complete |
| 9 | A1 | 匯出可列印頁與 CSV | 欄位齊全（模組 07 §2） |
| 10 | Fable | 整合測試：對 `signoff_package_versions.content_text` UPDATE、對 `approvals` UPDATE（以 `fju_app`） | 被拒（測試輸出） |

## 6. 持久化序列
SQL：`signoff_version_status` v1 complete（歷史）、v2 superseded、v3 complete；步驟 3 後 `signoff_package_versions` 只有 v1、v2（無自動 v3）；`approvals` v2 兩列保留、v3 六列。

## 7. 授權與直接請求
S03 舊 cookie 直接 POST approve v2：`VERSION_SUPERSEDED`；T1 直接 POST teacherApprove v3：`NOT_PARTICIPANT`；A1 直接 POST approve 代 S04：`FORBIDDEN`。

## 8. 證據
四份證據（舊頁拒絕、舊老師失權、新人可讀、進度重算）截圖與 JSON、匯出檔、測試輸出。

## 9. 判定
SGN-09（1–8）、SGN-11（9、10）。

## 10. 失敗後
副本丟棄重建；不回填主線。
