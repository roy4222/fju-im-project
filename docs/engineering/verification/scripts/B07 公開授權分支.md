---
type: codex-script
script: B07
kind: branch
project: FJU IM Project
updated: 2026-09-12
revision: v1.3
status: NOT_RUN
---
# B07｜公開授權分支（隔離副本）

> v1.1（2026-09-12，Codex 第二輪）：依授權範圍改寫（RR06）：素材替換先撞 `AUTHORIZATION_NOT_COVERED`，需新授權版本；撤權測試改為暖快取→停 worker→作廢→直接請求→啟 worker（RR08）；步驟 1–8 改 1–12。
>
> v1.2（2026-09-13，Codex 第三輪）：起點 P09 完成後 115 已封存，副本內先解封（步驟 0）再做所有寫入，不放寬封存唯讀規則（B4）。
>
> v1.3（2026-09-13，Codex 第四輪 C4）：步驟 1–3、5 改為「改草稿→儲存成功→按發布」才期待拒絕；草稿儲存不檢查授權。

## 1. 目的與案例
授權未涵蓋（用途、素材版本）、個資檢查、新授權版本、未核閱素材、歷屆外部依據、授權失效即時擋且與 worker 無關。案例：SHW-11（含補證 SHW-03、06）。

## 2. 環境、版本、前置、角色
隔離副本 `fju-branch-b07`：起點主線 P09 完成後（115 已封存；副本內先解封，步驟 0）；對外入口插槽 host `https://b2.fju.roy422.dev`；角色 A1、S06–S10、T2、訪客；worker 可停啟（Fable）。

## 3. 起點
A1：`/dashboard/admin/showcase`。

## 4–5. 逐步操作與預期
| 步驟 | 角色 | 操作 | 預期 |
|---|---|---|---|
| 0 | A1 | 解封 115（理由；副本內） | active；封存期間建版、同意、發布等寫入依產品規則被拒，本劇本的寫入都在解封後 |
| 1 | A1 | 開 G2 精選草稿（P08 步驟 11 建）；把授權引用改為 G2「期中結果確認」目前版本（無公開用途）→**儲存草稿**→按「發布」 | 儲存成功（草稿不檢查授權）；發布拒 `AUTHORIZATION_NOT_COVERED`（用途不符）；草稿仍保存 |
| 2 | A1 | 引用改回 G2 最終文件授權 v1；摘要加入 Email→儲存草稿→發布 | 儲存成功；發布拒 `PII_DETECTED`（先於涵蓋檢查） |
| 3 | A1 | 摘要改回；換海報檔（新 checksum）→儲存草稿→發布 | 儲存成功；發布拒 `AUTHORIZATION_NOT_COVERED`（素材版本不在授權範圍） |
| 4 | A1、S06–S10、T2 | A1 建 G2 最終文件授權 v2（範圍含新海報）；全員與 T2 同意 | v1 superseded（content_change）、v2 complete |
| 5 | A1 | 引用改為 v2→儲存草稿→未核閱新海報就發布 | 儲存成功；發布拒 `ASSET_NOT_APPROVED` |
| 6 | A1 | 核閱新海報→發布 | 公開 |
| 7 | A1 | 歷屆補登缺核可人；補齊後（含 `coverage`） | 拒絕；成功 |
| 8 | 訪客、Codex | 開 G2 精選列表、詳頁、JSON、OG、海報下載各一次並記錄回應 hash（暖快取） | 全部可見 |
| 9 | Fable | `docker compose stop worker`；確認 `/api/health` 的 `worker.lastTickAt` 不再前進 | worker 停止 |
| 10 | A1 | 作廢 G2 最終文件授權 v2（理由） | 回執；`signoff_version_status=void` |
| 11 | 訪客 | 立即直接請求步驟 8 的全部端點 | 全部被擋（404／空）；SQL：`showcase_entries.status` 仍 published（worker 未整理）→證明閘門不靠 worker |
| 12 | Fable | `docker compose start worker` | 撤稿整理；`showcase_entries.status=withdrawn`；tag 失效 |

## 6. 持久化序列
`showcase_versions.authorization_ref` 與 `summary_checksum`、`signoff_package_versions.authorization_scope`（v1、v2）、`asset_reviews` 兩筆 checksum、`external_authorizations.coverage`、步驟 11 時 `showcase_entries.status=published`、步驟 12 後 withdrawn。

## 7. 授權與直接請求
訪客直接請求各公開端點（步驟 8、11）；S01 直接請求精選 action：`FORBIDDEN`。

## 8. 證據
三種拒絕截圖與 JSON、公開頁 HTML 掃描、停 worker 窗口內（步驟 9–11）的作廢回執與全部端點回應（含時間戳）、SQL。

## 9. 判定
SHW-11（0–12；步驟 0 為前置）；SHW-03／06 補證。

## 10. 失敗後
副本丟棄重建；不回填主線。
