---
type: codex-script
script: B02
kind: branch
project: FJU IM Project
updated: 2026-09-12
revision: v1.1
status: NOT_RUN
---
# B02｜分組、指導與產學分支（隔離副本）

> v1.1（2026-09-12，Codex 第二輪）：副本內新帳號改 SX1–SX6（SX6 供換成員，RR07）；步驟 10、12 的簽核預期改為「目前版本 superseded、不自動建新版」（RR05）；新增步驟 16 T3 發布 C2，原 16–19 改 17–20。

## 1. 目的與案例
提案終止與釋放、提案期限與撤回作廢、組長更換、真並發認領、重派含評分連動、類型衝突、換成員與解散、批次指派、合作案解除與換案、解散後閱覽、分組事件通知。案例：GRP-04、10、11、12、13、14、17、18、19、20；NTF-15。

## 2. 環境、版本、前置、角色
隔離副本 `fju-branch-b02`：起點主線 P08 完成後（有評分與簽核）；業務鐘回到 2026-09-15（成組期，只在副本）；角色 A1、T1、T2、T3、S01–S13 與副本內新註冊並核准 SX1–SX6（六人；SX1–SX5 組 G0，SX6 不入組，供步驟 12）。副本前置：主線的 C2 只是草稿，步驟 16 由 T3 先發布才可連結。

## 3. 起點
SX1：`/dashboard/student/group`。

## 4–5. 逐步操作與預期
| 步驟 | 角色 | 操作 | 預期 |
|---|---|---|---|
| 1 | SX1 | 提案 G0（SX1–SX5）；SX2、SX3 確認；SX4 拒絕 | 終止（declined）；全員釋放；通知 |
| 2 | SX1 | 重發；全員確認前 SX2 撤回確認 | 終止（member_withdrew）；再重發需全員重確認 |
| 3 | SX1 | 重發；提案人撤回 | 終止 |
| 4 | SX1 | 重發；推鐘超過到期 | worker 終止（expired）；釋放 |
| 5 | SX1 | 重發；A1 作廢（理由） | 終止（admin_voided） |
| 6 | SX1 | 重發；到期時間顯示＝min(預設天數, 成組截止) | 顯示正確 |
| 7 | SX1 | 全員確認成立 G0（SX1 組長） | 成立 |
| 8 | SX1 | 換組長給 SX2（成組期、無指導、無案） | 成功；歷史 |
| 9 | A1 | 建未指派產學組 GX；T2、T3 以整合測試同步屏障並發認領（Fable 跑 `advisor.claim.concurrency.test`）；瀏覽器只做先後 | 測試輸出一成功一 `ALREADY_CLAIMED` |
| 10 | A1 | 重派 G1 主指導 T1→T2：對話框列 T1 的期中與期末評分指派，預設不勾；不勾執行 | 簽核目前版本 superseded（不自動建新版，等 A1 建）；評分不動；T1 仍可看已送分 |
| 11 | A1 | 對 G3 改類型為產學（已有主指導） | 拒絕；管理員路徑處理關聯後改 |
| 12 | A1 | G1 換成員：移出 S05、加入 SX6（未在任何組）；預覽→執行 | 歷史快照；簽核目前版本 superseded（不自動建版）；通知聯集；S05 之後可被邀請 |
| 13 | A1 | 移出組長 S01 未指定接任 | 拒絕；指定 S02 後成功 |
| 14 | A1 | 批次指派 CSV（先選屆別；六類預覽：正常、組不存在、老師不存在、已指派、重複列、revision 過期） | 逐列結果 |
| 15 | T2 | 解除 G2 與 C1 連結（理由） | 通知；G2 無案 |
| 16 | T3 | 發布 C2（副本內；主線只有草稿） | 已發布；可連結 |
| 17 | S06 | 換案連結 C2（已發布） | 成功 |
| 18 | T3 | 下架 C2 | 既有連結顯示「已下架」；不收新連結 |
| 19 | A1 | 解散 G2（預覽→理由→執行） | 05 凍結、06 停止、07 作廢；通知 |
| 20 | S06、S07、T2、T3、A1、S01 | 各自開 G2 歷史 | 依當時有效授權：S06／S07 唯讀、T2 唯讀、T3 拒、S01 拒、A1 可查 |

## 6. 持久化序列
步驟 1–5：`group_proposals` 各終止種類一筆；`proposal_occupancy` 為空；步驟 12：`group_memberships` S05 有 valid_to、SX3 有效；`signoff_version_status` 目前版本 superseded 且 `signoff_package_versions` 列數不變（無自動新版）；步驟 19：`groups.status=dissolved`、`evaluation_status` draft→invalidated、`signoff_version_status=void`、`drafts` 凍結。

## 7. 授權與直接請求
- SX4 拒絕後直接 POST confirm：`PROPOSAL_NOT_OPEN`。
- 非組長 S07 換案：`FORBIDDEN`。
- T3 對 G2 重派：`FORBIDDEN`。
- 解散後 S06 直接 POST saveDraft：`GROUP_DISSOLVED`。

## 8. 證據
每種終止的提案頁與通知、到期顯示、組長歷史、並發測試輸出、重派對話框截圖、換成員預覽與快照、批次預覽六類、連結歷史、下架顯示、解散預覽與六角色回應、SQL。

## 9. 判定
GRP-04（1）、GRP-12（2–6）、GRP-18（8、13）、GRP-10（9：瀏覽器＋整合測試都要）、GRP-11（10）、GRP-14（11）、GRP-13（12、13、19）、GRP-17（14）、GRP-19（15、17、18）、GRP-20（20）、NTF-15（各步通知）。

## 10. 失敗後
副本丟棄重建；不回填主線；並發測試失敗記缺陷 S1。
