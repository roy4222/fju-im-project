---
type: codex-script
script: P08
kind: mainline
project: FJU IM Project
updated: 2026-09-12
revision: v1.1
status: NOT_RUN
---
# P08｜期末繳交、評分與最終授權（年度主線 §9）

> v1.1（2026-09-12，Codex 第二輪）：新增 G3 最終結果更正（步驟 8，GRD-08）；先建精選草稿（步驟 11）再建最終文件授權簽核並固定授權範圍（步驟 12，RR06）；§6 SQL 改 join `signoff_version_status`、說明最終結果不落表；步驟編號 8–14 改為 9–16。

## 1. 目的與案例
FINAL1 開放前後 hash 不變；三組送期末；期末指派與評分；最終加權 85.287→85.29；G3 最終結果更正；先建精選草稿、再建最終文件授權簽核並把授權範圍固定在版本內；同意紀錄匯出。案例：GRD-06、GRD-08；補證 SUB-08、SUB-09、SUB-10、SUB-18；GRD-04、GRD-05、GRD-10、GRD-11；SGN-01–10；子步驟 SGN-11（匯出內容；最終 S11 於 B05 含改字被拒）、SHW-03（精選草稿建立；最終 S12 於 P09）。

## 2. 環境、版本、前置、角色
staging；P07 完成；業務鐘 2027-01-31 23:59→2027-02-01→05-31；角色全部。

## 3. 起點
Codex：記 MID1 各版本 hash；推鐘。

## 4–5. 逐步操作與預期
| 步驟 | 角色 | 操作 | 預期 |
|---|---|---|---|
| 1 | Codex | 推鐘 2027-02-01 00:00:00 | FINAL1 開放；MID1 hash 不變；期中結果不變 |
| 2 | S02、S07、S12 | 各組送 FINAL1（PDF） | 各版本 1 |
| 3 | S01 | G1 重送 | 版本 2 |
| 4 | A1 | 名單頁與附件 | 3／3；附件可下載 |
| 5 | A1 | 期末指派：G1→T1、T3（要求 2）；G2→T2；G3→T3 | 通知 |
| 6 | T1、T3 | G1 期末 90、90 | 期末 90；最終 82.145×0.6＋90×0.4＝85.287 顯示 85.29 |
| 7 | T2、T3 | G2 期末 85；G3 期末 80 | 3／3；G3 最終 88×0.6＋80×0.4＝84.8 |
| 8 | A1 | G3 最終結果更正 84.8→85（理由） | 原值與新值都顯示；`grade_overrides` 一列＋`override_review_state=effective`（GRD-08） |
| 9 | A1 | 匯出最終成績 | 檔案含期中、期末、最終（兩位）與更正註記 |
| 10 | S01 | 學生頁 | 無分數 |
| 11 | A1 | 建 G1、G2、G3 精選草稿（題目、摘要、海報 PNG 上傳、影片連結；不發布） | 草稿存在；海報「待核閱」；閘門顯示「尚無授權」（子步驟 SHW-03） |
| 12 | A1 | 建「最終文件授權」簽核 G1、G2、G3（新全文、附件 FINAL1 版本、授權範圍取自各組精選草稿：用途 public_showcase、題目、摘要 checksum、海報 checksum、影片連結） | 各 v1；版本頁在全文後列出授權範圍；不沿用期中同意 |
| 13 | S01–S05、T1 | G1 逐人同意→老師 | complete |
| 14 | S06–S10、T2；S11–S13、T3 | 同上 | 各 complete |
| 15 | A1 | 匯出 G1 同意紀錄（可列印頁與 CSV） | 含屆別、組別、用途、版本與 checksum、附件版本、授權範圍、參與者、帳號、當時姓名學號、角色、登入方式（來自 session）、按鈕原文、結果、雙時間；頁面標「站內內容確認與同意紀錄，行政採認待確認」 |
| 16 | S02 | 舊頁（v1 完成後）再按同意 | `ALREADY_VOTED`／唯讀 |

## 6. 持久化序列
步驟 3：S01 登出登入→FINAL1 兩版；T1 看到；SQL：`submission_versions` FINAL1 G1 兩列。步驟 6：重載→85.29；計算明細 API 回 `{final: "85.287", display: "85.29"}`（最終結果不落表，每次以 decimal 重算；證據是計算明細 JSON 與匯出檔，不是資料庫欄位）；`grade_overrides` 對 G1 零列。步驟 8：`grade_overrides` G3 一列（`original_value=84.8000`、`new_value=85.0000`）＋`override_review_state=effective`。步驟 14：`SELECT p.group_id, s.state FROM signoff_packages p JOIN signoff_version_status s ON s.version_id=p.current_version_id WHERE p.purpose='final_document'` 三列皆 complete；`signoff_package_versions.authorization_scope` 三版皆非空且含海報 checksum；`approvals` 每版人數等於參與者數。

## 7. 授權與直接請求
- S06 直接請求 G1 FINAL1：`FORBIDDEN`。
- T3 對 G2 期末送分：`NOT_ASSIGNED`。
- 訪客直接請求匯出：401；S01 請求匯出：`FORBIDDEN`。
- 學生 API 掃描：無分數。

## 8. 證據
推鐘前後 hash 表、各版本回執、計算明細（85.287／85.29、84.8）、更正回執、匯出檔、學生頁掃描、精選草稿截圖、授權範圍截圖、簽核進度、匯出的可列印頁 PDF 與 CSV、`ALREADY_VOTED` 回應、SQL。

## 9. 判定
GRD-06（6）PASS 條件：decimal 值與顯示值都符；GRD-08（8）；SUB-08／09／10／18、GRD-04／05／10／11、SGN-01–10 補證存 `cases/<ID>.json` 的 evidence 追加；SGN-11 子步驟 `steps/P08-15.json`；SHW-03 子步驟 `steps/P08-11.json`。主線結束條件：有效 13 人、3 組、期中 3／3、期末 3／3、正式評分 3／3、最終授權 3／3；未達即列缺口，不跳過。

## 10. 失敗後
期末送錯：重送新版本；評分錯：退回；簽核錯：重置。換版本→主線重新開始。
