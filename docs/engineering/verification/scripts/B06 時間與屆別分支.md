---
type: codex-script
script: B06
kind: branch
project: FJU IM Project
updated: 2026-09-12
revision: v1.1
status: NOT_RUN
---
# B06｜時間與屆別分支（隔離副本）

> v1.1（2026-09-12，Codex 第二輪）：SX1 在副本內註冊並核准（步驟 0；此時開放註冊屆別為 116），跨屆調整改為 116→115（RR07）。

## 1. 目的與案例
跨屆調整、封存與解封、模擬日期倒退與跨截止、通知匣範圍與封存已讀。案例：COH-08、09、10；NTF-20。

## 2. 環境、版本、前置、角色
隔離副本 `fju-branch-b06`：起點主線 P09 完成後（115 封存、116 存在）；角色 A1、S01、T1，副本內新註冊並核准 SX1（步驟 0；開放註冊屆別此時是 116）；worker 可停啟（Fable 執行）。

## 3. 起點
A1：`/dashboard/admin/cohorts`。

## 4–5. 逐步操作與預期
| 步驟 | 角色 | 操作 | 預期 |
|---|---|---|---|
| 0 | SX1、A1 | SX1 密碼註冊（開放註冊屆別＝116）→A1 核准 | SX1 屬 116-TEST |
| 1 | A1 | 解封 115（理由） | active；期限不延長；已撤銷關係不恢復；不重送通知；進入顯示「已解封」 |
| 2 | S01 | 舊頁存草稿 | 仍受原截止限制（`DEADLINE_PASSED`），不是 `COHORT_ARCHIVED` |
| 3 | A1 | 再封存 | 第二筆狀態事件 |
| 4 | A1 | 跨屆調整 SX1（116→115，理由；115 已於步驟 1 解封） | 116 名單移除、待辦消失；115 待辦出現；歷史保留（COH-08） |
| 5 | Codex | 設鐘 2026-11-16 23:59:50；建副本內項目 FX 截止 11/16 23:59；停 worker；推鐘到 11/17 00:02；啟 worker | 快照與彙整各一次 |
| 6 | Codex | 鐘回撥到 11/16 23:00 | 已作廢提案不復活、封存不變、快照不重做；FX 顯示「已截止（依快照）」或允許送出？→依規則：截止判定用當前業務時間，回撥後可再送但 `deadline_version` 不變，快照不重做；記錄行為 |
| 7 | S01 | 通知匣 | 顯示 115 與 116 全部；可篩屆別；封存屆通知可標已讀 |
| 8 | A1 | 兩屆並行：116 建項目 | S01 與 SX1（皆在 115）待辦不出現 |

## 6. 持久化序列
SQL：`cohort_status_events` 三筆、`business_clock_overrides` 含前後值與原因、`due_work` 兩筆 done、`notifications.read_at`。

## 7. 授權與直接請求
S01 直接請求 116 項目：`FORBIDDEN`；SX1 跨屆後直接請求 116 項目：`FORBIDDEN`；production 開模擬鐘頁：404（由 Fable 在 SOP 06 預演證明）。

## 8. 證據
解封回執、`DEADLINE_PASSED` 回應、狀態事件 SQL、鐘紀錄、worker 日誌、通知匣篩選截圖。

## 9. 判定
COH-09（1–3）、COH-08（4）、COH-10（5、6）、NTF-20（7）；步驟 6 的行為必須與模組 02 §2「回撥不復活作廢與封存」一致，其他細節若規格未寫→回產品待討論，不算缺陷。

## 10. 失敗後
副本丟棄重建；不回填主線。
