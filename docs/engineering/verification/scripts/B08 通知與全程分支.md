---
type: codex-script
script: B08
kind: branch
project: FJU IM Project
updated: 2026-09-12
status: NOT_RUN
---
# B08｜通知、去重補建與全程共用 UI 分支（隔離副本）

## 1. 目的與案例
額外事件、通知不能越權、去重與補建、共用互動與無障礙、導航與回執。案例：NTF-07、14、19；SHW-08、09（SHW-08 真機部分人工 Roy）。

## 2. 環境、版本、前置、角色
隔離副本 `fju-branch-b08`：起點主線 P06 完成後；角色 A1、S01、S05、T1；worker 可停啟；Codex 瀏覽器可設 viewport 360／390 與純鍵盤操作。

## 3. 起點
S01：通知匣。

## 4–5. 逐步操作與預期
| 步驟 | 角色 | 操作 | 預期 |
|---|---|---|---|
| 1 | A1 | 觸發額外事件：組別解散（副本內 G3）、更正待復核、逾期彙整、備份失敗（Fable 模擬） | 各對應收件人收到；首頁計數型顯示 |
| 2 | Fable | 停 worker；A1 發布公告；啟 worker | 通知一次；`event_projections` done |
| 3 | Fable | 注入：投影寫通知後中斷（測試鉤）；重啟 | 不重複（唯一鍵）；`notifications` 一筆 |
| 4 | Fable | 毒事件→failed；管理端重設 pending | 成功且不重複 |
| 5 | A1 | 把 S05 移出 G1 | S05 通知匣舊通知顯示「此項目目前無法存取」；點入 `FORBIDDEN` |
| 6 | A1 | 撤回一則無回答項目 | 通知顯示「來源已撤回」 |
| 7 | Codex | 360／390 viewport：登入、待辦、繳交、通知匣、返回保留篩選 | 核心資訊可達；無橫向捲動 |
| 8 | Codex | 純鍵盤：Tab 順序、Enter 送出、Esc 關對話框 | 可操作；焦點可見 |
| 9 | Codex | 403 頁 | 帶下一步連結；空頁文案一致 |
| 10 | Codex | 每個成功動作 | 回執可信（含時間與 ID）；導航一致 |
| R1 | Roy | 真機 iOS／Android 走核心路徑 | 人工證據 |

## 6. 持久化序列
`domain_events`、`event_projections`（attempts、state）、`notifications` 唯一；已讀跨登入。

## 7. 授權與直接請求
S05 直接請求被移出後的來源：`FORBIDDEN`；S01 直接請求他人通知：`FORBIDDEN`。

## 8. 證據
通知匣截圖、SQL、worker 日誌、測試輸出（中斷注入）、viewport 截圖、鍵盤操作錄影或截圖序列、403 截圖。

## 9. 判定
NTF-07（1）、NTF-19（2–4：瀏覽器＋整合測試）、NTF-14（5、6）、SHW-08（7、8；真機由 Roy 才 PASS，否則 BLOCKED（人工））、SHW-09（9、10）。

## 10. 失敗後
副本丟棄重建；不回填主線。
